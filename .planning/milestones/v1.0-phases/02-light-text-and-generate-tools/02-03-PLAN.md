---
phase: 02-light-text-and-generate-tools
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/lib/lorem.ts
  - src/lib/lorem.test.ts
  - src/components/tools/LoremIpsum.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/i18n/errors.ts
  - src/content/tools/lorem-ipsum.md
  - src/content/tools/zh/lorem-ipsum.md
autonomous: true
requirements:
  - LORM-01
  - LORM-02
  - LORM-03
  - LORM-04
  - LORM-05

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can generate dummy Latin on /tools/lorem-ipsum/ from an embedded WORDS list with no network client and no npm lorem package (LORM-01, D-08, T-02-05)"
    - "User chooses words or paragraphs and a count of at least 1; words mode is N space-separated words ending with a period (LORM-02, D-08)"
    - "Classic opening is a boolean on generateLorem: when true, prefix Lorem ipsum dolor sit amet then continue the corpus; not a second catalog identity (LORM-03, D-09, assumption_delta no-change)"
    - "ToolShell Copy works on the generated body (LORM-04); chrome/labels EN+ZH while body stays Latin (LORM-05, D-14)"
    - "Invalid count returns English Enter a count of at least 1 with ZH_ERRORS in this same slice"
    - "Generation is round-robin from index 0, not a PRNG; source-read of lorem.ts has no fetch"
    - "Catalog slug lorem-ipsum, category Generate, featured false; TOOLS toHaveLength 13; featured stays 6; existing ten relatedSlugs untouched (D-03, D-13)"
  artifacts:
    - path: src/lib/lorem.ts
      provides: "WORDS embedded corpus; generateLorem words|paragraphs classic boolean"
      exports: ["WORDS", "generateLorem"]
    - path: src/lib/lorem.test.ts
      provides: "words vs paragraphs, classic prefix, Latin body, invalid count, source-read no network client"
    - path: src/components/tools/LoremIpsum.tsx
      provides: "JsonFormatter-shaped island; generate button; mode/count/classic controls"
    - path: src/components/tools/ToolIsland.astro
      provides: "static LoremIpsum import and slug-equals branch"
    - path: src/data/tools.ts
      provides: "lorem-ipsum row featured false category Generate"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 13; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['lorem-ipsum'] chrome"
    - path: src/i18n/errors.ts
      provides: "ZH_ERRORS for Enter a count of at least 1"
    - path: src/content/tools/lorem-ipsum.md
      provides: "locale en SEO howTo faq"
    - path: src/content/tools/zh/lorem-ipsum.md
      provides: "locale zh SEO howTo faq"
  key_links:
    - from: src/components/tools/LoremIpsum.tsx
      to: src/lib/lorem.ts
      via: "generateLorem from island, not inlined"
      pattern: "generateLorem"
    - from: src/lib/lorem.ts
      to: WORDS
      via: "embedded const array; round-robin"
      pattern: "export const WORDS"
    - from: src/i18n/errors.ts
      to: generateLorem error string
      via: "ZH_ERRORS same-slice key"
      pattern: "Enter a count of at least 1"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/LoremIpsum.tsx
      via: "static import and slug === 'lorem-ipsum'"
      pattern: "slug === 'lorem-ipsum'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(13)"
      pattern: "toHaveLength\\(13\\)"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** generate local lorem dummy text by words or paragraphs with an optional classic opening, **so that** I can copy Latin filler without a network request.

<objective>
Ship the lorem-ipsum 8-file vertical slice (LORM-01..05). Clone JsonFormatter plus json.ts, not UuidGenerator (D-02). Embed classic Latin WORDS in src/lib/lorem.ts; no network client; no npm lorem package (D-08). Words mode = N space-separated words ending with a period (D-08). Classic opening is a boolean option on generateLorem, not a second catalog slug (D-09; assumption_delta_decision: noun catalog tool slug unchanged, decision no-change, rationale classic opening is a boolean option on generateLorem, not a second identity). Category Generate, featured false (D-03). ZH_ERRORS for the count error in this slice (D-14). Do not rewrite existing ten relatedSlugs (D-13). Round-robin, not a PRNG. No p wrapping (deferred LORM-06). Zero packages. No lib barrel.
Purpose: Third sequential slice; completeness at catalog length 13.
Output: lorem lib/test, LoremIpsum island, ToolIsland branch, catalog row, ui.ts, ZH_ERRORS, EN+ZH markdown; tools.test.ts length 13.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-light-text-and-generate-tools/02-CONTEXT.md
@.planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md
@.planning/phases/02-light-text-and-generate-tools/02-PATTERNS.md
@.planning/phases/02-light-text-and-generate-tools/02-01-PLAN.md
@.planning/phases/02-light-text-and-generate-tools/02-02-PLAN.md
@src/lib/json.ts
@src/components/tools/JsonFormatter.tsx
@src/components/tools/ToolIsland.astro
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/i18n/errors.ts
@src/i18n/useToolUi.ts
@src/content/tools/json-formatter.md
@src/components/tools/ToolIsland.test.ts
@.planning/codebase/CONVENTIONS.md

## Assumption delta (locked)

Detector fired on ROADMAP prose optional classic opening for lorem. This is a UI toggle, not an identity-model change.
- noun: catalog tool slug (unchanged)
- decision: no-change
- rationale: classic opening is a boolean option on generateLorem, not a second identity.
Recorded here so executors do not add a second slug or a second markdown file for classic mode.

<interfaces>
From src/lib/json.ts: result union; English errors; no throw.

From RESEARCH generateLorem: opts mode 'words' | 'paragraphs', count number, classic boolean. Invalid count → { ok: false, error: 'Enter a count of at least 1' }. WORDS const array ~80–120 classic Latin tokens (discretion). Round-robin from index 0. Classic true: prefix the five words Lorem ipsum dolor sit amet then continue. Words mode: N space-separated words ending with a period. Paragraphs: groups of about 40–60 words separated by blank lines.

From src/i18n/errors.ts: ZH_ERRORS Record; add 'Enter a count of at least 1' → '请输入至少为 1 的数量' in this slice (D-14).

From src/data/tools.ts after 02-02: 12 rows. Append lorem-ipsum. Forward relatedSlugs OK.

From ToolIsland.test.ts: source.includes(`slug === '${slug}'`) for every TOOLS slug.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Lorem-ipsum 8-file slice — embedded WORDS, generateLorem, catalog, island, EN/ZH</name>
  <precondition>Plan 02-02 is complete: TOOLS snapshots at 12, case-converter ToolIsland branch exists, featured length is 6</precondition>
  <reversibility rating="costly">Public catalog slug lorem-ipsum becomes an SSG route; renaming later needs content, island, and i18n updates together (D-03, D-09).</reversibility>
  <files>src/lib/lorem.test.ts, src/lib/lorem.ts, src/components/tools/LoremIpsum.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/lorem-ipsum.md, src/content/tools/zh/lorem-ipsum.md</files>
  <read_first>src/lib/json.ts, src/lib/json.test.ts, src/components/tools/JsonFormatter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/json-formatter.md, src/components/tools/ToolIsland.test.ts, .planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md</read_first>
  <behavior>
    - generateLorem({ mode: 'words', count: 0, classic: false }) → { ok: false, error: 'Enter a count of at least 1' } (same for negative count)
    - generateLorem({ mode: 'words', count: 5, classic: false }) ok true; text is five space-separated Latin words ending with a period
    - generateLorem({ mode: 'words', count: 5, classic: true }) ok true; text starts with Lorem ipsum dolor sit amet
    - generateLorem({ mode: 'paragraphs', count: 2, classic: false }) ok true; two paragraphs separated by a blank line
    - generated body matches Latin letters, spaces, commas, periods only
    - source-read src/lib/lorem.ts via readFileSync(new URL('./lorem.ts', import.meta.url)): the substring fetch is absent (same technique as ToolIsland.test.ts)
  </behavior>
  <action>
Ship the lorem-ipsum 8-file slice (LORM-01..05, D-02, D-03, D-08, D-09, D-13, D-14). Classic opening is a boolean on generateLorem, not a second catalog identity (assumption_delta no-change). Clone JsonFormatter, not UuidGenerator. Do not install an npm lorem package. Do not create a lib barrel. Do not rewrite existing ten relatedSlugs. Do not wrap paragraphs in HTML p tags (deferred LORM-06). Leave unrelated dirty worktree files alone.

1. Write src/lib/lorem.test.ts from the behavior block, then src/lib/lorem.ts. Export const WORDS as a classic Latin token list of about 80–120 words (discretion). Export generateLorem(opts: { mode: 'words' | 'paragraphs'; count: number; classic: boolean }). Count not an integer >= 1 → English error Enter a count of at least 1. Draw words by round-robin from WORDS index 0; do not use a PRNG (keeps tests deterministic). Words mode: exactly N tokens joined by spaces, then a period. Classic true: those five opening words first, then continue round-robin for the remaining count (if count is 5, the body is exactly the opening plus period). Paragraphs: count groups of about 40–60 words joined internally by spaces, groups joined by newline-newline; classic applies to the start of the first paragraph. Body Latin only. Source-read test uses readFileSync URL like ToolIsland.test.ts and asserts fetch is absent.
<!-- planner-discipline-allow: fetch -->

2. Append TOOLS: slug lorem-ipsum, name Lorem Ipsum Generator, category Generate, shortDescription Generate dummy Latin text in your browser., relatedSlugs word-counter, case-converter, password-generator, featured false. Bump tools.test.ts 12 → 13.

3. LoremIpsum.tsx cloned from JsonFormatter: default export, locale, useToolUi, ToolShell. Controls: mode words|paragraphs, numeric count default 3, classic checkbox default false, generate button labeled from ui. Call generateLorem in the island (lib import from ../../lib/lorem). isTooLarge on any text fields if present; count is a number input so still guard stringified count if it is a text field. Localize lib errors with err(). Output is the Latin text for Copy (LORM-04). Use class not className. Do not inline generation in the tsx without the lib.

4. ToolIsland.astro static import LoremIpsum; slug === 'lorem-ipsum' client:load.

5. ui.en and ui.zh tools['lorem-ipsum']: name, shortDescription, mode, words, paragraphs, count, classic, generate. Same keys both locales.

6. errors.ts ZH_ERRORS add Enter a count of at least 1 → 请输入至少为 1 的数量 in this same slice (D-14). Do not remove existing keys.

7. EN+ZH markdown: howTo 3, faq 3-5. Privacy: no network, corpus is local. Body is dummy Latin; chrome is translated. Classic opening is optional. Discretion on wording. No version-labeling language.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green at catalog length 13 featured 6; generateLorem words/paragraphs/classic/error paths pass; WORDS embedded; ToolIsland branch present; ZH_ERRORS has the count key; EN+ZH markdown exist.</done>
  <acceptance_criteria>
    - WORDS is an exported embedded array; lorem.test.ts source-read asserts fetch is absent from lorem.ts
    - Words mode emits N space-separated words ending with a period
    - classic true prefixes Lorem ipsum dolor sit amet
    - Invalid count returns Enter a count of at least 1 and ZH_ERRORS maps it
    - TOOLS lorem-ipsum category Generate featured false; toHaveLength 13; featured 6
    - ToolIsland has slug === 'lorem-ipsum'; island imports generateLorem from ../../lib/lorem
    - Single catalog slug only; classic is a boolean option, not a second route
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Paragraph grouping, classic-plus-count, and Latin-body asserts</name>
  <files>src/lib/lorem.ts, src/lib/lorem.test.ts</files>
  <read_first>src/lib/lorem.ts, src/lib/lorem.test.ts</read_first>
  <action>
Expand LORM-02/03/05 coverage on the same generateLorem API. Do not add a second slug. Do not add HTML wrapping. Do not add packages.

1. Paragraphs count 1 vs 2: two-paragraph output contains a blank line; each paragraph is non-empty Latin.

2. Classic true with words count greater than 5: starts with the five opening words then continues from the corpus without duplicating the opening as a second identity.

3. Repeat generateLorem with the same opts: identical output (round-robin determinism).

4. Keep the source-read assertion that the lib file does not contain fetch.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>Paragraph and classic-count tests green; generation deterministic; npm test green at length 13.</done>
  <acceptance_criteria>
    - paragraphs mode joins groups with a blank line
    - classic remains a boolean on generateLorem
    - same inputs yield the same Latin string
    - invalid count path unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>EN/ZH generate chrome, ZH_ERRORS, and dummy-Latin FAQ</name>
  <files>src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/lorem-ipsum.md, src/content/tools/zh/lorem-ipsum.md, src/components/tools/LoremIpsum.tsx</files>
  <read_first>src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/lorem-ipsum.md, src/content/tools/zh/lorem-ipsum.md, src/components/tools/LoremIpsum.tsx</read_first>
  <action>
Finish LORM-04/05 chrome (D-14). Do not bump catalog length. Do not rewrite existing ten relatedSlugs. Do not add a second lorem slug.

1. Island labels from copy.tools['lorem-ipsum'] including generate, mode, words, paragraphs, count, classic. err() maps the count error via ZH_ERRORS.

2. FAQ both locales: corpus is local (no upload, no remote list); output is dummy Latin; UI chrome is EN or ZH; optional classic opening. howTo 3.

3. Confirm Copy uses ToolShell output (the Latin body). Generate button type="button". No HTML injection APIs (T-02-04). No network client in the island (T-02-05).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <human-check>Open /tools/lorem-ipsum/; generate words and paragraphs; toggle classic; Copy the Latin body; /zh/ shows Chinese chrome with Latin output; featured still six.</human-check>
  </verify>
  <done>EN+ZH chrome and FAQ complete; ZH_ERRORS key present; npm test green; catalog 13 featured 6.</done>
  <acceptance_criteria>
    - ui en/zh tools['lorem-ipsum'] share mode, words, paragraphs, count, classic, generate
    - ZH_ERRORS contains Enter a count of at least 1
    - markdown faq states local corpus and Latin dummy body
    - npm test green with toHaveLength 13 and featured 6
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `WORDS`, `generateLorem` in `src/lib/lorem.ts`
- `LoremIpsum` default export in `src/components/tools/LoremIpsum.tsx`
- Catalog slug `lorem-ipsum` in `TOOLS` (classic is not a second slug)
- `ui.en.tools['lorem-ipsum']` and `ui.zh.tools['lorem-ipsum']`
- ZH_ERRORS key `Enter a count of at least 1`
- `src/content/tools/lorem-ipsum.md` and `src/content/tools/zh/lorem-ipsum.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor controls → generateLorem | Count/mode/classic stay in-browser |
| generated Latin → ToolShell / clipboard | Text only |
| island → network | Must not exist; corpus is embedded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information disclosure | src/lib/lorem.ts / LoremIpsum.tsx | high | mitigate | Embedded WORDS only; source-read test forbids a network client; no new API routes |
| T-02-03 | Denial of service | LoremIpsum count | low | mitigate | Reject count below 1; do not generate unbounded loops from non-numeric count |
| T-02-04 | Tampering | Lorem output | medium | mitigate | Latin string into ToolShell text; no HTML injection APIs |
| T-02-SC | Tampering | npm installs | high | accept | No package-manager installs; do not add an npm lorem package |

No-change on catalog identity: classic opening is not a new trust boundary or a new slug.
</threat_model>

<verification>
npm test after every task. Completeness at length 13. Source-read lorem.ts. Featured 6. No Playwright.
</verification>

<success_criteria>
- LORM-01..05 delivered for slug lorem-ipsum
- Classic opening is a boolean, not a second identity
- Catalog 13, featured 6, existing ten relatedSlugs untouched
- npm test green
</success_criteria>

<output>
Create `.planning/phases/02-light-text-and-generate-tools/02-03-SUMMARY.md` when done
</output>
