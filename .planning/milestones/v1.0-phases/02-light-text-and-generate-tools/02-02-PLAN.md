---
phase: 02-light-text-and-generate-tools
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/lib/cases.ts
  - src/lib/cases.test.ts
  - src/components/tools/CaseConverter.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/content/tools/case-converter.md
  - src/content/tools/zh/case-converter.md
autonomous: true
requirements:
  - CASE-01
  - CASE-02
  - CASE-03
  - CASE-04

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can paste on /tools/case-converter/ and see nine labeled rows: UPPER, lower, Title, camelCase, PascalCase, snake_case, kebab-case, CONSTANT_CASE, and URL slug (CASE-01, CASE-02, D-06, D-07, D-11)"
    - "Slug keeps CJK letters: 你好世界 slugifies to a non-empty string containing those Han letters; Latin diacritics strip via NFKD; punctuation becomes hyphens that collapse; no pinyin (CASE-03, D-07)"
    - "Each row has its own Copy using the same clipboard write API as ToolShell; newline-joined dump still goes to ToolShell output for copy-all (CASE-04, D-11)"
    - "Empty paste is ok false with empty error string like json parser idle; isTooLarge runs before convertCases (T-02-03)"
    - "Catalog slug case-converter, category Text, featured false; TOOLS toHaveLength 12; featured stays 6; existing ten relatedSlugs still untouched (D-03, D-13)"
    - "ToolIsland static import plus slug === 'case-converter'; EN+ZH ui and markdown howTo 3 / faq 3-5 including Han slug note"
  artifacts:
    - path: src/lib/cases.ts
      provides: "convertCases fan-out plus slugify; CaseResult union"
      exports: ["convertCases", "slugify"]
    - path: src/lib/cases.test.ts
      provides: "eight identifier cases, slug punctuation, Han non-empty slug"
    - path: src/components/tools/CaseConverter.tsx
      provides: "Nine labeled rows, per-row copy, joined ToolShell output"
    - path: src/components/tools/ToolIsland.astro
      provides: "static CaseConverter import and slug-equals branch"
    - path: src/data/tools.ts
      provides: "case-converter row featured false category Text"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 12; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['case-converter'] row labels"
    - path: src/content/tools/case-converter.md
      provides: "locale en SEO howTo faq"
    - path: src/content/tools/zh/case-converter.md
      provides: "locale zh SEO howTo faq"
  key_links:
    - from: src/components/tools/CaseConverter.tsx
      to: src/lib/cases.ts
      via: "convertCases after isTooLarge"
      pattern: "convertCases"
    - from: src/lib/cases.ts
      to: slugify
      via: "NFKD, strip marks, keep Letter and Number (Han included)"
      pattern: "slugify"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/CaseConverter.tsx
      via: "static import and slug === 'case-converter'"
      pattern: "slug === 'case-converter'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(12)"
      pattern: "toHaveLength\\(12\\)"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** paste text on case-converter and copy UPPER, lower, Title, identifier cases, and a CJK-preserving URL slug, **so that** I can transform strings entirely in the browser.

<objective>
Ship the case-converter 8-file vertical slice (CASE-01, CASE-02, CASE-03, CASE-04). Clone JsonFormatter plus json.ts for island/lib wiring (D-02). Fan-out UI like ColorConverter: nine rows plus per-row copy; joined dump to ToolShell (D-11). Title Case Unicode first letter of whitespace-separated words, no acronym table (D-06). Slug keeps CJK; NFKD Latin diacritics; no pinyin (D-07). Category Text, featured false (D-03). Do not rewrite existing ten relatedSlugs (D-13). No new English errors so no ZH_ERRORS keys (D-14). Zero packages. No lib barrel.
Purpose: Second sequential product slice after word-counter; completeness must stay green at catalog length 12.
Output: cases lib/test, CaseConverter island, ToolIsland branch, catalog row, ui.ts, EN+ZH markdown; tools.test.ts length 12.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-light-text-and-generate-tools/02-CONTEXT.md
@.planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md
@.planning/phases/02-light-text-and-generate-tools/02-PATTERNS.md
@.planning/phases/02-light-text-and-generate-tools/02-01-PLAN.md
@src/lib/json.ts
@src/components/tools/JsonFormatter.tsx
@src/components/tools/ColorConverter.tsx
@src/components/ToolShell.tsx
@src/components/tools/ToolIsland.astro
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/lib/limits.ts
@src/content/tools/json-formatter.md
@src/content.config.ts
@.planning/codebase/CONVENTIONS.md

<interfaces>
From src/lib/json.ts: empty trim → ok false error empty string. Case converter is a parser: same idle contract.

From src/lib/color.ts: ColorResult ok true value or ok false error; IDLE empty error.

From src/components/tools/ColorConverter.tsx: formatColor joins hex/rgb/hsl with newlines into ToolShell output; labeled fields live in children. Case-converter extends that to nine rows with extra per-row copy buttons. Do not edit ColorConverter or ToolShell.

From ToolShell onCopy: navigator.clipboard.writeText(props.output). Per-row copy uses the same writeText on that row's string.

From src/data/tools.ts after 02-01: 11 rows including word-counter. Append case-converter. Do not edit existing ten relatedSlugs. word-counter may already list case-converter in relatedSlugs.

From src/data/tools.test.ts after 02-01: toHaveLength 11 — bump to 12. Featured 6 unchanged.

From RESEARCH slugify: normalize NFKD, strip Unicode marks, toLocaleLowerCase, replace runs that are not Letter or Number with hyphen, collapse repeat hyphens, trim hyphens. Never filter with ASCII-only letter ranges (CASE-03).

From RESEARCH Title Case: Unicode-aware first letter of whitespace-separated words; no acronym exception table (D-06). Identifier cases (camel, Pascal, snake, kebab, CONSTANT) tokenize on whitespace, underscore/hyphen runs, and punctuation (RESEARCH A4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Case-converter 8-file slice — fan-out lib, catalog, island, EN/ZH pages</name>
  <precondition>Plan 02-01 is complete: TOOLS snapshots at 11, word-counter ToolIsland branch exists, featured length is 6</precondition>
  <reversibility rating="costly">Public catalog slug case-converter becomes an SSG route; renaming later needs content, island, and i18n updates together (D-03).</reversibility>
  <files>src/lib/cases.test.ts, src/lib/cases.ts, src/components/tools/CaseConverter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/content/tools/case-converter.md, src/content/tools/zh/case-converter.md</files>
  <read_first>src/lib/json.ts, src/lib/json.test.ts, src/components/tools/JsonFormatter.tsx, src/components/tools/ColorConverter.tsx, src/components/ToolShell.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/content/tools/json-formatter.md, .planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md</read_first>
  <behavior>
    - convertCases('') and whitespace-only → { ok: false, error: '' }
    - convertCases('hello world') ok true: upper HELLO WORLD, lower hello world, title Hello World, camel helloWorld, pascal HelloWorld, snake hello_world, kebab hello-world, constant HELLO_WORLD, slug hello-world
    - slugify('café') contains cafe (NFKD strip marks)
    - slugify('Hello, World!') is hello-world (punctuation to hyphen, collapse, trim)
    - slugify('你好世界') is truthy and includes those Han letters (CASE-03, D-07)
  </behavior>
  <action>
Ship the full 8-file case-converter slice (CASE-01, CASE-02, CASE-03, CASE-04, D-02, D-03, D-06, D-07, D-11, D-13). Do not clone UuidGenerator. Do not add packages. Do not edit ToolShell, ColorConverter, limits.ts, or existing ten TOOLS relatedSlugs. Do not create a lib barrel. Do not add sentence-case or line-by-line convert (deferred CASE-05/06). Leave unrelated dirty worktree files alone.

1. Write src/lib/cases.test.ts covering the behavior block, then implement src/lib/cases.ts. Export CaseResult as ok true with value object upper, lower, title, camel, pascal, snake, kebab, constant, slug — or ok false with error string. Export convertCases(input: string) and slugify(input: string). Empty/whitespace → idle empty error. UPPER via toLocaleUpperCase; lower via toLocaleLowerCase. Title: split on whitespace; uppercase the first Unicode code point of each non-empty word; no acronym table (D-06). Identifier cases: tokenize on whitespace, runs of underscore/hyphen, and punctuation; camel lowercases first token then capitalizes later tokens; Pascal capitalizes every token; snake/kebab/CONSTANT join with _ / - / _ and CONSTANT uppercases. slugify per RESEARCH: NFKD, strip mark characters, lowercase, keep Letter and Number (Han included), hyphenate the rest, collapse and trim hyphens. Never reduce Han to empty via ASCII-only keep sets. No pinyin.

2. Append TOOLS row: slug case-converter, name Case / Slug Converter, category Text, shortDescription Convert letter case and build a URL slug locally., relatedSlugs word-counter, regex-tester, url-encode, featured false. Bump tools.test.ts snapshot 11 → 12 and the it() title to has exactly 12 tools. Featured stays 6.

3. Create CaseConverter.tsx from JsonFormatter wiring (useToolUi, isTooLarge, locale, default export) plus ColorConverter fan-out: nine labeled rows in children for the CASE-01/02 outputs. Each row shows the string and a button that writes that row via navigator.clipboard.writeText (CASE-04, D-11). Pass newline-joined nine values as ToolShell output for copy-all. Empty result: error null or empty localized idle, output empty. Too-large: tooLarge, empty output (T-02-03). Use class not className. Do not assign HTML strings into the DOM (T-02-04). Do not edit ToolShell.

4. ToolIsland.astro: static import CaseConverter; branch slug === 'case-converter' client:load. Keep word-counter and the original ten.

5. ui.en.tools and ui.zh.tools keys for case-converter: name, shortDescription, text, upper, lower, title, camel, pascal, snake, kebab, constant, slug. Same key set both locales.

6. EN markdown src/content/tools/case-converter.md and ZH src/content/tools/zh/case-converter.md: locale, howTo exactly 3, faq 3-5. Include privacy (nothing uploaded) and that the slug keeps Han letters. Discretion on wording. No version-labeling language.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green at catalog length 12 featured 6; convertCases fans out eight cases plus slug; 你好世界 slug non-empty with Han; island has nine rows and per-row copy plus joined output; ToolIsland branch present; EN+ZH markdown exist.</done>
  <acceptance_criteria>
    - convertCases returns idle empty error for blank input and nine fields for hello world as in the behavior block
    - slugify('你好世界') is non-empty and contains those Han characters
    - CaseConverter renders nine labeled rows with per-row clipboard write and joined ToolShell output
    - TOOLS has case-converter category Text featured false; toHaveLength 12; featured 6
    - ToolIsland has slug === 'case-converter' and a static CaseConverter import
    - EN+ZH markdown howTo 3 and faq 3-5; no lib barrel; existing ten relatedSlugs unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Title Case, identifier tokenizer, and slug punctuation/diacritic edges</name>
  <files>src/lib/cases.ts, src/lib/cases.test.ts</files>
  <read_first>src/lib/cases.ts, src/lib/cases.test.ts, .planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md</read_first>
  <action>
Harden CASE-01/02/03 edges on the existing convertCases/slugify API (D-06, D-07). Do not change catalog, island, or markdown in this task. Do not add sentence case.

1. Title Case: mixed-language whitespace words; first Unicode letter of each word up, rest of that word lower; no acronym table. Test a two-word ASCII string and a string that includes Han plus ASCII.

2. Identifier tokenizer: mixed hello_world-foo becomes camel helloWorldFoo, Pascal HelloWorldFoo, snake hello_world_foo, kebab hello-world-foo, CONSTANT HELLO_WORLD_FOO.

3. Slug: café → cafe; repeated punctuation collapses to one hyphen; leading/trailing hyphens stripped; 你好世界 remains non-empty with Han.

4. Keep named exports convertCases and slugify. Do not throw. Do not add English error strings (idle empty only).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>Title, identifier, and slug edge tests green; Han slug still non-empty; npm test green at length 12.</done>
  <acceptance_criteria>
    - Title Case uses whitespace words only and has no acronym exception list
    - Identifier cases tokenize underscore, hyphen, whitespace, and punctuation
    - slugify strips Latin marks, collapses hyphens, and keeps Han
    - convertCases blank contract unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Nine-row chrome labels and Han-slug FAQ at catalog 12</name>
  <files>src/i18n/ui.ts, src/content/tools/case-converter.md, src/content/tools/zh/case-converter.md, src/components/tools/CaseConverter.tsx</files>
  <read_first>src/i18n/ui.ts, src/content/tools/case-converter.md, src/content/tools/zh/case-converter.md, src/components/tools/CaseConverter.tsx, src/i18n/errors.ts</read_first>
  <action>
Finish CASE-04 chrome parity (D-11, D-14). No ZH_ERRORS keys (lib idle uses empty error only). Do not edit errors.ts. Do not bump catalog length. Do not rewrite existing ten relatedSlugs.

1. Confirm island labels come from copy.tools['case-converter'] for all nine rows plus the textarea. EN and ZH key sets must match (UiDict).

2. Per-row copy buttons use type="button" and navigator.clipboard.writeText of that row. ToolShell still receives the joined dump. Do not edit ToolShell.

3. FAQ on both locales: nothing uploaded; slug keeps CJK / Han and does not transliterate to pinyin; copy each row or copy all. howTo stays 3. Discretion on wording.

4. Size guard remains isTooLarge. Output is text, not HTML injection.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <human-check>Open /tools/case-converter/; paste hello world and 你好世界; nine rows appear; copy one row; slug row is non-empty Han; homepage featured still six.</human-check>
  </verify>
  <done>EN+ZH labels and FAQ complete; nine-row copy works; npm test green; catalog 12 featured 6.</done>
  <acceptance_criteria>
    - en and zh ui tools['case-converter'] share text plus nine row label keys
    - Both markdown files mention Han-preserving slug and privacy
    - CaseConverter does not use HTML injection APIs for row values
    - npm test green with toHaveLength 12 and featured 6
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `convertCases`, `slugify`, `CaseResult` in `src/lib/cases.ts`
- `CaseConverter` default export in `src/components/tools/CaseConverter.tsx`
- Catalog slug `case-converter` in `TOOLS`
- `ui.en.tools['case-converter']` and `ui.zh.tools['case-converter']`
- `src/content/tools/case-converter.md` and `src/content/tools/zh/case-converter.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor paste → CaseConverter island | Untrusted textarea into convertCases |
| per-row copy → clipboard | Same clipboard API as ToolShell; still client-only |
| static SSG → browser | No tool API; no network in this slice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Denial of service | CaseConverter.tsx / isTooLarge | medium | mitigate | isTooLarge on the paste before convertCases |
| T-02-04 | Tampering | CaseConverter rows | medium | mitigate | Row values as text in inputs/code; no HTML injection APIs |
| T-02-SC | Tampering | npm installs | high | accept | No package-manager installs this plan |
</threat_model>

<verification>
npm test after every task. Completeness at length 12. Han slug assertion in cases.test.ts. Featured 6. No Playwright.
</verification>

<success_criteria>
- CASE-01..04 delivered for slug case-converter
- 你好世界 slug non-empty with Han
- Catalog 12, featured 6, existing ten relatedSlugs untouched
- npm test green
</success_criteria>

<output>
Create `.planning/phases/02-light-text-and-generate-tools/02-02-SUMMARY.md` when done
</output>
