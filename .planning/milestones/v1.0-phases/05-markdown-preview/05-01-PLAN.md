---
phase: 05-markdown-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/markdown.ts
  - src/lib/markdown.test.ts
  - src/components/tools/MarkdownPreview.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/i18n/errors.ts
  - src/i18n/errors.test.ts
  - src/content/tools/markdown-preview.md
  - src/content/tools/zh/markdown-preview.md
  - src/styles/global.css
autonomous: true
requirements:
  - MD-01
  - MD-02
  - MD-03
  - MD-04
  - MD-05

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can open /tools/markdown-preview/ (and /zh/tools/markdown-preview/ when that tree exists), paste Markdown, and see a live GFM preview of headings, lists, links, code fences, tables, strikethrough, and task lists (MD-01)"
    - "Preview HTML is sanitized after parse; XSS payloads do not execute; Marked's removed sanitizer option is never used (MD-02)"
    - "Remote images and data URIs are stripped so the preview does not fetch the network; no broken-image icon (MD-03)"
    - "ToolShell Copy writes the sanitized HTML string, not source Markdown (MD-04)"
    - "Empty source shows a blank .md-preview with Copy disabled, not placeholder copy (MD-05)"
    - "Catalog slug markdown-preview, category Format, featured false; TOOLS toHaveLength 17; getFeaturedTools stays 6; relatedSlugs json-formatter, text-diff, word-counter; existing ten relatedSlugs unchanged (CAT-01, CAT-05, CONTEXT catalog additive)"
    - "After astro build, dist/_astro/JsonFormatter*.js does not contain the seven minify-surviving marked/DOMPurify identifiers; only src/lib/markdown.ts imports marked and dompurify (CAT-04)"
  artifacts:
    - path: src/lib/markdown.ts
      provides: "renderMarkdown plus MarkdownResult; thin wrap of marked.parse then DOMPurify.sanitize; idle empty string before parse; normalizeEmpty leftover empty p tags"
      exports: ["renderMarkdown"]
    - path: src/lib/markdown.test.ts
      provides: "jsdom env pragma; idle, GFM fixtures, XSS strip, img strip, normalizeEmpty, isolation, FAQ lock"
    - path: src/components/tools/MarkdownPreview.tsx
      provides: "Default-export island; split panes; .md-preview visual surface; isTooLarge on source; live useMemo; ToolShell copy payload is sanitized HTML"
    - path: src/components/tools/ToolIsland.astro
      provides: "static MarkdownPreview import and slug === 'markdown-preview' client:load locale={locale}"
    - path: src/data/tools.ts
      provides: "markdown-preview catalog row featured false category Format"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 17; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['markdown-preview'] UI-SPEC chrome keys name shortDescription markdown preview"
    - path: src/i18n/errors.ts
      provides: "ZH_ERRORS map for INPUT_TOO_LARGE_MSG"
    - path: src/i18n/errors.test.ts
      provides: "markdown-preview chrome-key describe plus too-large ZH map"
    - path: src/content/tools/markdown-preview.md
      provides: "locale en SEO howTo 3 faq 3-5 including local / nothing uploaded / not WYSIWYG / remote images blocked"
    - path: src/content/tools/zh/markdown-preview.md
      provides: "locale zh SEO howTo 3 faq with local / no-upload / remote images blocked"
    - path: src/styles/global.css
      provides: "additive .md-preview pane chrome and GFM child rules only"
    - path: package.json
      provides: "direct dependencies marked 18.0.13 and dompurify 3.4.15; jsdom 30.0.1 as a devDependency"
  key_links:
    - from: src/components/tools/MarkdownPreview.tsx
      to: src/lib/markdown.ts
      via: "renderMarkdown after isTooLarge and empty-source skip"
      pattern: "renderMarkdown"
    - from: src/lib/markdown.ts
      to: marked
      via: "named parse from package root only in this file"
      pattern: "from 'marked'"
    - from: src/lib/markdown.ts
      to: dompurify
      via: "default sanitize after parse with locked CFG"
      pattern: "from 'dompurify'"
    - from: src/components/tools/MarkdownPreview.tsx
      to: src/lib/limits.ts
      via: "isTooLarge(input) on Markdown source before parse"
      pattern: "isTooLarge"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/MarkdownPreview.tsx
      via: "static import and slug === 'markdown-preview' client:load"
      pattern: "slug === 'markdown-preview'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(17)"
      pattern: "toHaveLength\\(17\\)"
    - from: src/components/tools/MarkdownPreview.tsx
      to: src/components/ToolShell.tsx
      via: "error plus sanitized HTML copy payload; visual surface is children .md-preview"
      pattern: "ToolShell"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** paste Markdown and see a live sanitized GFM preview in the browser, **so that** I can preview formatting without uploading anything or fetching remote images.

<objective>
Ship the markdown-preview 8-file vertical slice at existing-tool parity (MD-01, MD-02, MD-03, MD-04, MD-05) plus CAT-01..06 kept green, including CAT-04 bundle isolation. Install official marked@18.0.13 and dompurify@3.4.15 plus jsdom@30.0.1 as a devDependency only. Thin src/lib/markdown.ts wrapping named parse then DOMPurify.sanitize with the locked CFG. Never Marked's removed sanitizer option. Never jsdom from the lib. Never a lib barrel. Island clones HEAD WordCounter locale/t(), HEAD SqlFormatter live useMemo, HEAD ToolShell with no locale prop — not dirty JsonFormatter (useToolUi) or dirty ToolShell. Catalog slug markdown-preview, Format, featured false; TOOLS 16 → 17; featured stays 6; relatedSlugs json-formatter, text-diff, word-counter; do not rewrite existing ten relatedSlugs (CAT-05, CONTEXT catalog additive). This phase maps INPUT_TOO_LARGE_MSG in ZH_ERRORS. Visual surface is children .md-preview; ToolShell pre is copy-only sanitized HTML (MD-04). CSS append .md-preview only; never retokenize :root; never reuse Phase 4 add/delete colors. After astro build, json-formatter chunk must not inherit the seven minify-surviving marked/DOMPurify identifiers.
Purpose: Third heavy-library catalog tool; visitors preview GFM as sanitized HTML in-browser with no network fetch.
Output: markdown lib/test, MarkdownPreview island, ToolIsland branch, catalog row, ui.ts chrome, ZH_ERRORS too-large map, EN+ZH markdown, additive CSS, marked@18.0.13, dompurify@3.4.15, jsdom@30.0.1 dev, CAT-04 build grep green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-markdown-preview/05-CONTEXT.md
@.planning/phases/05-markdown-preview/05-RESEARCH.md
@.planning/phases/05-markdown-preview/05-PATTERNS.md
@.planning/phases/05-markdown-preview/05-UI-SPEC.md
@.planning/phases/04-text-diff/04-01-SUMMARY.md
@.planning/phases/03-sql-formatter/03-01-SUMMARY.md
@.planning/phases/01-additive-tool-contract/01-01-SUMMARY.md
@src/lib/json.ts
@src/lib/sql.ts
@src/lib/sql.test.ts
@src/lib/diff.test.ts
@src/lib/limits.ts
@src/components/tools/WordCounter.tsx
@src/components/tools/SqlFormatter.tsx
@src/components/ToolShell.tsx
@src/components/tools/ToolIsland.astro
@src/components/tools/ToolIsland.test.ts
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/i18n/errors.ts
@src/i18n/errors.test.ts
@src/content.config.ts
@src/content/tools/sql-formatter.md
@src/styles/global.css
@.planning/codebase/CONVENTIONS.md

<interfaces>
From src/lib/json.ts: JsonResult is ok-true with a payload or ok-false with error string; empty input returns ok false and empty error before the engine; never throw. Clone the union as MarkdownResult ok-true with html string or ok-false with error string. Idle for markdown is empty string only — if (!input) — do not trim before parse (whitespace-only may still parse). Catch returns ok false and empty error (no non-empty English parse error).

From marked@18.0.13 (package root only): named export marked; call marked.parse(input) as string. Built-in gfm default true. Do not install marked-gfm-heading-id. Do not call marked.use or marked.setOptions. Do not pass Marked's removed sanitizer option (silent no-op that still emits script tags). Do not pass breaks true.

From dompurify@3.4.15 (package root only): default export DOMPurify; call DOMPurify.sanitize(dirty, CFG) after parse. Locked CFG: USE_PROFILES html true; FORBID_TAGS img, picture, source, video, audio, track, iframe, object, embed, form; FORBID_ATTR style, srcset, poster; KEEP_CONTENT false. Do not set ALLOWED_URI_REGEXP. Do not addHook. Do not forbid the HTML input tag (GFM task lists emit disabled checkboxes). Do not install @types/dompurify or isomorphic-dompurify.

From jsdom@30.0.1: devDependency only. markdown.test.ts first line is the vitest jsdom environment pragma. Never import jsdom from src/lib/markdown.ts.

From HEAD src/components/tools/WordCounter.tsx (git show HEAD:src/components/tools/WordCounter.tsx): default export { locale }: { locale: Locale }; import { t, type Locale } from '../../i18n/ui'; copy = t(locale); labels = copy.tools[slug]; ToolShell error/output without a locale prop; class not className; textarea onInput HTMLTextAreaElement cast; spellcheck false. This phase DOES map INPUT_TOO_LARGE_MSG via localizeError (unlike Phase 4).

From HEAD src/components/tools/SqlFormatter.tsx (git show HEAD:src/components/tools/SqlFormatter.tsx): live useMemo, no Generate button; size guard before lib; localizeError for lib errors.

From HEAD ToolShell (git show HEAD:src/components/ToolShell.tsx): props { error, output, children } — no locale. Copy is navigator.clipboard.writeText(props.output) when output is non-empty. Do not edit ToolShell.tsx. Do not pass locale into ToolShell.

From src/lib/limits.ts: INPUT_MAX_CHARS 100000; INPUT_TOO_LARGE_MSG is Input too large to process in the browser.; isTooLarge is input.length greater than that. Do not edit limits.ts. Size guard lives in the island, not the lib. Cap is on Markdown source.

From src/data/tools.ts: append-only row; ToolCategory includes Format; do not edit the first ten objects' relatedSlugs (CAT-05, CONTEXT catalog additive).

From src/data/tools.test.ts: it has exactly 16 tools uses toHaveLength(16) — bump to 17; featured toHaveLength(6) stays; existsSync(URL) EN+ZH loop already covers new slugs — never URL.pathname.

From src/components/tools/ToolIsland.astro: last branch is slug === 'text-diff' with TextDiff client:load locale={locale}. Add static import plus slug === 'markdown-preview' && MarkdownPreview client:load locale={locale}. Do not import marked or dompurify here. Do not use dynamic component tags.

From src/content.config.ts: howTo tuple of exactly 3; faq min 3 max 5; locale en or zh. Keep locale so dirty content.config still builds.

From src/i18n/ui.ts: ui.en.tools and ui.zh.tools must share keys. Append tools['markdown-preview'] on both with UI-SPEC keys only: name, shortDescription, markdown, preview. Do not drop Phase 2/3/4 keys. Do not add empty-state heading or empty-state body keys.

From src/i18n/errors.ts: append ZH_ERRORS for INPUT_TOO_LARGE_MSG to 输入过长，无法在浏览器中处理。 Do not drop existing keys.

From HEAD src/styles/global.css: .tool-grid.split already 1fr 1fr at 720px. Append only .md-preview rules. Do not retokenize :root. Do not reuse Phase 4 #3dd68c / #13291f.

From CONVENTIONS 8-file checklist: catalog row featured false; lib+test; island; ToolIsland branch; ui.ts EN+ZH; ZH_ERRORS for new English errors; EN md; ZH md. No lib barrel. Heavy packages imported only from that tool's src/lib.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end markdown-preview — paste source through lib, island, catalog, and EN/ZH page</name>
  <precondition>Phase 4 completeness is green at catalog length 16: src/data/tools.test.ts snapshots toHaveLength(16), featured length 6, ToolIsland.test.ts source-reads slug-equals branches including text-diff, and text-diff ZH markdown remains on disk</precondition>
  <reversibility rating="costly">Public catalog slug markdown-preview plus direct marked@18.0.13 and dompurify@3.4.15 become the Format-tool XSS contract; swapping the stack later is a coordinated lib, island, FAQ, CSS, and bundle-isolation change.</reversibility>
  <files>package.json, package-lock.json, src/lib/markdown.test.ts, src/lib/markdown.ts, src/components/tools/MarkdownPreview.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/i18n/errors.test.ts, src/content/tools/markdown-preview.md, src/content/tools/zh/markdown-preview.md, src/styles/global.css</files>
  <read_first>src/lib/json.ts, src/lib/sql.ts, src/lib/sql.test.ts, src/components/tools/WordCounter.tsx, src/components/tools/SqlFormatter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/i18n/errors.test.ts, src/content/tools/sql-formatter.md, src/content.config.ts, src/lib/limits.ts, src/styles/global.css, .planning/phases/05-markdown-preview/05-RESEARCH.md, .planning/phases/05-markdown-preview/05-PATTERNS.md, .planning/phases/05-markdown-preview/05-UI-SPEC.md, .planning/codebase/CONVENTIONS.md</read_first>
  <behavior>
    - renderMarkdown('') equals { ok: false, error: '' } and must not call parse on empty string
    - renderMarkdown('# Hello') is ok true and html contains an h1 with Hello
    - npm test green at TOOLS length 17 and featured 6 with markdown-preview catalog row featured false
  </behavior>
  <action>
Land the thinnest production markdown-preview path through every layer this phase touches (MD-01, MD-02, MD-03, MD-04, MD-05, CAT-01, CAT-02, CAT-03, CAT-06). Official packages marked@18.0.13 and dompurify@3.4.15 only, plus jsdom@30.0.1 as a devDependency. Thin src/lib/markdown.ts — do not import marked or dompurify from the island or ToolIsland. Do not edit ToolShell.tsx, limits.ts, json.ts, JsonFormatter.tsx, or the existing ten TOOLS relatedSlugs (CAT-05, CONTEXT catalog additive). Do not create a lib barrel. Do not install marked-gfm-heading-id, highlight.js, Prism, @types/dompurify, @types/marked, or isomorphic-dompurify. Stage only this plan's files_modified; leave unrelated dirty worktree files alone. Do not commit unrelated dirty i18n/pages. Do not pop any existing git stash. Clone HEAD WordCounter locale/t() wiring (git show HEAD:src/components/tools/WordCounter.tsx). Clone HEAD SqlFormatter live useMemo. Clone HEAD ToolShell (no locale prop). Do not clone dirty worktree JsonFormatter or dirty ToolShell (missing useToolUi / extra locale prop).

1. npm install marked@18.0.13 dompurify@3.4.15 then npm install -D jsdom@30.0.1 so package.json and the lockfile record those versions. Confirm resolved versions. Package legitimacy is Approved in 05-RESEARCH; no human checkpoint.

2. Create src/lib/markdown.test.ts then src/lib/markdown.ts (tests first). First line of the test file must be the vitest jsdom environment pragma. Named exports only. Export MarkdownResult ok-true with html string or ok-false with error string. Export renderMarkdown(input). If input is the empty string, return ok false and empty error BEFORE parse. Do not trim before parse. Otherwise call named parse from 'marked' then sanitize from 'dompurify' with a fresh locked CFG object: USE_PROFILES html true; FORBID_TAGS img picture source video audio track iframe object embed form; FORBID_ATTR style srcset poster; KEEP_CONTENT false. Do not set ALLOWED_URI_REGEXP. Do not addHook. Do not forbid the HTML input tag (task lists). Never pass Marked's removed sanitizer option. Never call marked.use or marked.setOptions. After sanitize, normalize leftover empty p tags to '' so image-only source yields html ''. Catch returns ok false and empty error. Never throw. Import from 'marked' and from 'dompurify' only in this file. Never import jsdom from this file.

3. Append one TOOLS row only (CAT-01, CAT-05, CONTEXT catalog additive): slug markdown-preview, name Markdown Preview, category Format, shortDescription Preview GitHub-flavored Markdown in your browser., relatedSlugs json-formatter, text-diff, word-counter in that order, featured false. In src/data/tools.test.ts change the snapshot from 16 to 17 and rename that it() to has exactly 17 tools. Do not change featured length 6. Do not edit the first ten objects' relatedSlugs.

4. Add ui.en.tools['markdown-preview'] and ui.zh.tools['markdown-preview'] with shared keys verbatim from 05-UI-SPEC Copywriting Contract: name, shortDescription, markdown, preview. EN/ZH strings must match the UI-SPEC table exactly (Markdown Preview / Markdown 预览, Preview GitHub-flavored Markdown in your browser. / 在浏览器里预览 GitHub 风格 Markdown。, Markdown / Markdown, Preview / 预览). Do not drop Phase 2/3/4 keys. Do not add empty-state heading or empty-state body keys.

5. Append ZH_ERRORS in src/i18n/errors.ts mapping INPUT_TOO_LARGE_MSG to 输入过长，无法在浏览器中处理。 Do not drop existing keys. Do not invent a second too-large string. Append a markdown-preview describe in src/i18n/errors.test.ts that asserts the four chrome keys on en and zh and that localizeError zh of INPUT_TOO_LARGE_MSG is that ZH string. Keep existing Phase 2/3/4 describes.

6. Create src/components/tools/MarkdownPreview.tsx: default export, locale prop, useState source. t(locale), labels from copy.tools['markdown-preview']. useMemo: if isTooLarge(input), error is localizeError(locale, INPUT_TOO_LARGE_MSG), output empty, html empty, do not call renderMarkdown (CAT-06, T-05-SIZE). Else if input === '', skip lib, error null, output empty, html empty (MD-05). Else renderMarkdown(input); html is r.ok ? r.html : ''; output is that html (MD-04); error null. useMemo deps input, locale. Render ToolShell error/output with children: div.tool-grid.split, source first — label markdown plus textarea rows 12, spellcheck false, onInput casts HTMLTextAreaElement; then a wrapping block with label id md-preview-label using labels.preview plus div.md-preview aria-labelledby that id. When html is non-empty, set dangerouslySetInnerHTML only of that already-sanitized html (MD-02). When html is empty, leave .md-preview with no children and do not set inner HTML. Never pass locale into ToolShell. class not className. Import renderMarkdown from ../../lib/markdown — do not import marked or dompurify from this file. Do not import useToolUi. Do not put the visual preview only inside ToolShell pre. No Generate button. No aria-live on .md-preview.

7. In ToolIsland.astro add a static import of MarkdownPreview and a branch slug === 'markdown-preview' with client:load and locale={locale} after the text-diff branch. Keep existing branches. Do not import marked or dompurify in ToolIsland.astro.

8. Append only to src/styles/global.css: .md-preview pane chrome min-height 160px, max-height 384px, overflow-y auto, overflow-x auto, background var(--bg), border 1px solid var(--border), border-radius 6px, padding 16px, font-family var(--sans), font-size 16px, line-height 1.5, color var(--text), overflow-wrap anywhere. GFM child rules per 05-UI-SPEC node table: h1 20px/600/1.2; h2–h6 16px/600/1.2; 16px top margin except first-child; lists padding-left 24px and li margin 8px; task checkbox gap 4px; inline code --mono --bg-elev 4px padding 6px radius; fenced pre --mono --bg-elev 16px padding overflow-x auto white-space pre (no token colors); table collapse 8px cell padding 1px solid var(--border) th weight 600; blockquote 4px left var(--border) padding-left 16px color var(--muted); hr 1px solid var(--border) margin 16px 0; del line-through color var(--text). Do not retokenize :root. Do not reuse Phase 4 add/delete colors. No img rules (tags are stripped). If the worktree CSS has unrelated dirty visual edits, add only this block.

9. Add src/content/tools/markdown-preview.md locale en and src/content/tools/zh/markdown-preview.md locale zh. Schema: title, description, intro, howTo exactly 3, faq 3 items. EN FAQ must state parsing runs in the browser and nothing is uploaded, this is not a WYSIWYG editor, and remote images are blocked including data URIs stripped. ZH FAQ must state local/no-upload and remote images blocked. Discretion on remaining wording. Do not mention later-phase tools.

No Playwright. No tsx tests. No API routes.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green with TOOLS length 17 and featured 6; markdown-preview catalog row featured false; ToolIsland contains slug === 'markdown-preview' with locale={locale}; EN+ZH markdown exist; renderMarkdown idle empty error before parse; heading fixture is ok true; MarkdownPreview uses isTooLarge then renderMarkdown, live useMemo, .md-preview children, and ToolShell sanitized HTML copy payload; ZH_ERRORS maps INPUT_TOO_LARGE_MSG; existing ten relatedSlugs unchanged; marked@18.0.13 and dompurify@3.4.15 are direct dependencies; jsdom@30.0.1 is a devDependency.</done>
  <acceptance_criteria>
    - package.json lists marked at 18.0.13 and dompurify at 3.4.15 as direct dependencies and jsdom at 30.0.1 as a devDependency; no @types/dompurify, no @types/marked, no isomorphic-dompurify, no marked-gfm-heading-id, no highlight.js, no Prism
    - src/lib/markdown.ts exports renderMarkdown; empty string is ok false empty error; hash-Hello is ok true with h1
    - MarkdownPreview.tsx default-exports, calls isTooLarge on source then renderMarkdown, live useMemo, ToolShell output is sanitized HTML, visual surface is children .md-preview
    - TOOLS includes markdown-preview category Format featured false; tools.test.ts toHaveLength 17; getFeaturedTools still length 6
    - ToolIsland.astro static-imports MarkdownPreview and has slug === 'markdown-preview' with locale={locale}
    - EN and ZH markdown exist with howTo length 3 and faq 3-5 including local / nothing uploaded / not WYSIWYG / remote images blocked
    - Existing ten TOOLS objects' relatedSlugs bytes unchanged; no lib barrel file created
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>GFM fixtures, XSS strip, img strip, normalizeEmpty, chrome keys, too-large source-read, and FAQ lock</name>
  <files>src/lib/markdown.ts, src/lib/markdown.test.ts, src/i18n/errors.test.ts, src/components/tools/MarkdownPreview.tsx, src/content/tools/markdown-preview.md, src/content/tools/zh/markdown-preview.md</files>
  <read_first>src/lib/markdown.ts, src/lib/markdown.test.ts, src/i18n/errors.test.ts, src/i18n/ui.ts, src/components/tools/MarkdownPreview.tsx, src/content/tools/markdown-preview.md, src/content/tools/zh/markdown-preview.md, src/lib/sql.test.ts, src/lib/diff.test.ts, .planning/phases/05-markdown-preview/05-RESEARCH.md, .planning/phases/05-markdown-preview/05-UI-SPEC.md</read_first>
  <behavior>
    - renderMarkdown of a GFM sample with heading, list, link, js fence, table, strikethrough, and task list is ok true and html keeps h1, ul/li, a, pre code language-js, table, del, and a disabled checkbox
    - renderMarkdown of a script-alert payload is ok true with html that does not contain a script tag
    - renderMarkdown of a remote image markdown and a data-URI image markdown is ok true with html that does not contain an img tag
    - renderMarkdown of image-only source is ok true with html equal to ''
    - ui.en.tools['markdown-preview'] and ui.zh.tools['markdown-preview'] share keys name, shortDescription, markdown, preview
    - MarkdownPreview.tsx source contains isTooLarge(input) and INPUT_TOO_LARGE_MSG and localizeError and does not import marked or dompurify
    - EN markdown faq answers mention browser / nothing uploaded / not WYSIWYG / remote images; ZH markdown faq answers mention local or 不会上传 and blocked images
  </behavior>
  <action>
Expand the proven lib and island into MD-01 GFM fixtures, MD-02 XSS strip, MD-03 img strip, MD-05 normalizeEmpty, CAT-06 chrome-key tests, too-large island source-read, and local-only FAQ source-read without changing architecture. Do not add packages. Do not edit ToolIsland.astro, tools.ts, JsonFormatter.tsx, ToolShell, or limits.ts. Keep idle empty-string-before-parse from the tracer. Keep the locked CFG. Never forbid the HTML input tag.

1. Extend src/lib/markdown.test.ts (keep the jsdom environment pragma as the first line) with: GFM heading/list/link/fence/table/strike/task kept after sanitize; a script payload stripped; https image and data-URI image absent (no img tag); image-only leftover empty p normalized to html ''; whitespace-only may parse then sanitize to empty html without throwing. Read files with readFileSync(new URL(..., import.meta.url)) never URL.pathname.

2. Keep the markdown-preview describe in src/i18n/errors.test.ts asserting chrome keys name, shortDescription, markdown, preview on ui.en.tools['markdown-preview'] and ui.zh.tools['markdown-preview'], plus ZH_ERRORS[INPUT_TOO_LARGE_MSG]. Do not drop existing lorem-ipsum, password-generator, sql-formatter, or text-diff describes.

3. Source-read MarkdownPreview.tsx for isTooLarge(input), INPUT_TOO_LARGE_MSG, localizeError, renderMarkdown, ../../lib/markdown, class not className, spellcheck false, tool-grid split, md-preview, and dangerouslySetInnerHTML only of already-sanitized html. Assert the island does not contain from 'marked' or from 'dompurify'. Source-read both markdown files so EN faq includes local/no-upload, not-WYSIWYG, and remote-images-blocked wording (including data URIs stripped) and ZH faq includes 不会上传 or equivalent local-only wording plus remote images blocked. howTo remains exactly 3. faq length 3–5.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>GFM fixtures kept; script and img stripped; image-only html is empty; chrome keys locked on en+zh; island source-reads isTooLarge plus INPUT_TOO_LARGE_MSG; FAQ local-only and remote-image lock; npm test green.</done>
  <acceptance_criteria>
    - GFM heading list link fence table strike and task checkbox survive sanitize
    - script payloads and img tags are absent from html
    - image-only source yields html ''
    - errors.test.ts shares the UI-SPEC chrome keys on en and zh without dropping Phase 2/3/4 describes
    - MarkdownPreview.tsx contains isTooLarge(input) and INPUT_TOO_LARGE_MSG and localizeError
    - EN+ZH markdown FAQ include local / nothing uploaded / not WYSIWYG / remote images blocked
  </acceptance_criteria>
</task>

<task type="auto">
  <name>CAT-04 isolation — only markdown.ts imports marked and dompurify; json-formatter chunk stays clean after build</name>
  <files>src/lib/markdown.test.ts, src/lib/markdown.ts</files>
  <read_first>src/lib/markdown.ts, src/lib/markdown.test.ts, src/components/tools/MarkdownPreview.tsx, src/components/tools/ToolIsland.astro, src/lib/json.ts, src/components/tools/JsonFormatter.tsx, package.json, .planning/phases/05-markdown-preview/05-RESEARCH.md, .planning/phases/04-text-diff/04-01-SUMMARY.md, .planning/phases/03-sql-formatter/03-01-SUMMARY.md</read_first>
  <action>
Prove CAT-04: marked and dompurify are imported only from src/lib/markdown.ts, MarkdownPreview imports ../../lib/markdown, ToolIsland does not import those packages, json.ts and JsonFormatter.tsx do not import them, markdown.ts does not import jsdom, completeness snapshot stays 17 with featured 6, and after a fresh astro build the json-formatter island chunk does not contain minify-surviving marked/DOMPurify identifiers (05-RESEARCH pitfall 4). Do not add a lib barrel. Do not edit JsonFormatter.tsx, json.ts, ToolShell, dirty pages, or dirty i18n modules. Do not import marked or dompurify from ToolIsland.astro. Do not trust a stale dist/_astro listing — always rebuild. Do not treat absence of marked.parse or sanitize or gfm as proof of isolation.

1. In src/lib/markdown.test.ts add a source-isolation test cloned from sql.test.ts / diff.test.ts: read markdown.ts, MarkdownPreview.tsx, ToolIsland.astro, json.ts, and JsonFormatter.tsx with readFileSync(new URL(...), utf8). Assert markdown.ts contains from 'marked' and from 'dompurify'. Assert the other four files do not contain those specifiers. Assert MarkdownPreview.tsx contains renderMarkdown and ../../lib/markdown. Assert markdown.ts does not contain from 'jsdom'.

2. Run a fresh npm run build then check every dist/_astro/JsonFormatter*.js file: none may contain DOMPurify, FORBID_TAGS, ALLOWED_URI_REGEXP, uponSanitizeElement, listIsTask, listReplaceTask, or github.com/markedjs/marked. If no JsonFormatter js chunk exists, that is a failure (do not pass on a missing chunk). If npm run build fails because of uncommitted page or i18n files that are not in this plan's files_modified (Phase 3/4 dirty-tree pattern), copy those failing files aside, restore them from HEAD, rebuild, run the identifier check, then restore the aside copies. Do not rewrite those dirty files this phase. Do not pop any existing git stash. If the json-formatter chunk leaks, keep architecture: move the package load to a dynamic import inside renderMarkdown of the same named parse and sanitize from the package roots (still only inside markdown.ts), rebuild, and re-check. Do not import the packages from ToolIsland.astro or a barrel to fix the leak.
  </action>
  <verify>
    <automated>npm test && npm run build && node --input-type=module -e "import fs from 'node:fs'; import path from 'node:path'; const dir='dist/_astro'; if (!fs.existsSync(dir)) { console.error('missing dist/_astro'); process.exit(1); } const files = fs.readdirSync(dir).filter((f) => f.startsWith('JsonFormatter') && f.endsWith('.js')); if (!files.length) { console.error('no JsonFormatter js chunk'); process.exit(1); } let bad = false; for (const f of files) { const t = fs.readFileSync(path.join(dir, f), 'utf8'); if (t.includes('DOMPurify') || t.includes('FORBID_TAGS') || t.includes('ALLOWED_URI_REGEXP') || t.includes('uponSanitizeElement') || t.includes('listIsTask') || t.includes('listReplaceTask') || t.includes('github.com/markedjs/marked')) { console.error('CAT-04 leak in ' + f); bad = true; } } process.exit(bad ? 1 : 0);"</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary, or output contains "CAT-04 leak", or output contains "missing dist/_astro", or output contains "no JsonFormatter js chunk"</fails_when>
    <human-check>Open /tools/markdown-preview/ (and /zh/tools/markdown-preview/ if that tree is being served): Markdown source left/top and Preview right/bottom; paste headings/lists/tables/tasks/fences and see live HTML in .md-preview without a Generate button; empty source leaves the preview blank with no Start typing copy; paste a script tag and a remote image — no alert, no network image request, no broken-image icon; Copy writes sanitized HTML not source Markdown; oversize source shows 输入过长，无法在浏览器中处理。 on ZH or the English too-large string on EN; homepage featured count still six.</human-check>
  </verify>
  <done>npm test green; only markdown.ts imports marked and dompurify; never jsdom from lib; fresh astro build; JsonFormatter island chunk has none of the seven minify-surviving identifiers; featured still 6; length 17.</done>
  <acceptance_criteria>
    - markdown.test.ts isolation test: markdown.ts has from 'marked' and from 'dompurify'; MarkdownPreview.tsx, ToolIsland.astro, json.ts, JsonFormatter.tsx do not
    - MarkdownPreview.tsx imports renderMarkdown from ../../lib/markdown
    - markdown.ts does not import jsdom
    - Fresh npm run build succeeds (with dirty-page isolation if needed, without rewriting those pages)
    - Every dist/_astro/JsonFormatter*.js file exists after build and contains none of DOMPurify, FORBID_TAGS, ALLOWED_URI_REGEXP, uponSanitizeElement, listIsTask, listReplaceTask, github.com/markedjs/marked
    - No src/lib barrel file exists
  </acceptance_criteria>
</task>

</tasks>

<!-- planner-discipline-allow: DOMPurify -->
<!-- planner-discipline-allow: FORBID_TAGS -->
<!-- planner-discipline-allow: ALLOWED_URI_REGEXP -->
<!-- planner-discipline-allow: uponSanitizeElement -->
<!-- planner-discipline-allow: listIsTask -->
<!-- planner-discipline-allow: listReplaceTask -->
<!-- planner-discipline-allow: github.com/markedjs/marked -->
<!-- planner-discipline-allow: INPUT_TOO_LARGE_MSG -->
<!-- planner-discipline-allow: from 'marked' -->
<!-- planner-discipline-allow: from 'dompurify' -->
<!-- planner-discipline-allow: dangerouslySetInnerHTML -->

## Artifacts this phase produces

Slice symbols and paths created by this plan:

- `renderMarkdown`, `MarkdownResult` in `src/lib/markdown.ts`
- `MarkdownPreview` default export in `src/components/tools/MarkdownPreview.tsx`
- Catalog slug `markdown-preview` in `TOOLS`
- `ui.en.tools['markdown-preview']` and `ui.zh.tools['markdown-preview']`
- `ZH_ERRORS` map for `INPUT_TOO_LARGE_MSG`
- `src/content/tools/markdown-preview.md` and `src/content/tools/zh/markdown-preview.md`
- Additive `.md-preview` rules in `src/styles/global.css`
- Direct dependencies `marked@18.0.13` and `dompurify@3.4.15`; devDependency `jsdom@30.0.1`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor paste → MarkdownPreview island | Untrusted Markdown (and raw HTML inside it) crosses into in-browser lib only |
| markdown.ts → marked then DOMPurify | Third-party parser emits HTML; sanitizer is the XSS boundary before the DOM sink |
| island → .md-preview + ToolShell output | Sanitized HTML may be bound as inner HTML; Copy uses clipboard |
| static SSG → browser | No tool API route; no fetch in this slice; remote images must not be emitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-XSS | Elevation of Privilege | MarkdownPreview.tsx / markdown.ts | high | mitigate | Call DOMPurify.sanitize after marked.parse; bind dangerouslySetInnerHTML only of that already-sanitized string; never pass Marked's removed sanitizer option; never innerHTML of raw Marked output |
| T-05-IMG | Information Disclosure | markdown.ts CFG | high | mitigate | FORBID_TAGS includes img plus picture source video audio track iframe object embed form; FORBID_ATTR style srcset poster; USE_PROFILES html true drops SVG image; do not set ALLOWED_URI_REGEXP; do not addHook |
| T-05-SIZE | Denial of Service | MarkdownPreview.tsx / isTooLarge | medium | mitigate | Call isTooLarge on Markdown source before parse; INPUT_MAX_CHARS stays 100000 in limits.ts (do not edit); do not run Marked or DOMPurify when over cap |
| T-05-FAQ | Information Disclosure | island / FAQ | medium | mitigate | No fetch and no tool API route; FAQ states nothing is uploaded and remote images are blocked; session-only component state; do not write localStorage |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Direct install is marked@18.0.13 plus dompurify@3.4.15 plus jsdom@30.0.1 dev only (05-RESEARCH Approved, no postinstall). Do not install @types/dompurify, isomorphic-dompurify, or marked-gfm-heading-id. No [SLOP] packages. No blocking human checkpoint (seam SUS was too-new, contradicted by official READMEs + download volume). |
| T-05-CFG | Tampering | renderMarkdown options | low | accept | Pass a fresh CFG object; do not spread user JSON into marked or DOMPurify options; public preview has no secrets |

</threat_model>

<verification>
Inherit Phase 4 `npm test` after tasks 1–2. Task 3 adds `npm run build` plus a Node read of dist/_astro/JsonFormatter*.js that fails on missing chunk or CAT-04 leak (the seven minify-surviving identifiers). Completeness: ToolIsland slug-equals loop and EN+ZH existsSync must pass at length 17. Featured length 6. markdown.test.ts covers idle, GFM, XSS, img strip, normalizeEmpty, and import isolation. Do not run Playwright. Do not trust stale dist/_astro from before these packages. If dirty pages break the build, isolate without rewriting them.
</verification>

<success_criteria>
- MD-01..05 delivered for slug markdown-preview at 8-file parity
- Named parse then DOMPurify.sanitize with locked CFG; idle empty string before parse
- Visual .md-preview plus sanitized HTML copy payload; empty source is blank not placeholder
- isTooLarge on source with ZH_ERRORS map for INPUT_TOO_LARGE_MSG; FAQ local / nothing uploaded / not WYSIWYG / remote images blocked in EN+ZH
- Catalog 17, featured 6, existing ten relatedSlugs untouched (CAT-05, CONTEXT catalog additive)
- CAT-04: only markdown.ts imports marked and dompurify; json-formatter chunk clean after fresh build
- npm test green and npm run build CAT-04 grep green
</success_criteria>

<output>
Create `.planning/phases/05-markdown-preview/05-01-SUMMARY.md` when done
</output>
