---
phase: 08-mobile-hamburger-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/NavMenu.astro
  - src/components/Header.astro
  - src/styles/global.css
  - src/i18n/ui.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "At max-width 640px the visitor sees #navToggle and Tools/Blog/About are collapsed; at min-width 641px the full nav row stays visible and #navToggle is not in tab order (NAV-01, D-03)"
    - "Hamburger is a real button type=button with aria-expanded and aria-controls=navMenu matching open state (NAV-02, D-01)"
    - "Visitor can close an open menu with Escape and focus returns to #navToggle (NAV-03, D-02)"
    - "When the menu is collapsed at max-width 640px, Tools/Blog/About are not keyboard-focusable via both inert on #navMenu and visibility hidden on .nav-links (NAV-04, D-02)"
    - "Control names come from ui.ts nested nav.menu / nav.close on both en and zh; script swaps aria-label from data-label-menu / data-label-close and does not hardcode English (NAV-05, D-03)"
    - "ThemeToggle stays a sibling outside #navMenu and remains visible at every width (D-01, D-04)"
    - "Open menu is an overlay under header.site (position absolute) and does not push page content (D-01)"
    - "Pointer-down outside header.site closes an open menu; clicks inside the header including ThemeToggle do not (D-02)"
    - "Widening past 640px force-closes: removes is-open, sets aria-expanded false, restores nav.menu label, and removes inert so desktop links stay operable (NAV-01, D-03)"
    - "No body scroll lock and no focus trap; tab may leave the open overlay into main (D-02)"
    - "Menu open state is DOM-only; a new page load is always collapsed; NavMenu does not write web storage (D-04)"
    - "First paint is the closed header; NavMenu is static Astro with is:inline; no skeleton, spinner, or hydration wait (UI loading, D-01)"
    - "Open/close is DOM class plus ARIA only; script failure has no role=alert and no error copy (UI error, D-02)"
    - "Nav is a fixed three-link set; never render empty-nav chrome, a placeholder icon, or No pages copy (UI empty)"
    - "Overlay is flex-direction column; three links wrap with overflow-wrap anywhere and no inner scroll; header row does not wrap (UI overflow / long-text)"
    - "Happy path is the existing SSG Tools/Blog/About hrefs and labels; count is fixed at three; no collection empty/partial/populated chrome (UI populated / partial / zero-one-many, D-01)"
    - statement: "At max-width 640px the header stays one row (logo, 44×44 hamburger, 44×44 theme toggle) and the three nav links overlay under the header instead of wrapping the bar"
      verification: backstop
    - "Files are authored from HEAD chrome; do not apply stash entries; do not commit src/pages/zh or LangSwitch (D-04)"
    - "Zero new npm packages; no Playwright; no src/lib; no Preact island (D-04)"
  artifacts:
    - path: src/components/NavMenu.astro
      provides: "Static #navToggle button, 3-line SVG, is:inline IIFE for click/Escape/pointerdown/matchMedia/inert (NAV-02, NAV-03, NAV-04, D-01, D-02)"
      min_lines: 40
    - path: src/components/Header.astro
      provides: "Optional locale default en; DOM order logo, NavMenu, #navMenu three links, ThemeToggle sibling outside the collapsible group (NAV-01, D-01, D-03)"
    - path: src/styles/global.css
      provides: "#navToggle 44px box default display none; max-width 640px overlay and inline-flex hamburger (NAV-01, NAV-04, D-03)"
      min_lines: 180
    - path: src/i18n/ui.ts
      provides: "Nested en.nav and zh.nav menu/close strings as sibling of tools (NAV-05, D-03)"
  key_links:
    - from: src/components/Header.astro
      to: src/components/NavMenu.astro
      via: "NavMenu rendered after logo with locale={locale}"
      pattern: "NavMenu"
    - from: src/components/Header.astro
      to: src/components/ThemeToggle.astro
      via: "ThemeToggle sibling after #navMenu, not inside .nav-links"
      pattern: "ThemeToggle"
    - from: src/components/NavMenu.astro
      to: "#navMenu"
      via: "aria-controls=navMenu; boot on DOMContentLoaded because #navMenu is a later sibling"
      pattern: "navMenu"
    - from: src/components/NavMenu.astro
      to: src/i18n/ui.ts
      via: "t(locale).nav.menu / nav.close baked into aria-label and data-label-*"
      pattern: "nav.menu"
    - from: src/styles/global.css
      to: "nav.nav.is-open"
      via: "@media (max-width: 640px) .nav.is-open .nav-links visibility visible"
      pattern: "is-open"
---

<objective>
Ship an accessible max-width 640px hamburger on HEAD chrome so a visitor can open, close, and keyboard-navigate Tools/Blog/About without a wrapping header — per D-01 (markup and placement), D-02 (inert, Escape, outside pointer, no trap), D-03 (ui.ts labels, locale prop, 640px, widen force-close), and D-04 (HEAD-only files, 44px 3-line SVG, DOM-only state).

Purpose: Phase 8 mobile nav. Later locale pages can pass locale=zh into Header without this phase committing overlay pages.

Output: NavMenu.astro, Header restructure, overlay plus hamburger rules in global.css, nested nav keys in ui.ts. No new npm packages. No Playwright. No src/lib. No Preact island. No BaseLayout, ThemeToggle, or ThemeInit edits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-mobile-hamburger-menu/08-CONTEXT.md
@.planning/phases/08-mobile-hamburger-menu/08-RESEARCH.md
@.planning/phases/08-mobile-hamburger-menu/08-PATTERNS.md
@.planning/phases/08-mobile-hamburger-menu/08-UI-SPEC.md
@.planning/phases/08-mobile-hamburger-menu/08-VALIDATION.md
@.planning/phases/07-theme-foundation/07-01-SUMMARY.md

<interfaces>
From git show HEAD:src/components/Header.astro — no props today. Frontmatter is SITE_NAME plus ThemeToggle import. ThemeToggle sits inside .nav-links after About. Phase 8 must move it out.

From git show HEAD:src/i18n/ui.ts — export type Locale = 'en' | 'zh'; export function t(locale: Locale): UiDict. Import Locale from this file, not from i18n/locales (that path does not exist on HEAD).

From git show HEAD:src/layouts/BaseLayout.astro — keep render as Header with no props (defaults en). Do not edit this file.

From git show HEAD:src/components/ThemeToggle.astro — static button id=themeToggle, is:inline click, 44px CSS already in global.css. Do not edit this file.

Locked CONTEXT groups cited as D-01 Markup and placement, D-02 Interaction and a11y, D-03 Labels i18n and breakpoint, D-04 Chrome HEAD/ZH and out of scope.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end hamburger open/close — one path</name>
  <reversibility rating="costly">Header DOM order (logo, #navToggle, #navMenu, ThemeToggle) and disclosure ids are the chrome contract later locale pages consume; undoing it is a coordinated Header/CSS/script change, not a data migration.</reversibility>
  <precondition>src/components/ThemeToggle.astro exists from Phase 7 and git show HEAD:src/components/Header.astro contains ThemeToggle</precondition>
  <files>src/components/NavMenu.astro, src/components/Header.astro, src/i18n/ui.ts, src/styles/global.css</files>
  <read_first>.planning/phases/08-mobile-hamburger-menu/08-UI-SPEC.md, .planning/phases/08-mobile-hamburger-menu/08-CONTEXT.md, .planning/phases/08-mobile-hamburger-menu/08-PATTERNS.md, .planning/phases/08-mobile-hamburger-menu/08-RESEARCH.md, src/components/ThemeToggle.astro, src/components/Header.astro, src/i18n/ui.ts, src/styles/global.css</read_first>
  <action>
    Before any edit, restore HEAD bytes of src/components/Header.astro and src/styles/global.css with git checkout HEAD -- on those two paths only. Do not apply or pop git stash entries. Work from that HEAD chrome, not the dirty worktree overlay (D-04). Leave BaseLayout, ThemeToggle, ThemeInit, Footer, pages, and other dirty paths untouched. Do not add src/lib/nav.ts.

    In src/i18n/ui.ts add a nested nav object as a sibling of tools on both en and zh so copy.nav.menu types via UiDict (NAV-05, D-03). Do not use dotted string keys. EN menu is Open menu and close is Close menu. ZH menu is 打开菜单 and close is 关闭菜单. Do not change tools entries.

    Create src/components/NavMenu.astro as static Astro, not a hydrated island (D-01, D-04). Frontmatter imports t and Locale from ../i18n/ui, Props locale: Locale, const copy = t(Astro.props.locale). Markup is only the button from the NavMenu button section of 08-UI-SPEC.md: type=button, id=navToggle, aria-expanded=false, aria-controls=navMenu, aria-label={copy.nav.menu}, data-label-menu={copy.nav.menu}, data-label-close={copy.nav.close}. One SVG class nav-toggle-icon, viewBox 0 0 24 24, width 20 height 20, aria-hidden true, focusable false, path fill none stroke currentColor stroke-width 2 stroke-linecap round d=M5 7h14M5 12h14M5 17h14. Do not morph the icon. Do not add a visible caption. Do not use an icon pack.

    Put one script is:inline IIFE in NavMenu.astro (classic script, not type=module, no imports). Clone ThemeInit IIFE plus var style only; do not copy storage or matchMedia prefers-color-scheme. Because #navMenu is a later sibling in Header, wrap boot that queries navMenu / nav.nav / header.site in DOMContentLoaded when document.readyState is loading, else run boot immediately (08-RESEARCH Pattern 3). Null-check #navToggle, #navMenu, nav.nav, and header.site and return; no role=alert.

    Implement the Script contract in 08-UI-SPEC.md verbatim (NAV-02, NAV-03, NAV-04, D-02, D-03): click #navToggle toggles; keydown Escape when open closes and navToggle.focus(); pointerdown on document closes when open and the event target is outside header.site; matchMedia (max-width: 640px) change when matches becomes false force-closes. Do not attach a listener to the window resize event. Open always adds is-open on nav.nav, aria-expanded true, aria-label from data-label-close, removes inert. Close always removes is-open, aria-expanded false, aria-label from data-label-menu, and sets inert on #navMenu only when the 640px query currently matches (remove inert when wider). First run calls that inert rule. Use getAttribute / setAttribute for labels and expanded. Do not lock document.body overflow. Do not install a focus trap. Do not persist open state.

    In src/components/Header.astro, import NavMenu and type Locale from ../i18n/ui. Add optional locale prop defaulting to en (D-03). DOM order locked (D-01): logo, NavMenu locale={locale}, div.nav-links id=navMenu with the three HEAD anchors href=/tools/ Tools, href=/blog/ Blog, href=/about/ About, then ThemeToggle as a sibling outside #navMenu. Keep those hrefs and labels verbatim. Do not put ThemeToggle inside #navMenu.

    In src/styles/global.css add the architectural overlay rules needed for the one path (NAV-01, NAV-04): #navToggle default display none; inside @media (max-width: 640px) set #navToggle to inline-flex, .nav-links to position absolute top 100% left 0 right 0 visibility hidden, and .nav.is-open .nav-links to visibility visible. Put rules in this file, not a scoped component style. Do not add a second breakpoint. Leave .nav min-height 3.25rem, gap 1.25rem, logo font-weight 650, and desktop .nav-links margin-left auto. Do not make header.site sticky. Task 2 completes the 44px box and panel tokens.
  </action>
  <verify>
    <automated>npm test && test -f src/components/NavMenu.astro && rg -n "NavMenu" src/components/Header.astro && rg -n "id=\"navMenu\"" src/components/Header.astro && rg -n "ThemeToggle" src/components/Header.astro && rg -n "is:inline" src/components/NavMenu.astro && rg -F "id=\"navToggle\"" src/components/NavMenu.astro && rg -n "aria-controls" src/components/NavMenu.astro && rg -n "inert" src/components/NavMenu.astro && rg -n "DOMContentLoaded" src/components/NavMenu.astro && rg -n "Escape" src/components/NavMenu.astro && rg -n "pointerdown" src/components/NavMenu.astro && rg -n "matchMedia" src/components/NavMenu.astro && rg -n "max-width: 640px" src/styles/global.css && rg -n "Open menu" src/i18n/ui.ts && rg -n "打开菜单" src/i18n/ui.ts</automated>
    <fails_when>npm test exits non-zero; NavMenu.astro is missing; rg prints no matches for NavMenu or navMenu id or ThemeToggle in Header, or for is:inline, navToggle id, aria-controls, inert, DOMContentLoaded, Escape, pointerdown, or matchMedia in NavMenu, or for max-width 640px in global.css, or for Open menu / 打开菜单 in ui.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - src/components/NavMenu.astro exists with is:inline, id="navToggle", aria-expanded, aria-controls="navMenu", data-label-menu, data-label-close, DOMContentLoaded, inert, Escape, pointerdown, and matchMedia per D-01, D-02, D-03, NAV-02, NAV-03, NAV-04
    - src/components/Header.astro imports NavMenu, has optional locale defaulting to en, contains id="navMenu" with href="/tools/", href="/blog/", href="/about/", and ThemeToggle after that div per D-01, D-03
    - src/i18n/ui.ts contains nested nav.menu / nav.close on en (Open menu, Close menu) and zh (打开菜单, 关闭菜单) per NAV-05, D-03
    - src/styles/global.css contains @media (max-width: 640px), #navToggle, visibility: hidden, and .nav.is-open per NAV-01, NAV-04
    - src/components/ThemeToggle.astro, src/components/ThemeInit.astro, and src/layouts/BaseLayout.astro are not modified by this task
  </acceptance_criteria>
  <done>A visitor path exists: at max-width 640px a real button toggles an overlay of Tools/Blog/About with ARIA in sync, Escape and outside pointer close, widen clears leftover inert, and EN/ZH labels live on the button (NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, D-01, D-02, D-03, D-04).</done>
</task>

<task type="auto">
  <name>Hamburger 44px chrome and overlay panel tokens</name>
  <files>src/styles/global.css</files>
  <read_first>.planning/phases/08-mobile-hamburger-menu/08-UI-SPEC.md, src/styles/global.css</read_first>
  <action>
    In src/styles/global.css only, complete the NavMenu button box and Overlay plus breakpoint blocks from 08-UI-SPEC.md (NAV-01, D-03, D-04). #navToggle duplicates the #themeToggle 44px box (width height min-width min-height 44px, padding 0, margin 0, border 0, background transparent, color var(--text), cursor pointer, align-items and justify-content center) but default display none so the control is absent from desktop tab order. #navToggle svg is 20px display block. Do not share a class with #themeToggle and do not edit ThemeToggle.astro.

    Inside the existing @media (max-width: 640px) only: header.site position relative z-index 20; #navToggle display inline-flex; #themeToggle margin-left auto; .nav-links full overlay (flex-direction column, gap 16px, margin-left 0, padding 16px, background var(--panel), border-bottom 1px solid var(--border), z-index 1); .nav-links a padding 8px 0 and overflow-wrap anywhere. Do not add a top border or box-shadow on the overlay. Do not add --sp-* tokens. Do not restyle .tool-panel button. Do not change logo font-weight 650 or .nav min-height 3.25rem. Desktop keeps .nav-links margin-left auto. Do not introduce a second breakpoint. Do not copy overlay fonts or sticky/blur header from the dirty tree.
  </action>
  <verify>
    <automated>rg -n "#navToggle" src/styles/global.css && rg -n "max-width: 640px" src/styles/global.css && rg -n "z-index: 20" src/styles/global.css && rg -n "flex-direction: column" src/styles/global.css && rg -n "visibility: hidden" src/styles/global.css && rg -n "display: none" src/styles/global.css && npm test</automated>
    <fails_when>rg prints no matches for #navToggle, max-width 640px, z-index 20, flex-direction column, visibility hidden, or display none in global.css; npm test exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - src/styles/global.css contains #navToggle with display: none by default and display: inline-flex inside @media (max-width: 640px) per NAV-01, D-03
    - The 640px query contains header.site z-index: 20, #themeToggle margin-left: auto, .nav-links flex-direction: column, background var(--panel), border-bottom using var(--border), and .nav.is-open .nav-links visibility: visible per D-01, D-04
    - #navToggle box uses width/height/min-width/min-height 44px matching 08-UI-SPEC.md per D-04
  </acceptance_criteria>
  <done>At max-width 640px the header stays one row with a 44px hamburger and 44px theme toggle, and the three links overlay under the header with panel fill instead of wrapping the bar (NAV-01, D-01, D-04).</done>
</task>

<task type="auto">
  <name>Production inline menu script and safety gates</name>
  <files>src/components/NavMenu.astro, src/components/Header.astro, src/styles/global.css, src/i18n/ui.ts</files>
  <read_first>src/components/NavMenu.astro, src/components/Header.astro, package.json</read_first>
  <action>
    Do not add packages, test runners, Playwright, or a helper under src/lib. Confirm NavMenu is static Astro with is:inline, boots after the document is parsed so #navMenu exists, swaps labels only via data-label-menu / data-label-close attributes, writes aria-expanded only as the literals true and false, and never writes web storage or cookies (D-02, D-04). Confirm widen removes inert (T-08-02). Confirm ThemeToggle.astro, ThemeInit.astro, BaseLayout.astro, src/lib, astro.config.mjs, and src/pages are not in this phase diff. Confirm package.json and package-lock.json match HEAD. Confirm src/lib/nav.ts was not created.

    Run the existing Vitest suite and a production Astro build. After build, confirm dist HTML contains the raw navToggle control (not only a hashed module). Stage and commit only the four in-scope files listed in this plan frontmatter. Do not stage src/pages/zh, LangSwitch, or stash contents.
  </action>
  <verify>
    <automated>npm test && npm run build && rg -F "navToggle" dist --glob "*.html" && rg -n "is:inline" src/components/NavMenu.astro && ! rg -n "setItem" src/components/NavMenu.astro && ! rg -n "innerHTML" src/components/NavMenu.astro && ! rg -n "document.cookie" src/components/NavMenu.astro && ! rg -n "localStorage" src/components/NavMenu.astro && ! rg -n "from 'preact'" src/components/NavMenu.astro && ! rg -n "client:load" src/components/NavMenu.astro && test -z "$(git diff HEAD -- package.json package-lock.json)" && test ! -f src/lib/nav.ts</automated>
    <fails_when>npm test or npm run build exits non-zero; no dist HTML file contains navToggle; NavMenu.astro matches setItem, innerHTML, document.cookie, localStorage, a preact import, or client:load; package.json or package-lock.json differs from HEAD; src/lib/nav.ts exists</fails_when>
    <human-check>
      DevTools width 640px: one header row (logo, 44px hamburger, 44px theme toggle); links collapsed. Width 641px: no hamburger; Tools/Blog/About in one row. Click hamburger: overlay under header, aria-expanded true, label Close menu; ThemeToggle still in the header row; page content covered not pushed. Click a nav link: new page loads collapsed. Click ThemeToggle while open: theme flips; menu stays open. Pointer-down on main: menu closes without moving focus. Escape while open: menu closes; focus on #navToggle. Closed mobile tab order: logo, hamburger, ThemeToggle, main (no Tools/Blog/About). Open mobile tab order: hamburger, Tools, Blog, About, ThemeToggle, main (no trap). Open at 640 then drag to 900: overlay clears and desktop links work.
    </human-check>
  </verify>
  <acceptance_criteria>
    - dist HTML contains navToggle so the menu IIFE is not only a hashed module (NAV-02, D-01)
    - src/components/NavMenu.astro has is:inline and has no setItem, no innerHTML, no document.cookie, no localStorage, no preact import, and no client:load (D-04, T-08-01, T-08-03)
    - package.json matches HEAD (D-04)
    - src/lib/nav.ts does not exist
    - npm test and npm run build both exit 0
  </acceptance_criteria>
  <done>Production HTML inlines the disclosure script, labels stay SSG attribute copies, leftover inert cannot stick on desktop, no new packages, and existing lib tests still pass (NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSG strings → #navToggle attributes | ui.ts nav labels are baked into aria-label and data-label-* at build; script copies them into aria-label |
| Inline script → header DOM | NavMenu is:inline IIFE toggles classes, ARIA, and inert on #navMenu / nav.nav |
| Viewport → inert | matchMedia 640px plus close() must not leave inert on desktop links |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | NavMenu.astro labels | high | mitigate | Bake strings at SSG; copy data-label-menu / data-label-close with getAttribute / setAttribute only; never assign markup from strings (ASVS V5) |
| T-08-02 | Denial of service | NavMenu.astro inert leftover | high | mitigate | Close and matchMedia change remove inert when the 640px query does not match so desktop Tools/Blog/About stay operable |
| T-08-03 | Tampering | Menu open state | medium | mitigate | DOM-only state; new document always collapsed; NavMenu does not write web storage or cookies |
| T-08-04 | Denial of service | Missing #navMenu at parse | medium | mitigate | Null-check nodes; boot on DOMContentLoaded when still loading; no error chrome on failure |
| T-08-05 | Elevation of privilege | NavMenu inline script | low | accept | No CSP in repo today; keep the IIFE tiny with no eval; future CSP would allow this hash or a nonce |
| T-08-06 | Spoofing | #navToggle button | low | accept | Control is not an auth surface; names are SSG chrome strings |
| T-08-07 | Information disclosure | Menu state | low | accept | Open/closed is not a secret and is not persisted |
| T-08-08 | Repudiation | Nav clicks | low | accept | Public static pages; no accounts or audit log |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install tasks this phase; do not add Playwright or icon packs |

High-severity T-08-01 and T-08-02 are mitigated in NavMenu.astro (block-on high). T-08-SC has no install to perform.
</threat_model>

<verification>
After all three tasks: npm test green; npm run build green; NavMenu.astro exists with is:inline, ARIA, inert, Escape, pointerdown, matchMedia, DOMContentLoaded; Header DOM order logo / NavMenu / #navMenu / ThemeToggle; ui.ts has en+zh nav.menu/nav.close; global.css has max-width 640px overlay, #navToggle 44px, z-index 20; dist HTML contains navToggle; NavMenu has no web-storage writes and no markup injection; package.json matches HEAD; no src/lib/nav.ts. Manual 640px checklist lives in the last task human-check (end-of-phase harvest). Do not add Playwright or theme.test.ts.
</verification>

<success_criteria>
- NAV-01: hamburger visible at max-width 640px and nav links collapsed; wider widths keep the full nav; leftover open state cannot persist on desktop
- NAV-02: real button with aria-expanded and aria-controls=navMenu matching open state
- NAV-03: Escape closes and returns focus to #navToggle
- NAV-04: collapsed links are not focusable (inert and visibility hidden)
- NAV-05: nav.menu / nav.close on en and zh; script reads data-label-*
- D-01 D-02 D-03 D-04 all implemented; deferred focus trap, scroll lock, src/pages/zh, ThemeToggle zh label, spacing scale, and FAQ chrome are absent
</success_criteria>

<output>
Create `.planning/phases/08-mobile-hamburger-menu/08-01-SUMMARY.md` when done
</output>
