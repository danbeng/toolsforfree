---
phase: 10-interactive-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/FaqList.astro
autonomous: true
requirements:
  - CHR-01
  - CHR-02
  - CHR-03
  - CHR-04
  - CHR-05
  - CHR-06
  - CHR-07

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor hovering an enabled .tool-panel button (Copy and in-panel island actions) sees accent fill and --bg text with a 120ms color transition; hamburger #navToggle and #themeToggle stay 44px icon chrome (CHR-01, D-01)"
    - "Visitor pressing an enabled .tool-panel button sees a darker color-mix fill and border; no scale (CHR-02, D-01)"
    - "Visitor tabbing to Copy keeps the existing 2px --accent outline; FAQ summary has the same 2px / 2px-offset ring (CHR-03, D-01)"
    - "Empty output: Copy is disabled at opacity 0.45 with not-allowed cursor; hover and press do not invert (CHR-01, CHR-02, D-01, UI empty)"
    - "Visitor sees .tool-panel with 1px --border, box-shadow 0 1px 2px var(--border), padding var(--sp-4), radius 8px, readable in both themes (CHR-04, D-02)"
    - "Visitor can expand and collapse FAQ rows as native independent disclosures; first paint is closed; English h2 FAQ (CHR-05, D-03)"
    - "Visitor sees the UA disclosure triangle on each FAQ summary (CHR-06, D-03)"
    - "Idle accent-on-panel, hover --bg-on-accent, active darkened-accent with --bg text, and FAQ --text on canvas eyeball at 4.5:1 in both themes; disabled Copy stays 0.45 (CHR-07, D-04)"
    - "FAQ items=[] paints English h2 FAQ plus an empty .faq wrapper only; no extra empty copy (UI empty, D-03)"
    - "No fetch or spinner for Copy or FAQ (UI loading)"
    - "Tool parse errors stay existing .tool-error role=alert; this phase adds no clipboard-failure UI (UI error, D-02)"
    - "One details row per { question, answer }; many stack with --sp-4 between items (UI populated / zero-one-many, D-03)"
    - "FAQ question and answer wrap with overflow-wrap anywhere; no ellipsis (UI overflow / long-text, D-03)"
    - "prefers-reduced-motion: reduce turns the button color transition off (CHR-01, D-01)"
    - "Files are authored from HEAD chrome; do not apply stash entries; do not commit the ZH page tree or the locale switcher; do not edit the Preact tool-panel shell (D-04, HEAD vs overlay)"
    - "Zero new npm packages; no Playwright; no jsdom switch; no CSS unit tests; no src/lib edits; no Tailwind (D-04)"
    - statement: "In both themes, idle .tool-panel button accent-on-panel, hover --bg-on-accent, active darkened-accent with --bg text, and FAQ summary/answer --text on the canvas eyeball at 4.5:1; disabled Copy stays opacity 0.45 with no hover invert"
      verification: backstop
  artifacts:
    - path: src/styles/global.css
      provides: ".tool-panel shadow and --sp-4 padding; .tool-panel button hover/active/disabled; .faq details/summary/marker/focus (CHR-01, CHR-02, CHR-03, CHR-04, CHR-06, D-01, D-02, D-03)"
      min_lines: 250
    - path: src/components/FaqList.astro
      provides: "HEAD items-only Props; English h2 FAQ; native details/summary/p in div.faq (CHR-05, D-03, D-04)"
  key_links:
    - from: src/components/FaqList.astro
      to: src/pages/tools/[slug].astro
      via: "HEAD caller already passes items={page.data.faq}; this phase does not edit the page"
      pattern: "FaqList"
    - from: src/styles/global.css
      to: ".tool-panel button"
      via: ":hover:not(:disabled) invert and :active:not(:disabled) color-mix; Copy stays unclassed in HEAD ToolShell"
      pattern: "button:hover:not(:disabled)"
    - from: src/styles/global.css
      to: ".tool-panel"
      via: "box-shadow 0 1px 2px var(--border); padding var(--sp-4)"
      pattern: "box-shadow: 0 1px 2px var(--border)"
    - from: src/styles/global.css
      to: ".faq summary::marker"
      via: "UA triangle colored with --text; summary focus-visible 2px accent"
      pattern: "summary::marker"
  prohibitions:
    - "Do not commit overlay ZH pages or the locale switcher (D-04)"
    - "Do not edit or stage the Preact tool-panel shell (D-02, D-04)"
    - "Do not add a network endpoint for FAQ or Copy (privacy)"
    - "Do not pop stash@{0} or stash@{1} (D-04)"
---

<objective>
Ship HEAD interactive chrome: native FAQ disclosures, tool-panel --border shadow plus --sp-4 padding, and .tool-panel button invert hover / color-mix active — per D-01 (Button states), D-02 (Tool-panel chrome), D-03 (FAQ collapse), D-04 (Contrast, HEAD vs overlay), CHR-01–CHR-07.

Purpose: Phase 10 visual chrome. Visitors get polished Copy/island buttons, readable panels in both themes, and collapsible FAQs without overlay LED, ZH commits, or ToolShell edits.

Output: HEAD src/components/FaqList.astro rewritten to details/summary; src/styles/global.css Token implementation plus FAQ chrome copied from 10-UI-SPEC.md. No new npm packages. No Playwright. No src/lib. No Tailwind.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-interactive-chrome/10-CONTEXT.md
@.planning/phases/10-interactive-chrome/10-RESEARCH.md
@.planning/phases/10-interactive-chrome/10-PATTERNS.md
@.planning/phases/10-interactive-chrome/10-UI-SPEC.md
@.planning/phases/10-interactive-chrome/10-VALIDATION.md
@.planning/phases/09-grid-spacing/09-01-SUMMARY.md

<interfaces>
From git show HEAD:src/components/FaqList.astro — Props { items: { question: string; answer: string }[] } only. Markup is h2 FAQ plus a definition list of dt/dd. No locale. No ui.ts.

From git show HEAD:src/components/ToolShell.tsx — named export ToolShell; props error, output, children; Copy button type=button disabled={!props.output} labels Copy / Copied; wrapper class tool-panel. No locale. No LED. Do not edit this file (D-02).

From src/styles/global.css — :root has Phase 7 colors and Phase 9 --sp-1 through --sp-12; a:focus-visible, button:focus-visible is 2px --accent; #navToggle / #themeToggle are 44px transparent; .tool-panel is 1px --border, padding 1rem, radius 8px; .tool-panel button is transparent + accent stroke; :disabled opacity 0.45; no details rules.

From 10-UI-SPEC.md — copy Token implementation, FAQ chrome, and Markup (HEAD FaqList only) sections verbatim (D-01, D-02, D-03). Discretion already locked: 120ms ease; box-shadow 0 1px 2px var(--border); unclassed Copy; color-mix(in srgb, var(--accent) 72%, #000000).

Locked CONTEXT groups: D-01 Button states, D-02 Tool-panel chrome, D-03 FAQ collapse, D-04 Contrast / HEAD vs overlay.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end FAQ details plus panel and button chrome — one path</name>
  <reversibility rating="costly">Native FAQ markup plus .tool-panel / .tool-panel button CSS become the chrome contract later pages inherit; undoing it is a coordinated CSS and markup change, not a data migration.</reversibility>
  <precondition>git show HEAD:src/components/FaqList.astro contains FAQ and a definition list; src/styles/global.css contains .tool-panel with padding: 1rem and a:focus-visible, button:focus-visible; git stash list still shows stash@{0} and stash@{1}</precondition>
  <files>src/components/FaqList.astro, src/styles/global.css</files>
  <read_first>.planning/phases/10-interactive-chrome/10-UI-SPEC.md, .planning/phases/10-interactive-chrome/10-CONTEXT.md, .planning/phases/10-interactive-chrome/10-PATTERNS.md, .planning/phases/10-interactive-chrome/10-RESEARCH.md, .planning/phases/10-interactive-chrome/10-VALIDATION.md, src/components/FaqList.astro, src/styles/global.css</read_first>
  <action>
    Before any markup edit, restore HEAD bytes of src/components/FaqList.astro with git checkout HEAD -- src/components/FaqList.astro (Wave 0, D-04). Do not apply or pop git stash entries. Do not checkout src/styles/global.css (Phase 7–9 live there). Do not checkout or edit the Preact tool-panel shell. Leave Header, NavMenu, ThemeToggle, ThemeInit, src/lib, src/data/tools.ts, src/i18n, and src/pages/tools/[slug].astro untouched. Do not add src/lib/chrome.ts.

    Replace the HEAD definition-list markup with the Markup (HEAD FaqList only) section of 10-UI-SPEC.md (CHR-05, D-03). Keep Props as items only. Keep the English h2 FAQ string. Wrap rows in div.faq. Each row is details / summary / p with Astro text interpolation for question and answer strings. Omit the Boolean expanded attribute (presence would expand every row) and any exclusive-accordion grouping attribute (D-03). Do not add extra Props fields. Do not change the tool page caller.

    In src/styles/global.css only, replace the existing .tool-panel / form-control / button / disabled block with the Token implementation (executor source of truth) section of 10-UI-SPEC.md verbatim (CHR-01, CHR-02, CHR-04, D-01, D-02). That block must include box-shadow 0 1px 2px var(--border), padding var(--sp-4), radius 8px, 120ms color transition, invert hover on .tool-panel button:hover:not(:disabled), color-mix active on .tool-panel button:active:not(:disabled), disabled opacity 0.45, and prefers-reduced-motion: reduce { transition: none }. Keep form-control padding 0.6rem / margin-bottom 0.75rem and button padding 0.4rem 0.8rem. Do not add a Copy class. Do not invert #navToggle or #themeToggle. Do not add a second outline on .tool-panel button. Do not scale on press or hover. Do not introduce --shadow, --led, or --radius. Leave a:focus-visible, button:focus-visible as HEAD (CHR-03, D-01).

    Do not append .faq rules yet. Task 2 adds FAQ chrome CSS. Path-limited git add of src/components/FaqList.astro and src/styles/global.css only (D-04).
  </action>
  <verify>
    <automated>npm test && rg -F -- "button:hover:not(:disabled)" src/styles/global.css && rg -F -- "button:active:not(:disabled)" src/styles/global.css && rg -F -- "background: var(--accent)" src/styles/global.css && rg -F -- "color-mix(in srgb, var(--accent) 72%, #000000)" src/styles/global.css && rg -F -- "box-shadow: 0 1px 2px var(--border)" src/styles/global.css && rg -F -- "padding: var(--sp-4)" src/styles/global.css && rg -F -- "border-radius: 8px" src/styles/global.css && rg -F -- "transition: background-color 120ms ease, color 120ms ease, border-color 120ms ease" src/styles/global.css && rg -F -- "prefers-reduced-motion: reduce" src/styles/global.css && rg -F -- "opacity: 0.45" src/styles/global.css && rg -n "details" src/components/FaqList.astro && rg -n "summary" src/components/FaqList.astro && rg -F -- "FAQ" src/components/FaqList.astro && rg -F -- "class=\"faq\"" src/components/FaqList.astro && ! rg -n "dt>" src/components/FaqList.astro && ! rg -n "heading" src/components/FaqList.astro && ! rg -n "open" src/components/FaqList.astro && ! rg -F -- "name=" src/components/FaqList.astro && rg -F -- "a:focus-visible, button:focus-visible" src/styles/global.css && rg -F -- "width: 44px" src/styles/global.css && rg -F -- "padding: 0.4rem 0.8rem" src/styles/global.css && rg -F -- "--sp-1: 4px" src/styles/global.css && rg -n "card-grid" src/styles/global.css && rg -n "min-width: 1080px" src/styles/global.css</automated>
    <fails_when>npm test exits non-zero; rg prints no matches for hover:not(:disabled), active:not(:disabled), color-mix, box-shadow 0 1px 2px var(--border), padding var(--sp-4), 120ms transition, reduced-motion, details, summary, h2 FAQ, class=faq, existing focus rule, width 44px, button padding 0.4rem 0.8rem, --sp-1: 4px, card-grid, or min-width 1080px; or FaqList still contains a definition list, heading, open, or name=</fails_when>
  </verify>
  <acceptance_criteria>
    - src/components/FaqList.astro is HEAD items-only Props with English h2 FAQ and native details/summary/p inside div.faq per CHR-05, D-03
    - src/styles/global.css .tool-panel has --border shadow, var(--sp-4) padding, radius 8px per CHR-04, D-02
    - src/styles/global.css .tool-panel button uses :hover:not(:disabled) invert and :active:not(:disabled) color-mix per CHR-01, CHR-02, D-01
    - src/styles/global.css still contains Phase 9 --sp-1, card-grid, min-width 1080px, 44px hit targets, and the global 2px accent focus rule per D-04
  </acceptance_criteria>
  <done>A visitor path exists: HEAD FAQ rows are native disclosures, .tool-panel has a --border shadow and --sp-4 padding, and enabled panel buttons invert on hover and darken on press without touching the Preact shell (CHR-01, CHR-02, CHR-04, CHR-05, D-01, D-02, D-03, D-04).</done>
</task>

<task type="auto">
  <name>Ship FAQ marker, type, and summary focus chrome</name>
  <files>src/styles/global.css</files>
  <read_first>.planning/phases/10-interactive-chrome/10-UI-SPEC.md, src/styles/global.css, src/components/FaqList.astro</read_first>
  <action>
    In src/styles/global.css only, append the FAQ chrome section of 10-UI-SPEC.md verbatim (CHR-03, CHR-06, D-03). That block must include .faq details margin 0 0 var(--sp-4); .faq summary at 16px / 600 / 1.2 with padding var(--sp-2) 0, color var(--text), overflow-wrap anywhere; summary::marker color var(--text); .faq summary:focus-visible 2px --accent outline with 2px offset; .faq details child p at 16px / 400 / 1.5, color var(--text), margin 0 0 var(--sp-2).

    Keep the UA disclosure triangle. Do not hide it with list or WebKit-only display rules, and do not set summary to flex or block (CHR-06). Do not restyle page h2. Do not mute FAQ answers. Do not add FAQ row borders, cards, or accent fills. Do not add a second outline on .tool-panel button. Do not retokenize hamburger 44px, overlay 16px / 8px, .tool-panel button padding 0.4rem 0.8rem, form-control 0.6rem / 0.75rem, .tool-grid gap 1rem, footer rem, or --content 52rem (D-02). New CSS weights this phase stay 400 and 600 only.

    Path-limited git add of src/styles/global.css only (D-04).
  </action>
  <verify>
    <automated>rg -F -- "summary::marker" src/styles/global.css && rg -F -- ".faq summary:focus-visible" src/styles/global.css && rg -F -- ".faq details" src/styles/global.css && rg -F -- "padding: var(--sp-2) 0" src/styles/global.css && rg -F -- "margin: 0 0 var(--sp-4)" src/styles/global.css && rg -F -- "a:focus-visible, button:focus-visible" src/styles/global.css && rg -F -- "width: 44px" src/styles/global.css && rg -F -- "gap: 1rem" src/styles/global.css && rg -F -- "--content: 52rem" src/styles/global.css && rg -n "font-weight: 650" src/styles/global.css && ! rg -n "webkit-details-marker" src/styles/global.css && ! rg -n "tool-panel button:focus" src/styles/global.css && ! rg -n "^button:hover" src/styles/global.css && ! rg -F -- "transform:" src/styles/global.css && ! awk "/^\\.faq summary/,/^}/" src/styles/global.css | rg -F -- "list-style" && npm test</automated>
    <fails_when>rg prints no matches for summary::marker, .faq summary:focus-visible, .faq details, padding var(--sp-2) 0, FAQ details margin --sp-4, global focus rule, width 44px, gap: 1rem, --content: 52rem, or font-weight 650; webkit-details-marker or tool-panel button:focus or a file-start button:hover or transform: appears; list-style appears inside the .faq summary block; npm test exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - src/styles/global.css contains summary::marker, .faq summary:focus-visible, and .faq details spacing per CHR-03, CHR-06, D-03
    - src/styles/global.css still contains width: 44px, gap: 1rem, --content: 52rem, and font-weight: 650
    - src/styles/global.css has no webkit-details-marker, no .tool-panel button:focus, no file-start button:hover, and no transform:
  </acceptance_criteria>
  <done>FAQ summaries show a UA triangle, 16px/600 questions, --text answers, and a 2px accent keyboard ring, while hamburger and catalog literals stay as HEAD (CHR-03, CHR-06, D-01, D-03).</done>
</task>

<task type="auto">
  <name>Production build and overlay-commit safety gates</name>
  <files>src/styles/global.css, src/components/FaqList.astro</files>
  <read_first>src/styles/global.css, src/components/FaqList.astro, package.json, .planning/phases/09-grid-spacing/09-01-SUMMARY.md</read_first>
  <action>
    Do not add packages, Playwright, a jsdom Vitest switch, Tailwind, a helper under src/lib, or a CSS/jsdom test file. Confirm src/lib, src/data/tools.ts, and the Preact tool-panel shell were not edited this phase. Confirm package.json and package-lock.json match HEAD. Confirm src/lib/chrome.ts was not created. Confirm git stash list still shows both overlay-chrome-temp and the unrelated i18n stash; do not apply them (D-04).

    Run the existing Vitest suite (`npm test`). Then prove SSG with a production Astro build (T-10-01). If `npm run build` fails because dirty overlay pages import missing modules (same UNRESOLVED_IMPORT class as Phase 9 T-09-01), isolate overlay to `.git/gsd-overlay-backup-10-01`, restore those overlay paths to HEAD, move the untracked locale switcher and ZH page tree into that backup, run `npm run build` on HEAD plus this plan's two files, and `rg` `details` / `tool-panel` in `dist` **while still isolated**. Then restore the overlay from the backup. Do not pop git stash. Do not commit overlay files. Leave in-scope FaqList and global.css as this plan wrote them. Leave the dirty Preact shell in the worktree.

    After a green isolated (or clean) build and overlay restore, do **not** re-run `npm run build` on the restored dirty tree — that tree is known to fail SSG. Stage and commit only src/styles/global.css and src/components/FaqList.astro via path-limited git add (D-04). Never stage the ZH page tree, the untracked locale switcher, the Preact tool-panel shell, or the entire working tree.
  </action>
  <verify>
    <automated>npm test && rg -n "details" dist --glob "*.html" && rg -n "tool-panel" dist --glob "*.html" && test ! -f src/lib/chrome.ts && PKG_DIFF="$(git diff HEAD -- package.json package-lock.json src/data/tools.ts src/data/tools.test.ts)" && test -z "$PKG_DIFF" && STASH_LIST="$(git stash list)" && echo "$STASH_LIST" | rg -F "gsd-phase7-overlay-chrome-temp" && echo "$STASH_LIST" | rg -F "pre-02-01-merge unrelated i18n" && CACHED="$(git diff --cached --name-only)" && ! echo "$CACHED" | rg -F "src/pages/zh/" && ! echo "$CACHED" | rg -F "LangSwitch.astro" && ! echo "$CACHED" | rg -F "src/components/ToolShell.tsx"</automated>
    <fails_when>npm test exits non-zero; no dist HTML contains details or tool-panel; package.json, package-lock.json, src/data/tools.ts, or src/data/tools.test.ts differs from HEAD; src/lib/chrome.ts exists; git stash list does not contain gsd-phase7-overlay-chrome-temp or pre-02-01-merge unrelated i18n; cached names include src/pages/zh/, LangSwitch.astro, or src/components/ToolShell.tsx</fails_when>
    <human-check>
      On /tools/json-formatter/ (or /tools/uuid-generator/) in dark then light: idle Copy is transparent with accent border and text; hover enabled Copy fills --accent with --bg text, about 120ms, no scale; press is darker than hover, no scale; empty output Copy is opacity 0.45 and does not invert on hover. Tab to Copy: one 2px accent outline. Hover hamburger (≤640px) and theme toggle: they do not fill accent. Panel shows 1px border plus a small --border shadow in both themes. FAQ: English h2 FAQ, every row closed on first paint, UA triangle visible; opening one row leaves others as they were; Tab to a summary shows 2px accent outline; Enter/Space toggles (Safari may skip Tab). FAQ text is --text, not muted. Light-theme hovered Copy stays readable (tightest pair). DevTools emulate prefers-reduced-motion: reduce: no color transition. If color-mix is unsupported, active may match hover — still an invert. Cached diff names are only the two in-scope files.
    </human-check>
  </verify>
  <acceptance_criteria>
    - dist HTML contains details and tool-panel so FAQ disclosures and panel class survived SSG (CHR-04, CHR-05)
    - package.json matches HEAD; src/lib/chrome.ts does not exist (D-04)
    - git stash list still contains both named stash entries (D-04)
    - npm test exits 0; dist HTML from the isolated (or clean) build contains details and tool-panel (T-10-01)
  </acceptance_criteria>
  <done>Production HTML includes FAQ details and tool-panel, no new packages, overlay stashes untouched, Preact shell unstaged, and existing lib tests still pass (CHR-01–CHR-07, D-04, T-10-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSG FAQ strings → HTML | Author markdown { question, answer } rendered as text in FaqList; not visitor input |
| Pointer/keyboard → CSS | :hover / :active / :focus-visible on .tool-panel button and .faq summary |
| Git index → overlay files | Dirty ZH tree, locale switcher, and Preact shell must not enter the commit (T-10-01) |
| Worktree overlay → astro build | Dirty pages may UNRESOLVED_IMPORT; isolate for build proof then restore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | git commit / overlay pages / astro build | high | mitigate | Restore HEAD FaqList before markup; path-limited git add of FaqList.astro and global.css only; never commit src/pages/zh/ or LangSwitch.astro; do not pop stash@{0} or stash@{1}; if dirty overlay breaks astro, isolate to .git/gsd-overlay-backup-10-01 like Phase 9 T-09-01, build, then restore overlay |
| T-10-02 | Tampering | FaqList.astro answer/question render | medium | mitigate | Astro text interpolation only; never HTML-raw FAQ strings (ASVS V5, primary render boundary) |
| T-10-03 | Elevation of privilege | global button hover | medium | mitigate | Selector is .tool-panel button only so #navToggle / #themeToggle cannot inherit invert |
| T-10-04 | Tampering | FAQ widget | low | mitigate | Native details; no accordion script this phase |
| T-10-05 | Information disclosure | Theme / contrast | low | accept | Theme localStorage is unchanged from Phase 7; contrast is public CSS |
| T-10-06 | Denial of service | Chrome CSS | low | accept | Static CSS and native HTML; no layout worker |
| T-10-07 | Spoofing | Copy button | low | accept | Clipboard write stays existing HEAD ToolShell; this phase does not edit it |
| T-10-08 | Repudiation | FAQ clicks | low | accept | Public static pages; no accounts or audit log |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install tasks this phase; do not add Playwright, Tailwind, contrast tools, or icon packs |

High-severity T-10-01 is mitigated by HEAD restore, path-limited add, and overlay-isolation-for-build (block-on high). T-10-SC has no install to perform.
</threat_model>

<verification>
After all three tasks: npm test green; production build green on the isolated HEAD+plan tree if overlay SSG fails (T-10-01), then overlay restored — do not require a green `astro build` on the restored dirty tree; FaqList is details/summary with English h2 FAQ and items-only Props; .tool-panel has --border shadow and --sp-4 padding; .tool-panel button has :hover:not(:disabled) invert and :active:not(:disabled) color-mix; .faq summary::marker and summary:focus-visible exist; global 2px accent focus and 44px hit targets remain; dist HTML contains details and tool-panel; package.json matches HEAD; no src/lib/chrome.ts; stashes remain; cached names exclude ZH tree, locale switcher, and Preact shell. Manual hover/active/FAQ/contrast checklist lives in the last task human-check (end-of-phase harvest). Do not add Playwright or CSS unit tests.
</verification>

<success_criteria>
- CHR-01: .tool-panel button:hover:not(:disabled) invert plus 120ms color transition; no global button:hover
- CHR-02: .tool-panel button:active:not(:disabled) color-mix darken; no scale
- CHR-03: existing a/button 2px accent outline kept; .faq summary:focus-visible matches; no extra outline on .tool-panel button
- CHR-04: .tool-panel 1px --border, box-shadow 0 1px 2px var(--border), padding var(--sp-4), radius 8px
- CHR-05: FaqList is independent details/summary, closed first paint, English h2 FAQ
- CHR-06: summary::marker visible; triangle not removed
- CHR-07: human UAT both themes at 4.5:1; disabled Copy stays 0.45
- D-01, D-02, D-03, D-04 implemented; deferred LED, ZH commit, exclusive accordion, custom plus/minus, active scale, contrast npm, and hamburger-as-filled-button are absent
</success_criteria>

## Artifacts this phase produces

- `.tool-panel` `box-shadow: 0 1px 2px var(--border)` and `padding: var(--sp-4)` in `src/styles/global.css`
- `.tool-panel button:hover:not(:disabled)` invert and `:active:not(:disabled)` `color-mix` plus `prefers-reduced-motion: reduce`
- `.faq` details / summary / `::marker` / `:focus-visible` rules in `src/styles/global.css`
- HEAD `src/components/FaqList.astro` native disclosures (`div.faq` + `details` / `summary` / `p`)

## Flagged assumptions (research)

- RESEARCH A1: if a visitor engine rejects color-mix, :active may equal :hover invert; do not add @supports or a second token
- RESEARCH A2: Safari may ignore ::marker color; CHR-06 still holds when a triangle is visible
- RESEARCH A3: local luminance math is not a substitute for human UAT (CHR-07)
- RESEARCH A4: macOS Safari without Full Keyboard Access may not Tab to summary
- RESEARCH A5: extra title prop from dirty tool pages is unused if FaqList Props omit it; do not edit or commit that page

<output>
Create `.planning/phases/10-interactive-chrome/10-01-SUMMARY.md` when done
</output>
