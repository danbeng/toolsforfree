---
phase: 14-ci-green-on-main
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
  - CI-02

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - ".github/workflows/ci.yml exists, is named CI, and triggers on push and pull_request to main plus workflow_dispatch (CI-01, D-Triggers)"
    - "The workflow pins actions/setup-node@v4 to node-version 22 with cache npm, then runs exactly npm ci, npm test, and npm run build (CI-01, D-Steps, D-Triggers)"
    - "permissions are contents read; the file has no secrets, no deploy job, no lint, no astro check, and no gh invocation (CI-01, D-Triggers)"
    - "npm test then npm run build exit 0 on the current working tree with no overlay isolation and no stash pop (CI-02, D-Proof)"
    - "src/lib/crontab.ts stays unstaged after the proof; LED ToolShell and SITE_ORIGIN are untouched (D-Proof)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Node 22 CI file with npm ci, npm test, npm run build (CI-01, D-Triggers, D-Steps)"
      contains: "node-version: 22"
  key_links:
    - from: .github/workflows/ci.yml
      to: package.json
      via: "npm test script is vitest run; npm run build script is astro build"
      pattern: "npm run build"
    - from: .github/workflows/ci.yml
      to: package-lock.json
      via: "npm ci plus setup-node cache npm hashes the lockfile"
      pattern: "npm ci"
  prohibitions:
    - "Do not commit src/lib/crontab.ts"
    - "Do not pop stash@{0} or stash@{1}"
    - "Do not commit LED ToolShell or tool-panel__chrome"
    - "Do not change SITE_ORIGIN"
    - "Do not run gh repo create or add a remote"
    - "Do not add lint, astro check, or a deploy job"
    - "Path-limited git add of .github/workflows/ci.yml only"
---

<objective>
Add `.github/workflows/ci.yml` and prove `npm test` then `astro build` on the current tree without overlay isolation (CI-01, CI-02, D-Triggers, D-Steps, D-Proof).

Purpose: A future clean checkout of main can run the same two commands the workflow declares. This phase ships the file and the local proof only. No GitHub remote is created.

Output: `.github/workflows/ci.yml` pinned to Node 22, plus a green `npm test` and `npm run build` while `src/lib/crontab.ts` stays unstaged.

Hard fences (D-Proof): Do not stage `src/lib/crontab.ts`. Do not pop `stash@{0}` or `stash@{1}`. Do not commit LED ToolShell / `tool-panel__chrome`. Do not change `SITE_ORIGIN`. Do not run `gh repo create`. Do not add catalog tools. Path-limited `git add` of `.github/workflows/ci.yml` only (plus planning docs the executor owns). Vitest 5: do not pass `-x`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-ci-green-on-main/14-CONTEXT.md
@.planning/phases/14-ci-green-on-main/14-RESEARCH.md
@.planning/phases/14-ci-green-on-main/14-VALIDATION.md
@.planning/phases/14-ci-green-on-main/COVERAGE.md
@package.json
@astro.config.mjs
@src/data/site.ts

<interfaces>
Workflow to write at `.github/workflows/ci.yml` (D-Triggers, D-Steps). User override 2026-09-22 pins Node 22, not Node 20. RESEARCH.md's Node 20 sample is superseded. Do not copy `node-version: 20` from that sample.

name: CI
on.push.branches: [main]
on.pull_request.branches: [main]
on.workflow_dispatch: present, no inputs
permissions.contents: read
jobs.ci.runs-on: ubuntu-latest
steps, in order:
  uses actions/checkout@v4
  uses actions/setup-node@v4 with node-version 22 and cache npm
  run npm ci
  run npm test
  run npm run build

package.json scripts (do not edit): test is `vitest run`; build is `astro build`. Proof commands are `npm test` then `npm run build`. Do not pass `-x` (Vitest 5 rejects it).

Git allowlist for this plan's product commit: `.github/workflows/ci.yml` only. Never stage `src/lib/crontab.ts`, `src/components/ToolShell.tsx`, `src/data/site.ts`, `astro.config.mjs`, or `package.json`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Write Node 22 CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/14-ci-green-on-main/14-CONTEXT.md (D-Triggers, D-Steps, D-Proof)
    - .planning/REQUIREMENTS.md (CI-01 is Node 22 after the 2026-09-22 override)
    - package.json scripts block (test and build names only; do not edit)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` per D-Triggers and D-Steps. This is the whole CI-01 slice: the file is the artifact, and a remote run is out of scope.

    Write exactly this shape. Name the workflow `CI`. Triggers are push to main, pull_request to main, and workflow_dispatch with no inputs. Top-level `permissions: contents: read`. One job id `ci` on `ubuntu-latest`. Steps in order: `actions/checkout@v4`, `actions/setup-node@v4` with `node-version: 22` and `cache: npm`, then `run: npm ci`, `run: npm test`, `run: npm run build`.

    Node 22 is the locked pin (user override 2026-09-22; CI-01; D-Triggers). Astro 7.3.2 engines.node is `>=22.12.0`. Do not write `node-version: 20`. RESEARCH.md still shows 20 because it was written before the override; ignore that sample.

    Do not add lint, `astro check`, extra jobs, secrets, env, a deploy step, or any `gh` invocation (D-Triggers, D-Steps). Do not edit package.json, the lockfile, astro.config.mjs, or src/data/site.ts. Do not stage `src/lib/crontab.ts`. Do not pop `stash@{0}` or `stash@{1}`.
  </action>
  <verify>
    <automated>rg -N "node-version: 22" .github/workflows/ci.yml; rg -N "npm ci" .github/workflows/ci.yml; rg -N "npm test" .github/workflows/ci.yml; rg -N "npm run build" .github/workflows/ci.yml; rg -N "workflow_dispatch" .github/workflows/ci.yml; rg -N "contents: read" .github/workflows/ci.yml</automated>
    <fails_when>any rg exits non-zero, or the file contains node-version: 20, or the file contains an astro check step, or the file contains a gh invocation</fails_when>
  </verify>
  <done>`.github/workflows/ci.yml` exists with name CI, Node 22, npm ci, npm test, npm run build, and the locked triggers (CI-01, D-Triggers, D-Steps).</done>
</task>

<task type="auto">
  <name>Prove npm test and astro build without staging crontab</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/14-ci-green-on-main/14-CONTEXT.md (D-Proof fences)
    - package.json (scripts.test is vitest run; scripts.build is astro build)
  </read_first>
  <action>
    Prove CI-02 on the current working tree per D-Proof. Do not create a worktree, do not pop a stash, and do not stage any product file.

    From the repo root, run `npm test` then `npm run build`. Both must exit 0. Do not pass `-x` or `--bail` to Vitest. Blog glob warnings during the build are not a failure if the process exits 0. Do not add blog posts, do not edit islands, and do not edit `src/lib/crontab.ts` to chase a warning.

    After both commands, confirm `git status --short -- src/lib/crontab.ts` still shows the file modified (untracked-or-modified marker, not a clean path). Confirm `git diff --cached --name-only` does not list `src/lib/crontab.ts`. Confirm `git stash list` still contains both `stash@{0}` and `stash@{1}`. Confirm `src/data/site.ts` still has `SITE_ORIGIN` of `https://example.com`.

    If a later commit of this plan is requested, path-limited `git add .github/workflows/ci.yml` only. Never `git add -A`. Never `git add src/lib/crontab.ts`. Never `git add src/components/ToolShell.tsx`. Do not change `SITE_ORIGIN`. Do not run `gh repo create`.
  </action>
  <verify>
    <automated>npm test && npm run build && rg "src/lib/crontab.ts" &lt;(git status --short -- src/lib/crontab.ts) && rg "stash@\{0\}" &lt;(git stash list) && rg "stash@\{1\}" &lt;(git stash list)</automated>
    <fails_when>npm test or npm run build exits non-zero, or git status/stash list fails inside the process substitution, or crontab.ts is absent from git status --short, or either stash entry is missing</fails_when>
  </verify>
  <done>npm test and npm run build exit 0 on the dirty tree; src/lib/crontab.ts is still unstaged; both stashes remain; SITE_ORIGIN is unchanged (CI-02, D-Proof).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow file → future runner | Committed YAML is the only new executable surface. No remote exists this phase, so the runner is not invoked here. |
| working tree → git index | Dirty `src/lib/crontab.ts` must not cross into the index (T-14-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | git index / src/lib/crontab.ts | high | mitigate | Path-limited add of `.github/workflows/ci.yml` only. Proof command checks `git status --short -- src/lib/crontab.ts` still lists the file. Do not `git add -A`. Do not stage crontab.ts. |
| T-14-02 | Information Disclosure | .github/workflows/ci.yml | medium | mitigate | No secrets, no env, no curl, no third-party upload. Steps are `npm ci`, `npm test`, `npm run build` only (D-Steps). |
| T-14-03 | Elevation of Privilege | GITHUB_TOKEN scope | medium | mitigate | Top-level `permissions: contents: read`. No `contents: write`. No deploy job. |
| T-14-04 | Tampering | pull_request checkout | low | accept | Event is `pull_request`, not `pull_request_target`. No remote this phase, so the event cannot fire. No push step is added. |
| T-14-05 | Tampering | npm ci vs lockfile | low | accept | Step is `npm ci`, not `npm install`. Lockfile is not edited. No new packages. |
</threat_model>

<verification>
CI-01: `.github/workflows/ci.yml` contains `node-version: 22`, `npm ci`, `npm test`, `npm run build`, `workflow_dispatch`, and `contents: read`.
CI-02: `npm test` then `npm run build` exit 0 while `src/lib/crontab.ts` stays unstaged and both stashes remain.
Do not treat blog glob warnings as failure when the build exits 0.
</verification>

<success_criteria>
- Workflow file matches D-Triggers and D-Steps with Node 22 (CI-01).
- Local `npm test` and `npm run build` pass without overlay isolation (CI-02, D-Proof).
- `src/lib/crontab.ts` unstaged; stashes unpopped; SITE_ORIGIN unchanged; no remote created.
</success_criteria>

<output>
Create `.planning/phases/14-ci-green-on-main/14-01-SUMMARY.md` when done
</output>
