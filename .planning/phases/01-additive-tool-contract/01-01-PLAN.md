---
phase: 01-additive-tool-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/tools.test.ts
  - src/components/tools/ToolIsland.test.ts
  - .planning/codebase/CONVENTIONS.md
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
  - CAT-05
  - CAT-06

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npm test fails when a TOOLS slug has no ToolIsland slug-equals branch, instead of a blank island shipping"
    - "npm test fails when EN or ZH markdown is missing for any catalog slug"
    - "Catalog snapshot stays at 10 tools this phase; getFeaturedTools length stays 6"
    - "Category grouping counts TOOLS.length so later slices update one snapshot only"
    - "Existing ten tools are untouched; this plan only changes tests plus CONVENTIONS.md"
    - "CONVENTIONS.md lists the 8-file add-a-tool checklist and the island-split / no-lib-barrel rule"
  artifacts:
    - path: src/data/tools.test.ts
      provides: "Catalog snapshot, unique slugs, featured === 6, grouping via TOOLS.length, EN+ZH markdown existsSync loop"
    - path: src/components/tools/ToolIsland.test.ts
      provides: "Source-read coverage: every TOOLS slug has a slug-equals branch"
    - path: .planning/codebase/CONVENTIONS.md
      provides: "8-file checklist (CAT-06) and island-split rule (CAT-04)"
  key_links:
    - from: src/data/tools.test.ts
      to: src/content/tools/{slug}.md and src/content/tools/zh/{slug}.md
      via: "existsSync(new URL(..., import.meta.url)) looped over TOOLS"
      pattern: "existsSync\\("
    - from: src/components/tools/ToolIsland.test.ts
      to: src/components/tools/ToolIsland.astro
      via: "readFileSync URL + includes slug-equals substring per TOOLS slug"
      pattern: "slug === '\\$\\{slug\\}'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts TOOLS
      via: "grouping reduce count equals TOOLS.length; snapshot toHaveLength(10)"
      pattern: "TOOLS\\.length"
---

## Phase Goal

**As a** catalog maintainer, **I want to** have CI fail when a catalog slug lacks a ToolIsland branch or EN/ZH markdown, **so that** a missing island cannot ship as a blank panel, featured cards stay at six, and the existing ten tools keep working unchanged.

<objective>
Lock the additive tool contract in CI without shipping any of the eight new tools (Completeness harness, Catalog tests, Island split, Phase 1 delivery boundary).

Purpose: Later slices add tools by satisfying tests + CONVENTIONS instead of rewriting the existing ten. CAT-01 through CAT-06 are harness + rules that go green as the catalog grows.

Output: Extended `src/data/tools.test.ts`, new `src/components/tools/ToolIsland.test.ts`, appended 8-file checklist and island-split section in `.planning/codebase/CONVENTIONS.md`. Catalog remains 10 rows. No product-code edits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-additive-tool-contract/01-CONTEXT.md
@.planning/phases/01-additive-tool-contract/01-RESEARCH.md
@.planning/phases/01-additive-tool-contract/01-PATTERNS.md
@src/data/tools.test.ts
@src/data/tools.ts
@src/components/tools/ToolIsland.astro
@.planning/codebase/CONVENTIONS.md
@vitest.config.ts
@package.json

<interfaces>
From src/data/tools.ts (read-only this phase):
- export type ToolCategory = 'Format' | 'Auth' | 'Encode' | 'Generate' | 'Text' | 'Time' | 'Color'
- export interface Tool { slug: string; name: string; category: ToolCategory; shortDescription: string; relatedSlugs: string[]; featured: boolean }
- export const TOOLS: Tool[]
- export function getTool(slug: string): Tool | undefined
- export function getFeaturedTools(): Tool[]
- export function getToolsByCategory(): { category: ToolCategory; tools: Tool[] }[]
- export function getRelatedTools(slug: string): Tool[]

From src/data/tools.test.ts today (keep all of these cases):
- it('has exactly 10 tools') expect(TOOLS).toHaveLength(10)
- unique slugs via Set size
- getFeaturedTools() toHaveLength(6) and arrayContaining json-formatter, jwt-decoder
- getRelatedTools('json-formatter') excludes self and length > 0
- getTool('nope') is undefined
- groups by category: reduce tools.length currently toBe(10) — change only this one to TOOLS.length

From src/components/tools/ToolIsland.astro (read-only): each tool is a static import plus a line of the form slug === 'json-formatter' (single quotes, kebab slug) with client:load.

From vitest.config.ts (do not change): include src/**/*.test.ts, environment node, passWithNoTests true. New coverage file MUST be ToolIsland.test.ts not tsx.

From package.json: script test is vitest run. No new packages.

Diff allowlist only: src/data/tools.test.ts, src/components/tools/ToolIsland.test.ts, .planning/codebase/CONVENTIONS.md.
Do not edit src/data/tools.ts, ToolIsland.astro, islands, pages, src/lib/limits.ts, relatedSlugs, i18n, or content markdown.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end catalog completeness — island branch plus EN/ZH markdown for every TOOLS slug</name>
  <files>src/components/tools/ToolIsland.test.ts, src/data/tools.test.ts</files>
  <read_first>src/data/tools.test.ts, src/components/tools/ToolIsland.astro, src/data/tools.ts, vitest.config.ts, .planning/phases/01-additive-tool-contract/01-PATTERNS.md</read_first>
  <action>
Create the CI path that fails a missing ToolIsland branch or missing locale markdown (Completeness harness, CAT-02, CAT-03). Do not edit ToolIsland.astro or tools.ts. Do not insert the eight future catalog slugs. Do not add Playwright, jsdom, or packages.

1. Create src/components/tools/ToolIsland.test.ts (sibling of ToolIsland.astro; filename must match src/**/*.test.ts). Import readFileSync from node:fs, describe/expect/it from vitest, and TOOLS from ../../data/tools. In a describe named ToolIsland coverage, read ToolIsland.astro with readFileSync(new URL('./ToolIsland.astro', import.meta.url), 'utf8') — pass that URL object into readFileSync, never take .pathname. Loop TOOLS (not a copied slug array). For each slug, expect source.includes of a template slug-equals check with a single-quoted kebab slug (the production if-chain shape). Use includes only; do not parse with a regular expression; do not assert import lines; do not import Preact islands; do not compile the astro file. Add one short comment that every TOOLS slug needs a ToolIsland branch and that the 8-file checklist lives in .planning/codebase/CONVENTIONS.md.

2. In src/data/tools.test.ts, add existsSync from node:fs above the vitest import (runtime then vitest then local ./tools, matching PATTERNS). Keep every existing it() including has exactly 10 tools, unique slugs, featured six, related, getTool nope, and grouping. Add one it() named has EN and ZH markdown for every catalog slug: loop TOOLS, build en as new URL(`../content/tools/${slug}.md`, import.meta.url) and zh as new URL(`../content/tools/zh/${slug}.md`, import.meta.url), then expect existsSync(en) and existsSync(zh) with messages missing EN markdown for ${slug} / missing ZH markdown for ${slug}. Pass the URL objects to existsSync. If existsSync rejects a URL in this Node/Vitest pair, convert with fileURLToPath from node:url and pass that string — still do not use URL.pathname. Loop TOOLS, never a hardcoded slug list. Add one short comment that this completeness loop is the 8-file contract in .planning/codebase/CONVENTIONS.md.

Do not change grouping, snapshot length, or featured asserts in this task. Do not mock node:fs. Do not create src/lib/index.ts. Do not touch product files outside the allowlist. If other files are already dirty in the working tree, leave them alone.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, Vitest reports failed tests, output lacks "ToolIsland coverage", or output lacks "has EN and ZH markdown for every catalog slug"</fails_when>
  </verify>
  <done>npm test is green for the current ten tools; ToolIsland.test.ts exists and loops TOOLS with includes of the slug-equals substring; tools.test.ts asserts EN+ZH markdown via existsSync(URL) per catalog slug; product catalog and islands unchanged.</done>
  <acceptance_criteria>
    - src/components/tools/ToolIsland.test.ts exists and is included by vitest (not a tsx file)
    - Coverage loops TOOLS from src/data/tools and uses readFileSync with a URL from import.meta.url
    - Each slug is checked with includes of the slug-equals single-quoted form, not a regular expression and not import-line matching
    - tools.test.ts adds EN and ZH existsSync(URL) for every TOOLS slug; snapshot still toHaveLength(10)
    - git changes from this task stay on the two test files only
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Lock catalog snapshot, featured six, and grouping via TOOLS.length</name>
  <files>src/data/tools.test.ts</files>
  <read_first>src/data/tools.test.ts, src/data/tools.ts</read_first>
  <action>
Finish CAT-01 catalog invariants in src/data/tools.test.ts only (Catalog tests, Phase 1 delivery boundary). Keep the length snapshot: it('has exactly 10 tools') must still expect(TOOLS).toHaveLength(10) — later phases bump this number when they add rows; this phase must not grow the catalog.

Change only the grouping assertion: getToolsByCategory reduce of tools.length must equal TOOLS.length (not a second hardcoded 10). Keep unique slugs, getFeaturedTools toHaveLength(6) with json-formatter and jwt-decoder in the featured set, related tools for json-formatter, and getTool('nope') undefined.

Do not add featured-false asserts against tools that are not in the catalog yet; document that rule in task 3. Do not edit src/data/tools.ts, relatedSlugs, or featured flags. Do not add markdown-preview or the other locked-future slugs to tests or TOOLS.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, Vitest reports failed tests, or the grouping test still compares the reduced count to a numeric 10 instead of TOOLS.length</fails_when>
  </verify>
  <done>Grouping follows TOOLS.length; snapshot remains 10; featured remains 6; unique slugs and existing getter tests still pass; tools.ts untouched.</done>
  <acceptance_criteria>
    - expect(TOOLS).toHaveLength(10) still present
    - grouping count uses TOOLS.length
    - getFeaturedTools() still toHaveLength(6) and still requires json-formatter and jwt-decoder
    - unique slugs, related, and getTool('nope') cases remain
    - src/data/tools.ts not modified
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Append 8-file checklist and island-split rule to CONVENTIONS.md</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <read_first>.planning/codebase/CONVENTIONS.md, .planning/phases/01-additive-tool-contract/01-PATTERNS.md</read_first>
  <reversibility rating="costly">Later phases treat this checklist and island-split text as the add-a-tool contract; changing it later is a coordinated doc plus test update, not a data migration.</reversibility>
  <action>
Append CAT-04 and CAT-06 rules to .planning/codebase/CONVENTIONS.md (Island split, Phase 1 delivery boundary). Do not rewrite Naming, Style, or other existing sections. Insert the new sections after Module Design / Barrel Files and before the footer line Convention analysis: 2026-09-10.

Add heading Adding a tool (8-file checklist). State: one slice per tool; do not land a catalog row without the rest; clone JsonFormatter.tsx plus src/lib/json.ts, not UuidGenerator.tsx (that island has no lib). Then the eight items exactly: (1) catalog row in src/data/tools.ts with unique kebab slug, existing ToolCategory, relatedSlugs, featured false; (2) src/lib topic module plus colocated test with result union and English errors; (3) Preact island default export with locale, ToolShell, isTooLarge; (4) ToolIsland.astro static import plus slug-equals client:load branch; (5) EN and ZH entries in src/i18n/ui.ts tools[slug] (no new category this milestone); (6) every new English lib error added to ZH_ERRORS in src/i18n/errors.ts in the same slice; (7) EN markdown src/content/tools/{slug}.md with locale en, howTo length 3, faq 3-5; (8) ZH markdown src/content/tools/zh/{slug}.md with locale zh and the same schema. State that the tools.test.ts currently-N-tools snapshot updates when the catalog grows and that featured count stays 6.

Add heading Island split (heavy libraries) with: do not add src/lib/index.ts or any src/lib barrel; import the concrete file; heavy deps (SQL, Markdown, QR, Diff) import only from that tool’s src/lib or island; light tools stay static imports in ToolIsland.astro; keep ToolIsland.astro static imports plus client:load because Astro forbids client directives on dynamic tags; do not convert the existing ten static imports to dynamic import(); SQL / Markdown / QR will dynamic-import the fat library inside their island in Phases 3, 5, 6, not at ToolIsland.astro top; from the first heavy lib (Phase 3), inspect dist/_astro/ so json-formatter does not inherit those chunks.

Do not install heavy libraries. Do not add dist/_astro regression tests (that is Phase 3). Do not add INPUT byte caps. Do not create empty shells for the eight tools. Do not edit product code. Stage and commit only the three allowlisted paths if committing.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, Vitest reports failed tests, or CONVENTIONS.md lacks both headings Adding a tool (8-file checklist) and Island split (heavy libraries)</fails_when>
    <human-check>Confirm this plan’s diff is only src/data/tools.test.ts, src/components/tools/ToolIsland.test.ts, and .planning/codebase/CONVENTIONS.md — no edits to tools.ts, ToolIsland.astro, islands, pages, limits, i18n, or content markdown.</human-check>
  </verify>
  <done>CONVENTIONS.md contains the eight checklist items and island-split / no-barrel / static ToolIsland rules; npm test still green; no product files changed by this plan.</done>
  <acceptance_criteria>
    - New sections sit before the Convention analysis footer and do not replace Naming or Style
    - All eight checklist items are present including featured false, ZH_ERRORS, EN md, ZH md
    - Island-split forbids a src/lib barrel, keeps static ToolIsland imports, and defers chunk inspection to Phase 3 in documentation only
    - Clone target is JsonFormatter plus src/lib/json.ts
    - npm test remains green with catalog length 10
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest process → repo filesystem | Completeness tests read ToolIsland.astro and content markdown via node:fs; paths must stay inside those trees |
| Catalog TOOLS → SSG pages | Unmatched ToolIsland branches render a blank panel with no build error (integrity of public pages) |
| Future islands → client bundles | A src/lib barrel or top-level heavy import would pull SQL/Markdown/QR/Diff onto unrelated tool pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | ToolIsland.astro unmatched slug | high | mitigate | ToolIsland.test.ts source-read: every TOOLS slug must include a slug-equals branch so CI fails before a blank panel ships (CAT-03) |
| T-01-02 | Denial of service | Missing EN/ZH markdown | medium | mitigate | tools.test.ts existsSync(URL) per TOOLS slug; pages already throw at astro build (CAT-02) |
| T-01-03 | Information disclosure | Future src/lib barrel pulling heavy deps onto every island | high | mitigate | CONVENTIONS island-split forbids src/lib/index.ts; no barrel created this phase (CAT-04) |
| T-01-04 | Elevation of privilege | Completeness tests that fake the filesystem | medium | mitigate | Use real node:fs only; do not introduce memfs or vi.mock of fs |
| T-01-05 | Tampering | Accidental rewrite of the existing ten tools | high | mitigate | Diff allowlist: tests + CONVENTIONS.md only; CAT-05 proven by not touching product files and keeping existing Vitest green |
| T-01-06 | Spoofing | Public static site, no auth in this phase | low | accept | No new identity surface; catalog data is trusted static source |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager installs this phase; legitimacy gate not applicable |
</threat_model>

<verification>
Run `npm test` after every task (Vitest run, Node environment, no watch). Suite must stay green on the current ten tools. Confirm ToolIsland coverage and markdown existence tests appear in the output. Confirm grouping uses TOOLS.length while toHaveLength(10) remains. Confirm CONVENTIONS.md gained the 8-file checklist and island-split sections. Do not require astro build. Do not add Playwright. Do not revert unrelated dirty files; do not stage them in this plan’s commit.
</verification>

<success_criteria>
- Completeness harness is enforceable: a catalog slug without a ToolIsland slug-equals branch or without EN+ZH markdown fails `npm test` (CAT-02, CAT-03)
- Unique slugs, featured length 6, catalog snapshot 10, grouping TOOLS.length (CAT-01)
- Existing ten tools and relatedSlugs untouched; existing Vitest green (CAT-05)
- 8-file checklist (live compute, copy, size guard, EN+ZH, ZH_ERRORS) and island-split / no-barrel rule documented in CONVENTIONS.md (CAT-04, CAT-06)
- No new catalog rows, no heavy libraries, no product-file edits
</success_criteria>

<output>
Create `.planning/phases/01-additive-tool-contract/01-01-SUMMARY.md` when done
</output>
