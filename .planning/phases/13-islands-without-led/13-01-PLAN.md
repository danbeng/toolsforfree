---
phase: 13-islands-without-led
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ToolShell.tsx
  - src/components/tools/ToolIsland.astro
  - src/components/tools/JsonFormatter.tsx
  - src/components/tools/WordCounter.tsx
  - src/components/tools/JwtDecoder.tsx
  - src/components/tools/Base64Tool.tsx
  - src/components/tools/UrlEncode.tsx
  - src/components/tools/HashGenerator.tsx
  - src/components/tools/UuidGenerator.tsx
  - src/components/tools/RegexTester.tsx
  - src/components/tools/UnixTimestamp.tsx
  - src/components/tools/CrontabExplainer.tsx
  - src/components/tools/ColorConverter.tsx
  - src/components/tools/CaseConverter.tsx
  - src/components/tools/LoremIpsum.tsx
  - src/components/tools/PasswordGenerator.tsx
  - src/components/tools/SqlFormatter.tsx
  - src/components/tools/TextDiff.tsx
  - src/components/tools/MarkdownPreview.tsx
  - src/components/tools/QrCode.tsx
autonomous: true
requirements:
  - ISLE-01
  - ISLE-02
  - ISLE-03
  - ISLE-04

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ToolShell accepts required locale and localizes Copy/Copied via t(locale) copy.copy / copy.copied; committed chrome has no LED panel classes (ISLE-01, D-Shell, T-13-01)"
    - "ToolIsland locale is required Locale from locales.ts with no optional marker and no en fallback; all 18 client:load islands receive locale={locale} (ISLE-02, D-Island)"
    - "JsonFormatter (original ten analog) takes required locale, uses useToolUi, and wraps no-LED ToolShell with locale={locale} (ISLE-03, D-Ten)"
    - "WordCounter (later-eight analog) keeps t(locale) and passes locale={locale} on ToolShell (ISLE-01, D-Eight)"
    - "The remaining original ten islands land dirty useToolUi (UuidGenerator may keep t(locale)) and wrap no-LED ToolShell with locale={locale} (ISLE-03, D-Ten)"
    - "All eight later-eight ToolShell call sites pass locale={locale}: WordCounter, CaseConverter, LoremIpsum, PasswordGenerator, SqlFormatter, TextDiff, MarkdownPreview, QrCode (D-Eight)"
    - "Completeness tests stay green and unchanged: every catalog slug has EN+ZH markdown and a ToolIsland slug === branch (ISLE-04)"
    - "Do not commit src/lib/crontab.ts"
    - "Do not pop stash@{0} or stash@{1}"
    - "Path-limited git add only — never git add -A"
    - "No SITE_ORIGIN change, no gh repo create, no new catalog tools, no Tailwind, no new npm packages"
    - "Do not edit src/styles/global.css"
    - "Overlay-free astro build is Phase 14; do not treat a dirty-tree astro build error as this phase failing"
    - statement: "In both themes, idle Copy accent-on-panel, hover --bg-on-accent, active darkened-accent with --bg text, and .tool-error --danger on --panel eyeball at 4.5:1; disabled Copy stays opacity 0.45 with no hover invert"
      verification: backstop
  artifacts:
    - path: src/components/ToolShell.tsx
      provides: "HEAD layout children then error then pre then Copy; required locale; t(locale) copy.copy / copy.copied (ISLE-01, D-Shell)"
      min_lines: 28
    - path: src/components/tools/ToolIsland.astro
      provides: "Required locale: Locale; locale={locale} on all 18 islands (ISLE-02, D-Island)"
      min_lines: 40
    - path: src/components/tools/JsonFormatter.tsx
      provides: "Original-ten analog: useToolUi plus ToolShell locale={locale} (ISLE-03, D-Ten)"
      min_lines: 20
    - path: src/components/tools/WordCounter.tsx
      provides: "Later-eight analog: t(locale) plus ToolShell locale={locale} (D-Eight)"
      min_lines: 30
    - path: src/data/tools.test.ts
      provides: "Unchanged EN+ZH markdown completeness loop (ISLE-04)"
      min_lines: 50
    - path: src/components/tools/ToolIsland.test.ts
      provides: "Unchanged slug === completeness loop (ISLE-04)"
      min_lines: 16
  key_links:
    - from: src/components/tools/ToolIsland.astro
      to: src/i18n/locales.ts
      via: "import type Locale from locales.ts; required Props locale"
      pattern: "from '../../i18n/locales'"
    - from: src/components/ToolShell.tsx
      to: src/i18n/ui.ts
      via: "t(props.locale) then copy.copy / copy.copied"
      pattern: "from '../i18n/ui'"
    - from: src/components/tools/JsonFormatter.tsx
      to: src/i18n/useToolUi.ts
      via: "useToolUi(locale) for copy / tooLarge / err"
      pattern: "useToolUi"
    - from: src/pages/tools/[slug].astro
      to: src/components/tools/ToolIsland.astro
      via: "Phase 12 already passes locale={locale} into ToolIsland"
      pattern: "locale={locale}"
  prohibitions:
    - "Do not commit LED ToolShell chrome"
    - "Do not commit src/lib/crontab.ts"
    - "Do not pop stash@{0} or stash@{1}"
    - "Path-limited git add only — never git add -A"
    - "No SITE_ORIGIN change, no gh repo create, no new catalog tools, no Tailwind, no new npm packages"
    - "Do not edit src/styles/global.css"
    - "Do not rewrite original ten from HEAD English-only islands"
    - "Do not force useToolUi on later eight"
    - "Do not edit completeness tests"
    - "Do not change src/lib parsers"
    - "Overlay-free astro build is Phase 14"
---

<objective>
Land locale Copy/Copied on the no-LED ToolShell and wire locale through ToolIsland into all 18 tool islands (ISLE-01, ISLE-02, ISLE-03, ISLE-04, D-Shell, D-Island, D-Ten, D-Eight).

Purpose: Visitors on EN and ZH tool pages get locale-correct Copy/Copied and island copy without shipping overlay LED chrome, unblocking Phase 14 overlay-free astro build.

Output: Rewritten ToolShell (HEAD layout + required locale), ToolIsland required locale on all 18 islands, path-limited land of original ten, locale={locale} on all eight later-eight ToolShell call sites.

Hard fences: Do not commit LED ToolShell chrome. Do not commit src/lib/crontab.ts. Do not pop stash@{0} or stash@{1}. Path-limited git add only — never git add -A. No SITE_ORIGIN change, no new catalog tools, no Tailwind, no new npm packages. Do not edit src/styles/global.css. Completeness tests unchanged. Overlay-free astro build is Phase 14.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-islands-without-led/13-CONTEXT.md
@.planning/phases/13-islands-without-led/13-RESEARCH.md
@.planning/phases/13-islands-without-led/13-UI-SPEC.md
@.planning/phases/13-islands-without-led/13-VALIDATION.md
@.planning/phases/13-islands-without-led/COVERAGE.md
@.planning/phases/11-i18n-kernel/11-01-SUMMARY.md
@src/components/ToolShell.tsx
@src/components/tools/ToolIsland.astro
@src/components/tools/JsonFormatter.tsx
@src/components/tools/WordCounter.tsx
@src/components/tools/UuidGenerator.tsx
@src/i18n/locales.ts
@src/i18n/ui.ts
@src/i18n/useToolUi.ts
@src/data/tools.test.ts
@src/components/tools/ToolIsland.test.ts
@src/pages/tools/[slug].astro
@src/pages/zh/tools/[slug].astro

<assumption_delta_decision>
noun: locale
decision: no-change
rationale: Phase 11 already shipped Locale, t(locale) copy/copied, and useToolUi. Phase 12 already passes locale into ToolIsland from EN and ZH slug pages. This phase consumes that kernel on islands and ToolShell; do not promote locale into a new module.
</assumption_delta_decision>

<interfaces>
From git show HEAD:src/components/ToolShell.tsx: named export ToolShell; props error string|null, output string, children; layout is div.tool-panel then children then optional p.tool-error role=alert then pre.tool-output wrapping code then button type=button onClick=onCopy disabled when output is empty; Copy labels are hardcoded English Copied/Copy; onCopy returns when output is empty, clipboard.writeText, setCopied true, 1500ms timeout. Preact class not className. No locale prop.

From dirty working-tree src/components/ToolShell.tsx: already has required locale and t(locale) labels, but also overlay chrome nodes that MUST NOT be committed (D-Shell, T-13-01). Never git add this file as-is. Rewrite from HEAD layout plus locale.

Landed ToolShell contract (13-UI-SPEC Markup, D-Shell): import useState from preact/hooks, ComponentChildren from preact, type Locale from ../i18n/locales (not ui.ts), t from ../i18n/ui. Props add required locale: Locale. const copy = t(props.locale). Button text is copied ? copy.copied : copy.copy. Same DOM order as HEAD. Unclassed button. No new CSS class on the button.

From src/i18n/ui.ts: t(locale) returns UiDict; top-level keys copy and copied (EN Copy/Copied, ZH 复制/已复制). There is no chromeLocal key and no output chrome key. Do not invent those keys.

From src/i18n/locales.ts: export type Locale = (typeof LOCALES)[number]; LOCALES is en then zh.

From src/i18n/useToolUi.ts: named export useToolUi(locale) returns { copy, tooLarge, err }. err maps empty/null to null then localizeError.

From src/components/tools/ToolIsland.astro (current): interface Props { slug: string; locale?: 'en' | 'zh' }; const { slug, locale = 'en' } = Astro.props. Original ten client:load islands omit locale. Later eight already pass locale={locale}. Completeness test greps slug === '${slug}'.

Landed ToolIsland (D-Island): import type Locale from ../../i18n/locales. interface Props { slug: string; locale: Locale }. const { slug, locale } = Astro.props. Every one of the 18 islands gets locale={locale}. Keep the boolean slug === chain. Do not dynamic-import.

From dirty src/components/tools/JsonFormatter.tsx (D-Ten analog): export default function JsonFormatter({ locale }: { locale: Locale }); useToolUi(locale); ToolShell already has locale={locale}. Land as written; do not rewrite from HEAD English-only.

From src/components/tools/UuidGenerator.tsx: original ten but uses t(locale) equivalent; ToolShell already has locale={locale}. Keep t(locale); do not force useToolUi.

From src/components/tools/WordCounter.tsx (D-Eight analog): already { locale }: { locale: Locale } and t(locale); ToolShell opening tag currently omits locale. Add locale={locale} only.

Later eight that omit locale on ToolShell (all eight, not only WordCounter): WordCounter.tsx, CaseConverter.tsx, LoremIpsum.tsx, PasswordGenerator.tsx, SqlFormatter.tsx, TextDiff.tsx, MarkdownPreview.tsx, QrCode.tsx.

Phase 12 pages already pass locale into ToolIsland: src/pages/tools/[slug].astro and src/pages/zh/tools/[slug].astro. Do not re-edit those pages.

Git allowlist (union of all tasks): src/components/ToolShell.tsx, src/components/tools/ToolIsland.astro, original ten JsonFormatter JwtDecoder Base64Tool UrlEncode HashGenerator UuidGenerator RegexTester UnixTimestamp CrontabExplainer ColorConverter, later eight WordCounter CaseConverter LoremIpsum PasswordGenerator SqlFormatter TextDiff MarkdownPreview QrCode. Never stage src/lib/crontab.ts, src/styles/global.css, src/data/site.ts, Header, LangSwitch, FaqList, completeness test files.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end no-LED Copy/Copied — ToolShell, ToolIsland required locale, json-formatter, WordCounter</name>
  <reversibility rating="costly">Required locale on ToolShell and ToolIsland becomes the contract all 18 islands must pass; undoing means touching every island call site.</reversibility>
  <precondition>src/i18n/locales.ts, src/i18n/ui.ts, and src/i18n/useToolUi.ts exist; src/pages/tools/[slug].astro and src/pages/zh/tools/[slug].astro already pass locale into ToolIsland; git stash list still shows stash@{0} and stash@{1}</precondition>
  <files>src/components/ToolShell.tsx, src/components/tools/ToolIsland.astro, src/components/tools/JsonFormatter.tsx, src/components/tools/WordCounter.tsx</files>
  <read_first>.planning/phases/13-islands-without-led/13-CONTEXT.md, .planning/phases/13-islands-without-led/13-UI-SPEC.md, .planning/phases/13-islands-without-led/13-RESEARCH.md, .planning/phases/13-islands-without-led/13-VALIDATION.md, src/components/ToolShell.tsx, src/components/tools/ToolIsland.astro, src/components/tools/JsonFormatter.tsx, src/components/tools/WordCounter.tsx, src/i18n/locales.ts, src/i18n/ui.ts, src/i18n/useToolUi.ts, src/components/tools/ToolIsland.test.ts, src/data/tools.test.ts</read_first>
  <action>
    Tracer for ISLE-01, ISLE-02, one original-ten path, and one later-eight ToolShell locale (D-Shell, D-Island, D-Ten, D-Eight). Isolation none; sequential on dirty main.

    Rewrite src/components/ToolShell.tsx from HEAD layout plus required locale. Do not git add the dirty working-tree file as-is (T-13-01). Start from git show HEAD:src/components/ToolShell.tsx. Keep named export, Preact class, children then error p.tool-error role=alert then pre.tool-output wrapping code then unclassed button type=button. Add required locale: Locale imported from ../i18n/locales. Import t from ../i18n/ui. const copy = t(props.locale). Button label is copied ? copy.copied : copy.copy. Keep onCopy: empty output returns; clipboard.writeText; setCopied true; 1500ms timeout; disabled={!props.output}. Do not wrap children in an extra body div. Do not add a chrome strip, status pip, output wrap, or output bar. Do not add clipboard-failure UI. Do not add a copy-btn class, SVG, or new CSS. Do not edit src/styles/global.css.

    Edit src/components/tools/ToolIsland.astro (D-Island, ISLE-02). Import type Locale from ../../i18n/locales. Change Props to { slug: string; locale: Locale }. Drop locale? and the en default. const { slug, locale } = Astro.props. On this tracer, add locale={locale} at least on JsonFormatter and WordCounter client:load sites. Prefer adding locale={locale} to all 18 islands in this same edit so completeness branches stay one shape; if you only wire two now, task 2 and task 3 still require the remaining original ten plus remaining later eight.

    Path-limited add dirty src/components/tools/JsonFormatter.tsx as written (D-Ten). It already has { locale }: { locale: Locale }, useToolUi, and ToolShell locale={locale}. Do not rewrite it from HEAD English-only. Do not change src/lib/json.ts.

    Edit src/components/tools/WordCounter.tsx only on the ToolShell opening tag: add locale={locale} next to error and output (D-Eight). Keep t(locale) and INPUT_TOO_LARGE_MSG. Do not migrate WordCounter onto useToolUi.

    Do not edit completeness tests. Do not edit global.css, crontab.ts, Header, LangSwitch, FaqList, SITE_ORIGIN, or catalog TOOLS. Do not pop stash entries. Do not run overlay-free astro build (Phase 14). Stage only this task's four files with path-limited git add. After write, confirm ToolShell source has locale, t(, copy.copy, copy.copied, and tool-panel, and that git diff --cached --name-only lists only the four files.
  </action>
  <verify>
    <automated>npx vitest run src/data/tools.test.ts src/components/tools/ToolIsland.test.ts src/i18n/useToolUi.test.ts src/i18n/locales.test.ts</automated>
    <fails_when>non-zero exit, or a summary line with 0 passed, or tools.test / ToolIsland.test / useToolUi.test / locales.test not collected</fails_when>
    <human-check>Open /tools/json-formatter/ idle Copy then Copied after click; open /zh/tools/json-formatter/ idle 复制 then 已复制; no chrome strip above the panel children; WordCounter Copy also localizes. Contrast backstop: idle Copy accent-on-panel, hover --bg-on-accent, .tool-error --danger on --panel.</human-check>
  </verify>
  <acceptance_criteria>
    - ToolShell required locale plus t(locale) Copy/Copied and HEAD DOM order per ISLE-01, D-Shell
    - ToolIsland required Locale from locales.ts; JsonFormatter and WordCounter receive locale={locale} per ISLE-02, D-Island
    - JsonFormatter lands dirty useToolUi island wrapping ToolShell locale={locale} per ISLE-03, D-Ten
    - WordCounter ToolShell opening tag includes locale={locale} per D-Eight
    - Completeness tests still pass unchanged per ISLE-04
    - Cached names are only the four tracer files; crontab.ts and global.css are not staged (T-13-01)
  </acceptance_criteria>
  <done>One happy path works end-to-end: EN/ZH json-formatter hydrates with locale, no-LED ToolShell shows locale Copy/Copied, WordCounter ToolShell compiles with required locale. Slice committed via path-limited add of the four listed files only (ISLE-01, ISLE-02, D-Shell, D-Island, D-Ten, D-Eight).</done>
</task>

<task type="auto">
  <name>Land remaining original-ten islands with locale and no-LED ToolShell</name>
  <files>src/components/tools/JwtDecoder.tsx, src/components/tools/Base64Tool.tsx, src/components/tools/UrlEncode.tsx, src/components/tools/HashGenerator.tsx, src/components/tools/UuidGenerator.tsx, src/components/tools/RegexTester.tsx, src/components/tools/UnixTimestamp.tsx, src/components/tools/CrontabExplainer.tsx, src/components/tools/ColorConverter.tsx, src/components/tools/ToolIsland.astro</files>
  <read_first>src/components/tools/JsonFormatter.tsx, src/components/tools/JwtDecoder.tsx, src/components/tools/UuidGenerator.tsx, src/components/tools/ToolIsland.astro, .planning/phases/13-islands-without-led/13-CONTEXT.md, .planning/phases/13-islands-without-led/13-RESEARCH.md</read_first>
  <action>
    ISLE-03 / D-Ten. Path-limited add the remaining dirty original-ten islands as written. Do not rewrite from HEAD English-only. Do not change any src/lib parser, including crontab.ts (do not add crontab.ts).

    Files to land: JwtDecoder.tsx, Base64Tool.tsx, UrlEncode.tsx, HashGenerator.tsx, UuidGenerator.tsx, RegexTester.tsx, UnixTimestamp.tsx, CrontabExplainer.tsx, ColorConverter.tsx. Each already takes { locale }: { locale: Locale }. Nine of ten (not UuidGenerator) already call useToolUi. UuidGenerator may keep t(locale) as equivalent (D-Ten, D-Eight equivalent rule). Every ToolShell in these files must include locale={locale}; if any opening tag still omits it, add it.

    If ToolIsland.astro still omits locale={locale} on any original-ten client:load site other than JsonFormatter, add locale={locale} now (D-Island, ISLE-02). Keep every slug === '…' branch so ToolIsland.test.ts stays green. Do not collapse the if-chain into a map.

    Do not edit completeness tests. Do not edit global.css, Header, LangSwitch, FaqList, SITE_ORIGIN, or later-eight islands in this task. Do not pop stash entries. Stage only this task's files with path-limited git add. Never git add -A. Confirm git diff --cached --name-only does not list crontab.ts or global.css.
  </action>
  <verify>
    <automated>npx vitest run src/i18n/useToolUi.test.ts src/data/tools.test.ts src/components/tools/ToolIsland.test.ts</automated>
    <fails_when>non-zero exit, or a summary line with 0 passed, or useToolUi.test / tools.test / ToolIsland.test not collected</fails_when>
  </verify>
  <acceptance_criteria>
    - Remaining original ten islands land dirty with required locale per ISLE-03, D-Ten
    - UuidGenerator may keep t(locale); the other eight remaining originals keep useToolUi
    - ToolIsland original-ten branches pass locale={locale} per ISLE-02, D-Island
    - Completeness tests unchanged and green per ISLE-04
    - crontab.ts is not staged
  </acceptance_criteria>
  <done>Original ten islands use locale copy plus useToolUi or equivalent and wrap the no-LED ToolShell (ISLE-03, D-Ten). Completeness stays green (ISLE-04).</done>
</task>

<task type="auto">
  <name>Add locale={locale} on remaining later-eight ToolShell call sites</name>
  <files>src/components/tools/CaseConverter.tsx, src/components/tools/LoremIpsum.tsx, src/components/tools/PasswordGenerator.tsx, src/components/tools/SqlFormatter.tsx, src/components/tools/TextDiff.tsx, src/components/tools/MarkdownPreview.tsx, src/components/tools/QrCode.tsx, src/components/tools/ToolIsland.astro, src/components/tools/WordCounter.tsx</files>
  <read_first>src/components/tools/WordCounter.tsx, src/components/tools/CaseConverter.tsx, src/components/tools/LoremIpsum.tsx, src/components/tools/PasswordGenerator.tsx, src/components/tools/SqlFormatter.tsx, src/components/tools/TextDiff.tsx, src/components/tools/MarkdownPreview.tsx, src/components/tools/QrCode.tsx, src/components/tools/ToolIsland.astro, src/components/ToolShell.tsx, .planning/phases/13-islands-without-led/13-RESEARCH.md</read_first>
  <action>
    D-Eight / ISLE-01 call-site completeness. All eight later-eight islands already take locale as a prop and use t(locale) / localizeError. Do not migrate them onto useToolUi. Add locale={locale} on every ToolShell opening tag that still omits it.

    Patch CaseConverter.tsx, LoremIpsum.tsx, PasswordGenerator.tsx, SqlFormatter.tsx, TextDiff.tsx, MarkdownPreview.tsx, QrCode.tsx. If WordCounter.tsx still lacks locale on ToolShell (tracer miss), add it here too. Change only the ToolShell opening tag plus whatever is required for the prop to typecheck. Do not restyle islands. Do not touch src/lib.

    Confirm ToolIsland.astro later-eight branches still pass locale={locale} (they already do). If any of the 18 branches still omit it, add it. Count of locale={locale} in ToolIsland.astro must equal 18.

    After tests pass, run npm test. Stage only this task's island files (and ToolIsland.astro if edited) with path-limited git add. Never git add -A. Confirm git diff --cached --name-only does not list crontab.ts, global.css, or src/data/site.ts. git stash list still shows stash@{0} and stash@{1}. Overlay-free astro build remains Phase 14.

    Final ToolShell gate before commit: the file must still be the HEAD layout plus locale (children, error, pre, button). If the working tree drifted back to overlay chrome, rewrite it again from HEAD plus locale before staging (T-13-01).
  </action>
  <verify>
    <automated>npx vitest run src/data/tools.test.ts src/components/tools/ToolIsland.test.ts src/i18n/useToolUi.test.ts src/i18n/locales.test.ts</automated>
    <fails_when>non-zero exit, or a summary line with 0 passed, or tools.test / ToolIsland.test / useToolUi.test / locales.test not collected</fails_when>
  </verify>
  <acceptance_criteria>
    - All eight later-eight ToolShell call sites include locale={locale} per D-Eight (WordCounter, CaseConverter, LoremIpsum, PasswordGenerator, SqlFormatter, TextDiff, MarkdownPreview, QrCode)
    - Later eight keep t(locale) / localizeError; no useToolUi rewrite
    - ToolIsland has locale={locale} on all 18 islands per ISLE-02, D-Island
    - Completeness tests unchanged and green per ISLE-04
    - npm test green on the files above; fences held; T-13-01 ToolShell still HEAD+locale
  </acceptance_criteria>
  <done>Every ToolShell call site passes required locale; later eight keep t(locale); all 18 islands receive locale from ToolIsland (ISLE-01, ISLE-02, D-Eight). Completeness green (ISLE-04). Hard fences held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git staging → main | Broad add would land overlay ToolShell chrome and crontab.ts onto the public tree |
| Island locale prop → Copy/Copied labels | Missing locale on ZH pages would spoof EN chrome; default en on ToolIsland is the old failure mode |
| Tool output string → clipboard / pre | Untrusted tool text must stay in text nodes, not HTML |
| Visitor clipboard write | Copy must no-op when output is empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | src/components/ToolShell.tsx commit | high | mitigate | Rewrite from HEAD layout plus required locale; never git add the dirty overlay file as-is; path-limited add; after write assert overlay chrome tokens are absent; block-on high |
| T-13-02 | Spoofing | src/components/tools/ToolIsland.astro locale | medium | mitigate | Required locale: Locale from locales.ts; drop locale? and en default; pages already pass locale; all 18 islands get locale={locale} |
| T-13-03 | Tampering | git add of islands | high | mitigate | Path-limited add of the allowlist only; never git add -A; cached names must omit crontab.ts and global.css |
| T-13-04 | Information disclosure | ToolShell output / MarkdownPreview | low | accept | Keep output in pre/code text; do not introduce dangerouslySetInnerHTML in ToolShell; MarkdownPreview sanitized HTML stays island-local |
| T-13-05 | Tampering | ToolShell onCopy | low | mitigate | Keep HEAD if (!props.output) return and disabled={!props.output} |
| T-13-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager installs this phase; RESEARCH Package Legitimacy Audit table is empty / N/A |

ASVS L1: high-severity T-13-01 and T-13-03 are mitigated in the tracer and commit tasks. Block-on high is satisfied. T-13-SC accepted because this plan installs nothing.
</threat_model>

## Artifacts this phase produces

- `src/components/ToolShell.tsx` — HEAD layout plus required locale plus t(locale) Copy/Copied
- `src/components/tools/ToolIsland.astro` — required Locale; locale={locale} on all 18 islands
- Original-ten islands — dirty useToolUi (UuidGenerator t(locale)) landed path-limited
- Later-eight islands — ToolShell locale={locale} on all eight call sites
- Completeness tests — unchanged, still green

Symbols consumed, not redefined: `Locale`, `t(locale)`, `useToolUi`, `localizeError`.

## Flagged assumptions (EDGE_ABSENT)

Specless probes were unclassified or unresolved. Planner assumptions, not silent drops:

- ISLE-01 empty Copy: empty output disables Copy; no extra empty-state string in the pre (UI Considerations empty row).
- ISLE-01 loading: no fetch; Copied for 1500ms is the only success feedback; do not add aria-busy.
- ISLE-01 error vs output: islands pass output empty on parse failure so Copy disables; ToolShell does not XOR error vs output itself.
- ISLE-02 zero-one-many: always exactly one Copy button; 18 slug branches stay boolean slug ===, never a dynamic map.
- Contrast / invert: UI Considerations backstop — eyeball 4.5:1 idle/hover/active Copy and .tool-error; disabled stays opacity 0.45.
- Overlay-free astro build: Phase 14; dirty-tree build failure is not this phase failing.

Values/safety prohibitions are the hard fences in must_haves.prohibitions. Do not auto-dismiss.

<verification>
After all three tasks:

1. npx vitest run src/data/tools.test.ts src/components/tools/ToolIsland.test.ts src/i18n/useToolUi.test.ts src/i18n/locales.test.ts
2. npm test
3. Path-limited staging only. git diff --cached --name-only must not list src/lib/crontab.ts or src/styles/global.css.
4. git stash list still shows stash@{0} and stash@{1}.
5. src/data/site.ts still contains https://example.com. package.json dependencies were not added.
6. Overlay-free astro build is Phase 14. Do not treat a dirty-tree astro build error as this phase failing.
</verification>

<success_criteria>
- ISLE-01: ToolShell required locale, t(locale) Copy/Copied, HEAD DOM order, no overlay chrome (D-Shell, T-13-01)
- ISLE-02: ToolIsland required locale; all 18 islands get locale={locale} (D-Island)
- ISLE-03: Original ten land dirty useToolUi or UuidGenerator t(locale) and wrap no-LED ToolShell (D-Ten)
- ISLE-04: tools.test.ts and ToolIsland.test.ts unchanged and green
- D-Eight: all eight later-eight ToolShell call sites pass locale={locale}; later eight keep t(locale)
- Hard fences held
</success_criteria>

<output>
Create `.planning/phases/13-islands-without-led/13-01-SUMMARY.md` when done
</output>
