---
phase: 06-qr-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/qr.ts
  - src/lib/qr.test.ts
  - src/components/tools/QrCode.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/i18n/errors.ts
  - src/i18n/errors.test.ts
  - src/content/tools/qr-code.md
  - src/content/tools/zh/qr-code.md
  - src/styles/global.css
  - src/lib/limits.ts
autonomous: true
requirements:
  - QR-01
  - QR-02
  - QR-03
  - QR-04
  - QR-05
  - QR-06
  - QR-07

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can open /tools/qr-code/ (and /zh/tools/qr-code/ when that tree exists), type text or a URL, see a live black-on-white QR preview, choose ECC L/M/Q/H default M, and download PNG (QR-01, QR-02, QR-03)"
    - "Empty or whitespace-only generate text leaves .qr-preview blank with Download PNG disabled; no sample QR (QR-01 idle)"
    - "Visitor can select a local image file, decode the payload in the browser, and copy that string via ToolShell; generate never writes pixels or a data-URL into output (QR-04, QR-05)"
    - "Oversize image files are rejected with IMAGE_TOO_LARGE_MSG on file.size before bitmap, distinct from the text char cap; width or height over 4096 uses the same image-cap string (QR-06)"
    - "The tool never requests camera or getUserMedia; no qr/dom.js, capture, or video (QR-07)"
    - "Catalog slug qr-code, category Generate, featured false; TOOLS toHaveLength 18; getFeaturedTools stays 6; relatedSlugs uuid-generator, password-generator, hash-generator; existing ten relatedSlugs unchanged (CAT-01, CAT-05, CONTEXT catalog additive)"
    - "After astro build, dist/_astro/JsonFormatter*.js does not contain the seven minify-surviving qr identifiers; only src/lib/qr.ts imports qr and qr/decode.js (CAT-04)"
  artifacts:
    - path: src/lib/qr.ts
      provides: "encodeQr plus decodeQr; thin wrap of encodeQR from qr and decodeQR from qr/decode.js; idle trim empty error before encode; ECC letter map; never qr/dom.js"
      exports: ["encodeQr", "decodeQr"]
    - path: src/lib/qr.test.ts
      provides: "Node env; idle, ECC map, capacity, scale-2 round-trip, noise, isolation, camera-forbid, PNG/output source-read, FAQ lock"
    - path: src/lib/limits.ts
      provides: "IMAGE_MAX_BYTES 5242880 and IMAGE_TOO_LARGE_MSG appended; existing isTooLarge unchanged"
    - path: src/components/tools/QrCode.tsx
      provides: "Default-export island; stacked generate then decode; split generate controls then .qr-preview; canvas PNG download; file decode; ToolShell output is decoded string"
    - path: src/components/tools/ToolIsland.astro
      provides: "static QrCode import and slug === 'qr-code' client:load locale={locale}"
    - path: src/data/tools.ts
      provides: "qr-code catalog row featured false category Generate"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 18; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['qr-code'] UI-SPEC chrome keys only"
    - path: src/i18n/errors.ts
      provides: "ZH_ERRORS maps for image-cap, not-found, not-an-image, encode-fail; keep existing text-cap"
    - path: src/i18n/errors.test.ts
      provides: "qr-code chrome-key describe plus five English error ZH maps"
    - path: src/content/tools/qr-code.md
      provides: "locale en SEO howTo 3 faq 3-5 including local / nothing uploaded / selected image file / never camera"
    - path: src/content/tools/zh/qr-code.md
      provides: "locale zh SEO howTo 3 faq with local / no-upload / selected file / never camera"
    - path: src/styles/global.css
      provides: "additive .qr-preview pane chrome only"
    - path: package.json
      provides: "direct dependency qr 0.7.0 only; no jsqr, qrcode, qr-scanner, @zxing, canvas, extra jsdom"
  key_links:
    - from: src/components/tools/QrCode.tsx
      to: src/lib/qr.ts
      via: "encodeQr after isTooLarge and idle skip; decodeQr after byte cap, bitmap, and 4096 side cap"
      pattern: "encodeQr"
    - from: src/lib/qr.ts
      to: qr
      via: "default encodeQR from package root only in this file"
      pattern: "from 'qr'"
    - from: src/lib/qr.ts
      to: qr/decode.js
      via: "default decodeQR only in this file; never qr/dom.js"
      pattern: "from 'qr/decode.js'"
    - from: src/components/tools/QrCode.tsx
      to: src/lib/limits.ts
      via: "isTooLarge on generate text; IMAGE_MAX_BYTES on file.size"
      pattern: "IMAGE_MAX_BYTES"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/QrCode.tsx
      via: "static import and slug === 'qr-code' client:load"
      pattern: "slug === 'qr-code'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(18)"
      pattern: "toHaveLength\\(18\\)"
    - from: src/components/tools/QrCode.tsx
      to: src/components/ToolShell.tsx
      via: "error plus decoded-string copy payload; visual QR is children .qr-preview"
      pattern: "ToolShell"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** generate a downloadable QR from text or a URL and decode a selected image file in the browser, **so that** I can encode and read QR payloads without uploading anything or granting camera access.

<objective>
Ship the qr-code 8-file vertical slice at existing-tool parity (QR-01, QR-02, QR-03, QR-04, QR-05, QR-06, QR-07) plus CAT-01..06 kept green, including CAT-04 bundle isolation. Install official qr@0.7.0 only. Thin src/lib/qr.ts wrapping encodeQR from 'qr' and decodeQR from 'qr/decode.js'. Never qr/dom.js. Never jsqr, qrcode, qr-scanner, @zxing, canvas, or a second jsdom. Never a lib barrel. Island clones HEAD WordCounter locale/t(), HEAD SqlFormatter native select, HEAD PasswordGenerator button-in-panel, HEAD ToolShell with no locale prop — not dirty JsonFormatter (useToolUi) or dirty ToolShell. Catalog slug qr-code, Generate, featured false; TOOLS 17 → 18; featured stays 6; relatedSlugs uuid-generator, password-generator, hash-generator; do not rewrite existing ten relatedSlugs (CAT-05, CONTEXT catalog additive). Map image-cap, not-found, not-an-image, and encode-fail in ZH_ERRORS this slice; keep the existing text-cap map (CAT-06). Visual surface is children .qr-preview; ToolShell pre is copy-only decoded text (QR-05). CSS append .qr-preview only; never retokenize :root. After astro build, json-formatter chunk must not inherit the seven minify-surviving qr identifiers.
Purpose: Last heavy-library catalog tool; visitors generate PNG QR and file-decode in-browser with no camera and no upload.
Output: qr lib/test, QrCode island, ToolIsland branch, catalog row, ui.ts chrome, ZH_ERRORS maps, EN+ZH markdown, additive CSS, limits image cap, qr@0.7.0, CAT-04 build grep green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-qr-code/06-CONTEXT.md
@.planning/phases/06-qr-code/06-RESEARCH.md
@.planning/phases/06-qr-code/06-PATTERNS.md
@.planning/phases/06-qr-code/06-UI-SPEC.md
@.planning/phases/05-markdown-preview/05-01-SUMMARY.md
@.planning/phases/03-sql-formatter/03-01-SUMMARY.md
@.planning/phases/01-additive-tool-contract/01-01-SUMMARY.md
@src/lib/json.ts
@src/lib/sql.ts
@src/lib/sql.test.ts
@src/lib/markdown.test.ts
@src/lib/limits.ts
@src/components/tools/WordCounter.tsx
@src/components/tools/SqlFormatter.tsx
@src/components/tools/PasswordGenerator.tsx
@src/components/ToolShell.tsx
@src/components/tools/ToolIsland.astro
@src/components/tools/ToolIsland.test.ts
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/i18n/errors.ts
@src/i18n/errors.test.ts
@src/content.config.ts
@src/content/tools/markdown-preview.md
@src/styles/global.css
@.planning/codebase/CONVENTIONS.md

<interfaces>
From src/lib/json.ts: JsonResult is ok-true with a payload or ok-false with error string; empty input returns ok false and empty error before the engine; never throw. Clone the union as QrEncodeResult ok-true with matrix boolean[][] or ok-false with error string, and QrDecodeResult ok-true with payload string or ok-false with error string. Idle for encode IS json-style trim — if (!input.trim()) return ok false empty error BEFORE encodeQR (library would still emit a v1 QR for empty text). Catch encode returns QR_ENCODE_FAIL_MSG. Catch decode returns QR_NOT_FOUND_MSG. Never rethrow.

From qr@0.7.0: default export encodeQR from package root 'qr'. Call encodeQR(text, 'raw', { ecc }) where ecc is low | medium | quartile | high. Default medium. Never pass UI letters L/M/Q/H as opts.ecc (invalid ecc=M). Never call encodeQR with output data-url or gif (GIF, not PNG). Never set border 0. Never import qr/dom.js.

From qr@0.7.0 decode: default export decodeQR from 'qr/decode.js'. Call decodeQR(imageData, { effort: Infinity, timeLimit: Infinity }). Throws finder/data on miss — catch in decodeQr. Image is { width, height, data: Uint8ClampedArray | Uint8Array }. Tests pass synthetic RGBA in Node; do not set a jsdom environment pragma on qr.test.ts. Never import jsdom from src/lib/qr.ts.

From HEAD src/components/tools/WordCounter.tsx (git show HEAD:src/components/tools/WordCounter.tsx): default export { locale }: { locale: Locale }; import { t, type Locale } from '../../i18n/ui'; copy = t(locale); labels = copy.tools[slug]; ToolShell error/output without a locale prop; class not className; textarea onInput HTMLTextAreaElement cast; spellcheck false.

From HEAD src/components/tools/SqlFormatter.tsx (git show HEAD:src/components/tools/SqlFormatter.tsx): live useMemo, native select, no Generate button for encode; size guard before lib; localizeError for lib errors.

From HEAD src/components/tools/PasswordGenerator.tsx (git show HEAD:src/components/tools/PasswordGenerator.tsx): native button type=button inside the panel; native control wrapping inside label.

From HEAD ToolShell (git show HEAD:src/components/ToolShell.tsx): props { error, output, children } — no locale. Copy is navigator.clipboard.writeText(props.output) when output is non-empty. Do not edit ToolShell.tsx. Do not pass locale into ToolShell.

From src/lib/limits.ts: INPUT_MAX_CHARS 100000; INPUT_TOO_LARGE_MSG is Input too large to process in the browser.; isTooLarge is input.length greater than that. APPEND IMAGE_MAX_BYTES 5242880 and IMAGE_TOO_LARGE_MSG Image is too large to process in the browser. Do not change isTooLarge or INPUT_MAX_CHARS. Text cap lives in the island before encode. Image cap is file.size before bitmap.

From src/data/tools.ts: append-only row; ToolCategory includes Generate; do not edit the first ten objects' relatedSlugs (CAT-05, CONTEXT catalog additive).

From src/data/tools.test.ts: it has exactly 17 tools uses toHaveLength(17) — bump to 18; featured toHaveLength(6) stays; existsSync(URL) EN+ZH loop already covers new slugs — never URL.pathname.

From src/components/tools/ToolIsland.astro: last branch is slug === 'markdown-preview' with MarkdownPreview client:load locale={locale}. Add static import plus slug === 'qr-code' && QrCode client:load locale={locale}. Do not import qr here. Do not use dynamic component tags.

From src/content.config.ts: howTo tuple of exactly 3; faq min 3 max 5; locale en or zh. Keep locale so dirty content.config still builds.

From src/i18n/ui.ts: ui.en.tools and ui.zh.tools must share keys. Append tools['qr-code'] on both with UI-SPEC keys only: name, shortDescription, generateSection, decodeSection, text, ecc, preview, downloadPng, image. Do not drop Phase 2/3/4/5 keys. Do not add empty-state heading or empty-state body keys. ECC option labels are the letters L M Q H on both locales.

From src/i18n/errors.ts: INPUT_TOO_LARGE_MSG is already mapped. Append ZH_ERRORS for IMAGE_TOO_LARGE_MSG, QR_NOT_FOUND_MSG, not-an-image, and encode-fail. Do not drop existing keys. Do not duplicate the text-cap key.

From HEAD src/styles/global.css: .tool-grid.split already 1fr 1fr at 720px. Append only .qr-preview rules. Do not retokenize :root. Do not reuse Phase 4 add/delete colors. Ignore dirty-overlay tokens.

From CONVENTIONS 8-file checklist: catalog row featured false; lib+test; island; ToolIsland branch; ui.ts EN+ZH; ZH_ERRORS for new English errors; EN md; ZH md. No lib barrel. Heavy packages imported only from that tool's src/lib.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end qr-code — encode plus decode through lib, island, catalog, and EN/ZH page</name>
  <precondition>Phase 5 completeness is green at catalog length 17: src/data/tools.test.ts snapshots toHaveLength(17), featured length 6, ToolIsland.test.ts source-reads slug-equals branches including markdown-preview, and markdown-preview ZH markdown remains on disk</precondition>
  <reversibility rating="costly">Public catalog slug qr-code plus direct qr@0.7.0 become the generate/decode contract; swapping the stack later is a coordinated lib, island, FAQ, CSS, and bundle-isolation change.</reversibility>
  <files>package.json, package-lock.json, src/lib/qr.test.ts, src/lib/qr.ts, src/lib/limits.ts, src/components/tools/QrCode.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/i18n/errors.test.ts, src/content/tools/qr-code.md, src/content/tools/zh/qr-code.md, src/styles/global.css</files>
  <read_first>src/lib/json.ts, src/lib/sql.ts, src/lib/sql.test.ts, src/components/tools/WordCounter.tsx, src/components/tools/SqlFormatter.tsx, src/components/tools/PasswordGenerator.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/i18n/errors.test.ts, src/content/tools/markdown-preview.md, src/content.config.ts, src/lib/limits.ts, src/styles/global.css, .planning/phases/06-qr-code/06-RESEARCH.md, .planning/phases/06-qr-code/06-PATTERNS.md, .planning/phases/06-qr-code/06-UI-SPEC.md, .planning/codebase/CONVENTIONS.md</read_first>
  <behavior>
    - encodeQr('') and encodeQr('   ') equal { ok: false, error: '' } and must not call encodeQR on empty or whitespace-only input
    - encodeQr('hello') is ok true and matrix is a non-empty boolean[][]
    - npm test green at TOOLS length 18 and featured 6 with qr-code catalog row featured false
  </behavior>
  <action>
Land the thinnest production qr-code path through every layer this phase touches (QR-01, QR-02, QR-03, QR-04, QR-05, QR-06, QR-07, CAT-01, CAT-02, CAT-03, CAT-06). Official package qr@0.7.0 only. Thin src/lib/qr.ts — do not import qr, qr/decode.js, or qr/dom.js from the island or ToolIsland. Do not edit ToolShell.tsx, json.ts, JsonFormatter.tsx, or the existing ten TOOLS relatedSlugs (CAT-05, CONTEXT catalog additive). Do not create a lib barrel. Do not install jsqr, qrcode, qr-scanner, @zxing/library, @zxing/browser, canvas, or a second jsdom. Stage only this plan's files_modified; leave unrelated dirty worktree files alone. Do not commit unrelated dirty i18n/pages. Do not pop any existing git stash. Clone HEAD WordCounter locale/t() wiring (git show HEAD:src/components/tools/WordCounter.tsx). Clone HEAD SqlFormatter native select. Clone HEAD PasswordGenerator button type=button in the panel. Clone HEAD ToolShell (no locale prop). Do not clone dirty worktree JsonFormatter or dirty ToolShell (missing useToolUi / extra locale prop).

1. npm install qr@0.7.0 so package.json and the lockfile record that version. Confirm the resolved version is 0.7.0. Package legitimacy is Approved in 06-RESEARCH; no human checkpoint.

2. Append IMAGE_MAX_BYTES 5242880 and IMAGE_TOO_LARGE_MSG Image is too large to process in the browser. to src/lib/limits.ts. Do not change isTooLarge or INPUT_MAX_CHARS.

3. Create src/lib/qr.test.ts then src/lib/qr.ts (tests first). Node environment — do not add a jsdom environment pragma. Named exports only. Export QrEcc as L | M | Q | H. Export QrEncodeResult and QrDecodeResult. Export QR_ENCODE_FAIL_MSG Cannot encode this text as a QR code. and QR_NOT_FOUND_MSG No QR code found in this image. Export encodeQr(input, ecc = M) and decodeQr(img). If !input.trim(), return ok false empty error BEFORE encodeQR. Map L to low, M to medium, Q to quartile, H to high inside this file; pass those library names only. Call encodeQR from 'qr' as encodeQR(text, 'raw', { ecc: mapped }). Catch → QR_ENCODE_FAIL_MSG. decodeQr calls decodeQR from 'qr/decode.js' with effort Infinity and timeLimit Infinity. Catch → QR_NOT_FOUND_MSG. Never throw. Import encodeQR from 'qr' and decodeQR from 'qr/decode.js' only in this file. Never import qr/dom.js. Never import jsdom. Never pass UI letter M as opts.ecc.

4. Append one TOOLS row only (CAT-01, CAT-05, CONTEXT catalog additive): slug qr-code, name QR Code, category Generate, shortDescription Generate a QR from text or decode an image in your browser., relatedSlugs uuid-generator, password-generator, hash-generator in that order, featured false. In src/data/tools.test.ts change the snapshot from 17 to 18 and rename that it() to has exactly 18 tools. Do not change featured length 6. Do not edit the first ten objects' relatedSlugs.

5. Add ui.en.tools['qr-code'] and ui.zh.tools['qr-code'] with shared keys verbatim from 06-UI-SPEC Copy keys table: name, shortDescription, generateSection, decodeSection, text, ecc, preview, downloadPng, image. EN/ZH strings must match the UI-SPEC table exactly (QR Code / 二维码, Generate a QR from text or decode an image in your browser. / 在浏览器里从文本生成二维码，或从图片解码。, Generate / 生成, Decode / 解码, Text or URL / 文本或网址, Error correction / 纠错级别, Preview / 预览, Download PNG / 下载 PNG, Image / 图片). Do not drop Phase 2/3/4/5 keys. Do not add empty-state heading or empty-state body keys.

6. Append ZH_ERRORS in src/i18n/errors.ts mapping IMAGE_TOO_LARGE_MSG to 图片过大，无法在浏览器中处理。, QR_NOT_FOUND_MSG to 图片中未找到二维码。, Could not read this file as an image. to 无法将此文件作为图片读取。, and QR_ENCODE_FAIL_MSG to 无法将这段文本编码为二维码。 Keep the existing INPUT_TOO_LARGE_MSG map; do not duplicate that key. Do not drop existing keys. Append a qr-code describe in src/i18n/errors.test.ts that asserts the nine chrome keys on en and zh and that localizeError zh of the four new English strings plus INPUT_TOO_LARGE_MSG matches those ZH strings. Keep existing Phase 2/3/4/5 describes.

7. Create src/components/tools/QrCode.tsx: default export, locale prop. t(locale), labels from copy.tools['qr-code']. useState input, ecc default M, decode payload string, decode error. Live useMemo on input plus ecc: if isTooLarge(input), generate error is localizeError(locale, INPUT_TOO_LARGE_MSG), no matrix, do not call encodeQr (CAT-06, T-06-SIZE). Else encodeQr(input, ecc); idle empty error means blank preview; encode fail localizes QR_ENCODE_FAIL_MSG; ok true yields matrix. Paint a canvas 256 by 256 black number 000000 on white number ffffff with 16 CSS-pixel quiet zone; modulePx is Math.floor((256 - 32) / n) with leftover as extra padding (QR-01). Idle: .qr-preview empty, no canvas child, Download PNG disabled. Download PNG (QR-02): enabled only when matrix is ok; click uses canvas toDataURL image/png plus a temporary anchor download qr-code.png. Never use encodeQR data-url. File input (QR-04, QR-07): type file, accept image/png,image/jpeg,image/webp,image/gif, no capture attribute. Compare file.size to IMAGE_MAX_BYTES before bitmap (QR-06); over cap localizes IMAGE_TOO_LARGE_MSG and does not read. Then createImageBitmap(file); failure localizes Could not read this file as an image. After bitmap, if width or height greater than 4096, IMAGE_TOO_LARGE_MSG without decodeQr. Else canvas getImageData then decodeQr. Success writes payload to ToolShell output (QR-05). Failure localizes QR_NOT_FOUND_MSG. Never fetch the file. Never getUserMedia, mediaDevices, or video. ToolShell error is decodeError ?? generateError; do not pass locale. output is the decoded string only — generate never assigns a data-URL or pixel dump. Changing generate text does not clear a successful decode payload. Layout: generate section title then div.tool-grid.split with controls first (label text plus textarea spellcheck false, then label ecc plus native select L/M/Q/H) and .qr-preview second (label preview plus surface plus button downloadPng). Then decode section title plus label image wrapping the file input. class not className. Import encodeQr and decodeQr from ../../lib/qr — do not import qr packages from this file. Do not import useToolUi. No Generate button for encode. No aria-live on .qr-preview. Idle surface aria-hidden true; populated canvas or img role img aria-label equal to labels.preview.

8. In ToolIsland.astro add a static import of QrCode and a branch slug === 'qr-code' with client:load and locale={locale} after the markdown-preview branch. Keep existing branches. Do not import qr in ToolIsland.astro.

9. Append only to src/styles/global.css: .qr-preview width min(256px, 100%), aspect-ratio 1, height auto, background var(--bg), border 1px solid var(--border), border-radius 6px. Do not set max-height 384px. Do not retokenize :root. Do not reuse Phase 4 add/delete colors. If the worktree CSS has unrelated dirty visual edits, add only this block.

10. Add src/content/tools/qr-code.md locale en and src/content/tools/zh/qr-code.md locale zh. Schema: title, description, intro, howTo exactly 3, faq 3 items. EN FAQ must state encoding and decoding run in the browser and nothing is uploaded, decode is from a selected image file, and the tool never uses the camera. ZH FAQ must state local/no-upload, selected file, and never camera. Discretion on remaining wording. Do not mention later-phase tools.

No Playwright. No tsx tests. No API routes.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green with TOOLS length 18 and featured 6; qr-code catalog row featured false; ToolIsland contains slug === 'qr-code' with locale={locale}; EN+ZH markdown exist; encodeQr idle empty error before encode; hello fixture is ok true; QrCode uses isTooLarge then encodeQr, file decode via decodeQr, live generate, .qr-preview children, canvas PNG download, and ToolShell decoded-string copy payload; ZH_ERRORS maps image-cap, not-found, not-an-image, encode-fail; existing ten relatedSlugs unchanged; qr@0.7.0 is a direct dependency.</done>
  <acceptance_criteria>
    - package.json lists qr at 0.7.0 as a direct dependency; no jsqr, no qrcode, no qr-scanner, no @zxing/library, no @zxing/browser, no canvas package, no extra jsdom install
    - src/lib/qr.ts exports encodeQr and decodeQr; empty and whitespace input is ok false empty error; hello is ok true with a matrix
    - QrCode.tsx default-exports, calls isTooLarge on generate text then encodeQr, compares file.size to IMAGE_MAX_BYTES, calls decodeQr after bitmap, ToolShell output is decoded text, visual surface is children .qr-preview
    - TOOLS includes qr-code category Generate featured false; tools.test.ts toHaveLength 18; getFeaturedTools still length 6
    - ToolIsland.astro static-imports QrCode and has slug === 'qr-code' with locale={locale}
    - EN and ZH markdown exist with howTo length 3 and faq 3-5 including local / nothing uploaded / selected image file / never camera
    - Existing ten TOOLS objects' relatedSlugs bytes unchanged; no lib barrel file created
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>ECC map, scale-2 round-trip, camera-forbid, PNG/output source-read, chrome keys, and FAQ lock</name>
  <files>src/lib/qr.ts, src/lib/qr.test.ts, src/i18n/errors.test.ts, src/components/tools/QrCode.tsx, src/content/tools/qr-code.md, src/content/tools/zh/qr-code.md</files>
  <read_first>src/lib/qr.ts, src/lib/qr.test.ts, src/i18n/errors.test.ts, src/i18n/ui.ts, src/components/tools/QrCode.tsx, src/content/tools/qr-code.md, src/content/tools/zh/qr-code.md, src/lib/sql.test.ts, src/lib/markdown.test.ts, .planning/phases/06-qr-code/06-RESEARCH.md, .planning/phases/06-qr-code/06-UI-SPEC.md</read_first>
  <behavior>
    - encodeQr default ecc is M mapped to medium; L Q H map to low quartile high; qr.ts source does not pass letter M as opts.ecc
    - A synthetic RGBA raster of an encoded matrix at scale 2 round-trips to the same string; 64 by 64 noise is ok false with QR_NOT_FOUND_MSG
    - encodeQr of 3000 A's at H is ok false with QR_ENCODE_FAIL_MSG
    - ui.en.tools['qr-code'] and ui.zh.tools['qr-code'] share keys name, shortDescription, generateSection, decodeSection, text, ecc, preview, downloadPng, image
    - QrCode.tsx source contains isTooLarge(input), IMAGE_MAX_BYTES, file.size, toDataURL image/png, download qr-code.png, encodeQr, decodeQr, ../../lib/qr, and does not import qr packages
    - EN markdown faq answers mention browser / nothing uploaded / selected image file / camera; ZH markdown faq answers mention local or 不会上传 and never camera
  </behavior>
  <action>
Expand the proven lib and island into QR-03 ECC map, QR-04 scale-2 round-trip, QR-02 PNG source-read, QR-05 output source-read, QR-07 camera-forbid, CAT-06 chrome-key tests, and local-only FAQ source-read without changing architecture. Do not add packages. Do not edit ToolIsland.astro, tools.ts, JsonFormatter.tsx, ToolShell, or limits.ts. Keep idle trim-before-encode from the tracer. Keep the ECC map inside qr.ts.

1. Extend src/lib/qr.test.ts (keep Node env, no jsdom pragma) with: default M maps to a successful matrix; L versus H matrices differ in size or pattern; qr.ts contains the mapped names low medium quartile high and does not pass letter M through to encodeQR; 'A'.repeat(3000) at H returns QR_ENCODE_FAIL_MSG; synthetic RGBA built from an ok matrix at two pixels per module plus encoder quiet cells round-trips via decodeQr to the same payload (https://example.com/ is a good fixture); 64 by 64 random noise returns QR_NOT_FOUND_MSG. Read files with readFileSync(new URL(..., import.meta.url)) never URL.pathname.

2. Source-read QrCode.tsx for toDataURL image/png, download qr-code.png, ToolShell output bound to decoded payload, IMAGE_MAX_BYTES compared to file.size, isTooLarge(input), localizeError, encodeQr, decodeQr, ../../lib/qr, class not className, spellcheck false, tool-grid split, qr-preview. Assert the island does not contain from 'qr', from 'qr/decode.js', or from 'qr/dom.js'. Assert lib plus island do not contain getUserMedia, mediaDevices, rearCamera, selfieCamera, or a capture attribute. Assert qr.ts does not contain from 'qr/dom.js' or from 'jsdom'.

3. Keep the qr-code describe in src/i18n/errors.test.ts asserting chrome keys name, shortDescription, generateSection, decodeSection, text, ecc, preview, downloadPng, image on ui.en.tools['qr-code'] and ui.zh.tools['qr-code'], plus ZH_ERRORS for INPUT_TOO_LARGE_MSG, IMAGE_TOO_LARGE_MSG, QR_NOT_FOUND_MSG, Could not read this file as an image., and QR_ENCODE_FAIL_MSG. Do not drop existing lorem-ipsum, password-generator, sql-formatter, text-diff, or markdown-preview describes.

4. Source-read both markdown files so EN faq includes local/no-upload, selected image file, and never-camera wording and ZH faq includes 不会上传 or equivalent local-only wording plus selected file and never camera. howTo remains exactly 3. faq length 3–5.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>ECC map locked; scale-2 round-trip passes; noise is not-found; camera strings absent; PNG download and decoded output source-read; chrome keys locked on en+zh; FAQ local-only and no-camera lock; npm test green.</done>
  <acceptance_criteria>
    - Default M maps; UI letters are not passed as encodeQR ecc
    - Scale-2 synthetic raster round-trips; noise returns QR_NOT_FOUND_MSG
    - Capacity overflow maps to QR_ENCODE_FAIL_MSG
    - errors.test.ts shares the UI-SPEC chrome keys on en and zh without dropping Phase 2/3/4/5 describes
    - QrCode.tsx contains isTooLarge(input), IMAGE_MAX_BYTES, toDataURL image/png, and download qr-code.png
    - EN+ZH markdown FAQ include local / nothing uploaded / selected image file / never camera
  </acceptance_criteria>
</task>

<task type="auto">
  <name>CAT-04 isolation — only qr.ts imports qr packages; json-formatter chunk stays clean after build</name>
  <files>src/lib/qr.test.ts, src/lib/qr.ts</files>
  <read_first>src/lib/qr.ts, src/lib/qr.test.ts, src/components/tools/QrCode.tsx, src/components/tools/ToolIsland.astro, src/lib/json.ts, src/components/tools/JsonFormatter.tsx, package.json, .planning/phases/06-qr-code/06-RESEARCH.md, .planning/phases/05-markdown-preview/05-01-SUMMARY.md, .planning/phases/03-sql-formatter/03-01-SUMMARY.md</read_first>
  <action>
Prove CAT-04: qr and qr/decode.js are imported only from src/lib/qr.ts, QrCode imports ../../lib/qr, ToolIsland does not import those packages, json.ts and JsonFormatter.tsx do not import them, qr.ts does not import qr/dom.js or jsdom, completeness snapshot stays 18 with featured 6, and after a fresh astro build the json-formatter island chunk does not contain minify-surviving qr identifiers (06-RESEARCH pitfall 5). Do not add a lib barrel. Do not edit JsonFormatter.tsx, json.ts, ToolShell, dirty pages, or dirty i18n modules. Do not import qr from ToolIsland.astro. Do not trust a stale dist/_astro listing — always rebuild. Do not treat absence of from 'qr' in a minified chunk as proof of isolation.

1. In src/lib/qr.test.ts add a source-isolation test cloned from sql.test.ts / markdown.test.ts: read qr.ts, QrCode.tsx, ToolIsland.astro, json.ts, and JsonFormatter.tsx with readFileSync(new URL(...), utf8). Assert qr.ts contains from 'qr' and from 'qr/decode.js'. Assert qr.ts does not contain from 'qr/dom.js'. Assert the other four files do not contain those specifiers. Assert QrCode.tsx contains encodeQr, decodeQr, and ../../lib/qr. Assert qr.ts does not contain from 'jsdom'. Assert lib plus island do not match getUserMedia, mediaDevices, rearCamera, or selfieCamera.

2. Run a fresh npm run build then check every dist/_astro/JsonFormatter*.js file: none may contain encodeQR, decodeQR, Capacity overflow, invalid ecc, pointsOnDetect, paulmillr, or data:image/gif. If no JsonFormatter js chunk exists, that is a failure (do not pass on a missing chunk). If npm run build fails because of uncommitted page or i18n files that are not in this plan's files_modified (Phase 3/4/5 dirty-tree pattern), copy those failing files aside, restore them from HEAD, rebuild, run the identifier check, then restore the aside copies. Do not rewrite those dirty files this phase. Do not pop any existing git stash. If a worktree dist is the only successful build artifact after dirty-main isolation, use that dist/_astro for CAT-04 evidence. If the json-formatter chunk leaks, keep architecture: move the package load to a dynamic import inside encodeQr/decodeQr of the same default encodeQR from 'qr' and decodeQR from 'qr/decode.js' (still only inside qr.ts), rebuild, and re-check. Do not import the packages from ToolIsland.astro or a barrel to fix the leak.
  </action>
  <verify>
    <automated>npm test && npm run build && node --input-type=module -e "import fs from 'node:fs'; import path from 'node:path'; const dir='dist/_astro'; if (!fs.existsSync(dir)) { console.error('missing dist/_astro'); process.exit(1); } const files = fs.readdirSync(dir).filter((f) => f.startsWith('JsonFormatter') && f.endsWith('.js')); if (!files.length) { console.error('no JsonFormatter js chunk'); process.exit(1); } let bad = false; for (const f of files) { const t = fs.readFileSync(path.join(dir, f), 'utf8'); if (t.includes('encodeQR') || t.includes('decodeQR') || t.includes('Capacity overflow') || t.includes('invalid ecc') || t.includes('pointsOnDetect') || t.includes('paulmillr') || t.includes('data:image/gif')) { console.error('CAT-04 leak in ' + f); bad = true; } } process.exit(bad ? 1 : 0);"</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary, or output contains "CAT-04 leak", or output contains "missing dist/_astro", or output contains "no JsonFormatter js chunk"</fails_when>
    <human-check>Open /tools/qr-code/ (and /zh/tools/qr-code/ if that tree is being served): Generate section above Decode; text/ECC left or top and 256 by 256 preview right or bottom; type a URL and see live black-on-white modules with no Generate button; clear text: blank .qr-preview, Download disabled, no sample QR; Download is qr-code.png not GIF; select a generated PNG and Copy writes the decoded string; oversize image shows 图片过大，无法在浏览器中处理。 on ZH or the English image-cap string on EN; no camera prompt; homepage featured count still six.</human-check>
  </verify>
  <done>npm test green; only qr.ts imports qr and qr/decode.js; never qr/dom.js from lib; fresh astro build; JsonFormatter island chunk has none of the seven minify-surviving identifiers; featured still 6; length 18.</done>
  <acceptance_criteria>
    - qr.test.ts isolation test: qr.ts has from 'qr' and from 'qr/decode.js'; QrCode.tsx, ToolIsland.astro, json.ts, JsonFormatter.tsx do not
    - QrCode.tsx imports encodeQr and decodeQr from ../../lib/qr
    - qr.ts does not import qr/dom.js or jsdom
    - Fresh npm run build succeeds (with dirty-page isolation if needed, without rewriting those pages, without popping stash)
    - Every dist/_astro/JsonFormatter*.js file exists after build and contains none of encodeQR, decodeQR, Capacity overflow, invalid ecc, pointsOnDetect, paulmillr, data:image/gif
    - No src/lib barrel file exists
  </acceptance_criteria>
</task>

</tasks>

<!-- planner-discipline-allow: encodeQR -->
<!-- planner-discipline-allow: decodeQR -->
<!-- planner-discipline-allow: Capacity overflow -->
<!-- planner-discipline-allow: invalid ecc -->
<!-- planner-discipline-allow: pointsOnDetect -->
<!-- planner-discipline-allow: paulmillr -->
<!-- planner-discipline-allow: data:image/gif -->
<!-- planner-discipline-allow: from 'qr' -->
<!-- planner-discipline-allow: from 'qr/decode.js' -->
<!-- planner-discipline-allow: from 'qr/dom.js' -->
<!-- planner-discipline-allow: getUserMedia -->
<!-- planner-discipline-allow: IMAGE_TOO_LARGE_MSG -->
<!-- planner-discipline-allow: INPUT_TOO_LARGE_MSG -->
<!-- planner-discipline-allow: QR_NOT_FOUND_MSG -->
<!-- planner-discipline-allow: QR_ENCODE_FAIL_MSG -->
<!-- planner-discipline-allow: IMAGE_MAX_BYTES -->

## Artifacts this phase produces

Slice symbols and paths created by this plan:

- `encodeQr`, `decodeQr`, `QrEncodeResult`, `QrDecodeResult` in `src/lib/qr.ts`
- `QrCode` default export in `src/components/tools/QrCode.tsx`
- Catalog slug `qr-code` in `TOOLS`
- `ui.en.tools['qr-code']` and `ui.zh.tools['qr-code']`
- `ZH_ERRORS` maps for image-cap, not-found, not-an-image, encode-fail
- `src/content/tools/qr-code.md` and `src/content/tools/zh/qr-code.md`
- Additive `.qr-preview` rules in `src/styles/global.css`
- `IMAGE_MAX_BYTES` and `IMAGE_TOO_LARGE_MSG` in `src/lib/limits.ts`
- Direct dependency `qr@0.7.0`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor text → QrCode island | Untrusted generate text crosses into in-browser encodeQr only |
| visitor File → QrCode island | Untrusted local image bytes cross into createImageBitmap then decodeQr; must not be fetched or uploaded |
| qr.ts → encodeQR / decodeQR | Third-party encoder/decoder; island must not import the packages (CAT-04) |
| island → canvas PNG + ToolShell output | Painted modules download as PNG; Copy uses decoded text node only |
| static SSG → browser | No tool API route; no camera permission; no remote image fetch for decode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-CAM | Information Disclosure | QrCode.tsx / qr.ts | high | mitigate | Never import qr/dom.js; no getUserMedia, mediaDevices, capture attribute, or video element; FAQ states never camera; source-read tests forbid those strings (QR-07) |
| T-06-SIZE | Denial of Service | QrCode.tsx / limits.ts | medium | mitigate | isTooLarge on generate text before encode; IMAGE_MAX_BYTES 5242880 on file.size before bitmap; reject width or height greater than 4096 before decodeQr; distinct IMAGE_TOO_LARGE_MSG (QR-06) |
| T-06-GIF | Tampering | QrCode.tsx download | medium | mitigate | PNG download is canvas toDataURL image/png plus anchor download qr-code.png; never encodeQR data-url (GIF) (QR-02) |
| T-06-XSS | Elevation of Privilege | QrCode.tsx / ToolShell | high | mitigate | Preview is canvas pixels not HTML of source; ToolShell output is decoded string in a text node (HEAD pre/code); never dangerouslySetInnerHTML of user text or payload (QR-05) |
| T-06-FAQ | Information Disclosure | island / FAQ | medium | mitigate | No fetch of the selected File; no tool API route; FAQ states nothing is uploaded, decode is a selected image file, never camera; session-only component state; do not write localStorage |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | Direct install is qr@0.7.0 only (06-RESEARCH Approved, no postinstall). Do not install jsqr, qrcode, qr-scanner, @zxing/*, canvas, or a second jsdom. No [SLOP] packages. No blocking human checkpoint (seam SUS was too-new, contradicted by official README + paulmillr + zero deps). CAT-04 greps the seven minify-surviving identifiers in JsonFormatter*.js |

</threat_model>

<verification>
Inherit Phase 5 `npm test` after tasks 1–2. Task 3 adds `npm run build` plus a Node read of dist/_astro/JsonFormatter*.js that fails on missing chunk or CAT-04 leak (the seven minify-surviving identifiers). Completeness: ToolIsland slug-equals loop and EN+ZH existsSync must pass at length 18. Featured length 6. qr.test.ts covers idle, ECC, capacity, scale-2 round-trip, noise, camera-forbid, and import isolation. Do not run Playwright. Do not trust stale dist/_astro from before this package. If dirty pages break the build, isolate without rewriting them and without popping stash.
</verification>

<success_criteria>
- QR-01..07 delivered for slug qr-code at 8-file parity
- encodeQR raw plus decodeQR with Infinity effort/timeLimit; idle trim before encode; ECC letters mapped in lib
- Visual .qr-preview plus decoded-text copy payload; empty generate text is blank not a sample QR
- isTooLarge on generate text; IMAGE_MAX_BYTES on file.size; FAQ local / nothing uploaded / selected file / never camera in EN+ZH
- Catalog 18, featured 6, existing ten relatedSlugs untouched (CAT-05, CONTEXT catalog additive)
- CAT-04: only qr.ts imports qr and qr/decode.js; json-formatter chunk clean after fresh build
- npm test green and npm run build CAT-04 grep green
</success_criteria>

<output>
Create `.planning/phases/06-qr-code/06-01-SUMMARY.md` when done
</output>
