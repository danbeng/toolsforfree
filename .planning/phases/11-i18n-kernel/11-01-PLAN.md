---
phase: 11-i18n-kernel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales.ts
  - src/i18n/locales.test.ts
  - src/i18n/path.ts
  - src/i18n/path.test.ts
  - src/i18n/ui.ts
  - src/i18n/useToolUi.ts
  - src/i18n/useToolUi.test.ts
autonomous: true
requirements:
  - KERN-01
  - KERN-02
  - KERN-03
  - KERN-04

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "locales.ts exports LOCALES as en then zh, Locale as typeof LOCALES[number], and LOCALE_META with en (hreflang en, htmlLang en, nativeLabel English) and zh (hreflang zh-Hans, htmlLang zh-Hans, nativeLabel 中文) per KERN-01, D-01"
    - "localizedPath, switchLocalePath, and localeFromPathname produce EN unprefixed URLs, ZH /zh/ URLs, and trailing slashes; they never throw; illegal locale is treated as en per KERN-02, D-02"
    - "Path helpers never emit a string that starts with two slashes (T-11-01, D-02)"
    - "localeFromPathname treats /zh and /zh/... as zh and treats /zhfoo/ plus empty input as en per D-02"
    - "useToolUi(locale) returns copy, tooLarge, and err; err of empty string or null is null; err wraps localizeError per KERN-03, D-03"
    - "ui.en and ui.zh have chrome keys for home, nav Tools/Blog/About, footer, 404, langSwitch, howTo, faq, localNote, copy/copied, tooLarge, categories, original-ten tools[slug], plus dirty-page keys; Locale is imported from locales.ts and re-exported per KERN-04, D-04"
    - "ui.en.tooLarge byte-matches INPUT_TOO_LARGE_MSG including the period (D-03, D-04)"
    - "copy.categories is keyed by catalog ToolCategory PascalCase names Format Auth Encode Generate Text Time Color (D-04)"
    - "Header can still import type Locale from i18n/ui because ui.ts re-exports it (D-01, D-04)"
    - "Do not commit LED ToolShell / tool-panel__chrome"
    - "Do not commit src/lib/crontab.ts"
    - "Do not pop stash@{0} or stash@{1}"
    - "Path-limited git add only — never git add -A"
    - "Do not mount LangSwitch, ZH tree, ToolIsland, or ToolShell this phase"
    - "No new npm packages, no Tailwind, no SITE_ORIGIN change"
  artifacts:
    - path: src/i18n/locales.ts
      provides: "LOCALES, Locale, LOCALE_META (KERN-01, D-01)"
      min_lines: 12
    - path: src/i18n/locales.test.ts
      provides: "LOCALES order, LOCALE_META.zh-Hans, chrome/fill asserts after expansion (KERN-01, KERN-04)"
      min_lines: 20
    - path: src/i18n/path.ts
      provides: "localizedPath, switchLocalePath, localeFromPathname; slash collapse (KERN-02, T-11-01, D-02)"
      min_lines: 30
    - path: src/i18n/path.test.ts
      provides: "Path table including /zhfoo, empty, illegal locale, two-slash input (KERN-02, T-11-01)"
      min_lines: 40
    - path: src/i18n/ui.ts
      provides: "Locale re-export, fill, chrome keys, categories, original-ten tools[slug] (KERN-04, D-04)"
      min_lines: 280
    - path: src/i18n/useToolUi.ts
      provides: "Pure useToolUi returning copy, tooLarge, err; empty err to null (KERN-03, D-03)"
      min_lines: 10
    - path: src/i18n/useToolUi.test.ts
      provides: "copy/tooLarge/err contract including empty to null (KERN-03, D-03)"
      min_lines: 20
  key_links:
    - from: src/i18n/ui.ts
      to: src/i18n/locales.ts
      via: "import type { Locale } from ./locales and export type { Locale }"
      pattern: "from './locales'"
    - from: src/i18n/path.ts
      to: src/i18n/locales.ts
      via: "import type { Locale } from ./locales only; never from ui"
      pattern: "from './locales'"
    - from: src/i18n/useToolUi.ts
      to: src/i18n/ui.ts
      via: "t(locale) for copy; tooLarge from copy.tooLarge"
      pattern: "from './ui'"
    - from: src/i18n/useToolUi.ts
      to: src/i18n/errors.ts
      via: "err wraps localizeError after empty-to-null"
      pattern: "localizeError"
    - from: src/i18n/ui.ts
      to: src/lib/limits.ts
      via: "ui.en.tooLarge equals INPUT_TOO_LARGE_MSG"
      pattern: "tooLarge"
  prohibitions:
    - "Do not commit LED ToolShell / tool-panel__chrome"
    - "Do not commit src/lib/crontab.ts"
    - "Do not pop stash@{0} or stash@{1}"
    - "Path-limited git add only — never git add -A"
    - "Do not mount LangSwitch, ZH tree, ToolIsland, or ToolShell this phase"
    - "No new npm packages, no Tailwind, no SITE_ORIGIN change"
    - "Do not create src/i18n/index.ts"
    - "Do not enable Astro i18n.routing"
    - "Do not expand ZH_ERRORS"
    - "Do not edit Header.astro, Footer.astro, RelatedTools.astro, ToolIsland.astro, original-ten islands, src/data/tools.ts, or src/data/site.ts"
---

<objective>
Ship the i18n kernel so pages and tool islands can resolve locale, localized paths, and chrome copy from locales.ts, path.ts, ui.ts, and useToolUi.ts (KERN-01, KERN-02, KERN-03, KERN-04, D-01, D-02, D-03, D-04).

Purpose: Unblock Phase 12 (LangSwitch / ZH tree) and Phase 13 (island locale) without mounting those consumers now.

Output: Seven i18n files (six new plus ui.ts expanded). No new npm packages.

Hard fences: Do not commit LED ToolShell / tool-panel__chrome. Do not commit src/lib/crontab.ts. Do not pop stash@{0} or stash@{1}. Path-limited git add only — never git add -A. Do not mount LangSwitch, ZH tree, ToolIsland, or ToolShell this phase. No new npm packages, no Tailwind, no SITE_ORIGIN change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-i18n-kernel/11-CONTEXT.md
@.planning/phases/11-i18n-kernel/11-RESEARCH.md
@.planning/phases/11-i18n-kernel/11-PATTERNS.md
@.planning/phases/11-i18n-kernel/11-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@src/i18n/ui.ts
@src/i18n/errors.ts
@src/i18n/errors.test.ts
@src/lib/limits.ts
@src/data/tools.ts
@src/components/Header.astro

<interfaces>
From src/i18n/ui.ts (HEAD): ui.en / ui.zh currently have tools for word-counter through qr-code plus nested nav.menu / nav.close. Inline Locale union lives at the bottom next to UiDict and t(locale). Header imports type Locale from ../i18n/ui. Do not nest navTools under nav.

From src/i18n/errors.ts: localizeError(locale, error) returns the same empty string when error is empty; ZH lookup is exact Record key; do not expand ZH_ERRORS this phase.

From src/lib/limits.ts: INPUT_TOO_LARGE_MSG is the sentence Input too large to process in the browser. with the period. ui.en.tooLarge must equal that constant.

From src/data/tools.ts: ToolCategory is Format | Auth | Encode | Generate | Text | Color | Time. Original-ten slugs are json-formatter, jwt-decoder, base64, url-encode, hash-generator, uuid-generator, regex-tester, unix-timestamp, crontab-explainer, color-converter.

Locked contracts to create:

src/i18n/locales.ts (D-01): export const LOCALES = ['en', 'zh'] as const; export type Locale = (typeof LOCALES)[number]; LOCALE_META Record of hreflang, htmlLang, nativeLabel.

src/i18n/path.ts (D-02): localizedPath(locale, path), switchLocalePath(pathname, target), localeFromPathname(pathname). Import Locale from ./locales only. Pure strings. No throw.

src/i18n/useToolUi.ts (D-03): named export useToolUi(locale) returns { copy, tooLarge, err }. copy = t(locale). tooLarge = copy.tooLarge. err maps empty/null to null then localizeError.

src/i18n/ui.ts (D-04): import type { Locale } from ./locales; export type { Locale }; export function fill(template, vars) replacing {token} from a string record.

Import graph (no cycles): locales.ts leaf; path.ts and errors.ts may import locales; ui.ts re-exports Locale and must not import path, errors, or useToolUi; useToolUi imports ui + errors.

Git allowlist only: src/i18n/locales.ts, src/i18n/locales.test.ts, src/i18n/path.ts, src/i18n/path.test.ts, src/i18n/useToolUi.ts, src/i18n/useToolUi.test.ts, src/i18n/ui.ts.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end locale plus path kernel — one path only</name>
  <reversibility rating="costly">LOCALES, Locale, and path helper signatures become the Phase 12/13 contract; renaming them later is a coordinated import change, not a data migration.</reversibility>
  <precondition>src/i18n/ui.ts and src/i18n/errors.ts exist on disk; package.json script test is vitest run; git stash list still shows stash@{0} and stash@{1}</precondition>
  <files>src/i18n/locales.ts, src/i18n/locales.test.ts, src/i18n/path.ts, src/i18n/path.test.ts, src/i18n/useToolUi.test.ts</files>
  <read_first>.planning/phases/11-i18n-kernel/11-CONTEXT.md, .planning/phases/11-i18n-kernel/11-RESEARCH.md, .planning/phases/11-i18n-kernel/11-PATTERNS.md, .planning/phases/11-i18n-kernel/11-VALIDATION.md, src/i18n/ui.ts, src/i18n/errors.ts, src/i18n/errors.test.ts, src/lib/limits.ts, src/components/Header.astro, vitest.config.ts</read_first>
  <action>
    Wave 0 first: create src/i18n/locales.test.ts, src/i18n/path.test.ts, and a src/i18n/useToolUi.test.ts stub. The stub is a vitest describe with it.todo for returns copy, tooLarge, and err — do not import useToolUi yet (module does not exist). Match errors.test.ts style: import { describe, expect, it } from vitest. Do not set jsdom. Do not add npm packages.

    locales.test.ts (KERN-01, D-01): import LOCALES, LOCALE_META, and type Locale from ./locales. Assert LOCALES equals ['en', 'zh'] in that order. Assert LOCALE_META.en is hreflang en, htmlLang en, nativeLabel English. Assert LOCALE_META.zh is hreflang zh-Hans, htmlLang zh-Hans, nativeLabel 中文. Keep a Locale assignability binding to en so the type export is referenced.

    path.test.ts (KERN-02, T-11-01, D-02): import localizedPath, switchLocalePath, localeFromPathname from ./path. Cover this table exactly: localizedPath en /tools/ to /tools/; zh /tools/ to /zh/tools/; en / to /; zh / to /zh/ (not /zh//); en tools without slash to /tools/; switchLocalePath /zh/tools/json-formatter/ to en yields /tools/json-formatter/; /tools/json-formatter/ to zh yields /zh/tools/json-formatter/; zh-to-zh keeps /zh/tools/json-formatter/; localeFromPathname /zh/ and /zh and /zh/tools/ are zh; /tools/ and /zhfoo/ and empty string are en; localizedPath with illegal locale fr as never yields /tools/ and does not throw; switchLocalePath of a two-leading-slash host-like input to en returns a string that does not start with two slashes; localeFromPathname /ZH/ is en (case-sensitive).

    Then create src/i18n/locales.ts as a leaf (D-01): LOCALES const array en then zh; Locale derived from typeof LOCALES[number]; LOCALE_META Record keyed by Locale with hreflang, htmlLang, nativeLabel as above. No imports from other i18n files.

    Create src/i18n/path.ts (D-02, T-11-01): import type Locale from ./locales only. Named exports localizedPath, switchLocalePath, localeFromPathname. Pure string operations; no browser globals; no Astro; do not throw. Normalize: force a single leading slash, force a trailing slash, collapse extra leading slashes so the result never starts with two slashes, strip a scheme prefix if present. Illegal locale (anything other than zh) treated as en. localeFromPathname: zh only when the path is exactly /zh or starts with /zh/; /zhfoo/ is en. switchLocalePath strips an existing /zh prefix then applies localizedPath for the target. localizedPath zh plus / must be /zh/.

    Do not edit ui.ts, Header, LangSwitch, Footer, ToolIsland, ToolShell, original-ten islands, ZH pages, src/data/tools.ts, src/data/site.ts, or src/lib/crontab.ts. Do not pop stash entries. Do not create src/i18n/index.ts. Stage only the five files listed in this task with path-limited git add.
  </action>
  <verify>
    <automated>npx vitest run src/i18n/locales.test.ts src/i18n/path.test.ts src/i18n/errors.test.ts -x</automated>
    <fails_when>non-zero exit, or a summary line with 0 passed, or locales.test / path.test not collected</fails_when>
  </verify>
  <acceptance_criteria>
    - src/i18n/locales.ts exports LOCALES, Locale, and LOCALE_META per KERN-01, D-01
    - src/i18n/path.ts exports localizedPath, switchLocalePath, localeFromPathname per KERN-02, D-02
    - path.test.ts includes the /zhfoo, empty, illegal-locale, and two-leading-slash cases (T-11-01)
    - src/i18n/useToolUi.test.ts exists as Wave 0 stub without importing useToolUi
    - errors.test.ts still passes
  </acceptance_criteria>
  <done>One happy path works: localeFromPathname('/zh/tools/') is zh, localizedPath('zh', '/tools/') is /zh/tools/, switchLocalePath('/zh/tools/json-formatter/', 'en') is /tools/json-formatter/, and LOCALES/LOCALE_META match D-01. The slice is committed via path-limited add of the five listed files only.</done>
</task>

<task type="auto">
  <name>Expand ui.ts chrome, categories, original-ten labels, fill, Locale re-export</name>
  <files>src/i18n/ui.ts, src/i18n/locales.test.ts</files>
  <read_first>src/i18n/ui.ts, src/i18n/locales.ts, src/data/tools.ts, src/lib/limits.ts, src/i18n/errors.test.ts, src/components/Header.astro, .planning/phases/11-i18n-kernel/11-RESEARCH.md</read_first>
  <action>
    Edit src/i18n/ui.ts only plus chrome/fill asserts in locales.test.ts (KERN-04, D-01, D-04). Import type Locale from ./locales at the top. Remove the inline two-member Locale union. Re-export Locale so Header and WordCounter keep compiling. Keep export type UiDict and function t(locale). Do not import ./path, ./errors, or ./useToolUi.

    Add named export fill(template, vars) that replaces {word} tokens from a Record of strings; missing keys become empty string. Do not assign onto objects by attacker keys.

    Keep nested nav.menu / nav.close exactly as they are on en and zh. Do not nest navTools under nav. Keep later-eight tools blocks word-counter through qr-code unchanged.

    On both ui.en and ui.zh add top-level keys: langSwitch, navPrivacy, navTerms, navAbout, navTools, navBlog, footerRuns, homeLede, homeKicker, homeFeatured, homeViewAll, categories, notFoundTitle, notFoundDescription, notFoundBody, notFoundCta, localNote, howTo, faq, related, copy, copied, tooLarge, toolsIndexTitle, toolsIndexDescription, toolsIndexIntro, aboutTitle, aboutDescription, aboutBody, privacyTitle, privacyDescription, privacy (string array), termsTitle, termsDescription, termsBody, blogTitle, blogDescription, blogEmpty.

    Discretion strings must be usable by dirty pages: aboutDescription/aboutBody/privacyDescription/termsDescription/termsBody use {name} and aboutBody also {email}. EN tooLarge must be the same characters as INPUT_TOO_LARGE_MSG including the period. ZH tooLarge may be the existing Chinese size sentence from ZH_ERRORS. EN copy/copied are Copy/Copied. EN navTools/navBlog/navAbout are Tools/Blog/About. categories keys must be PascalCase Format, Auth, Encode, Generate, Text, Time, Color (EN identity labels; ZH 格式/鉴权/编码/生成/文本/时间/颜色). Do not lowercase category keys.

    Fill tools[slug] on both locales for the original ten. Every slug needs name, shortDescription, and island keys: json-formatter json; jwt-decoder jwt header payload note; base64 mode encode decode text base64; url-encode mode encode decode text encoded; hash-generator algorithm text; uuid-generator generate; regex-tester pattern flags testString groups; unix-timestamp mode unixToIso isoToUnix unit seconds milliseconds unixLabel isoLabel iso secondsOut millisecondsOut; crontab-explainer fieldLabel; color-converter hex rgb hsl. EN name values match catalog names in src/data/tools.ts. ZH names match content titles: JSON 格式化 / 校验, JWT 解码, Base64 编码 / 解码, URL 编码 / 解码, 哈希生成, UUID 生成, 正则测试, Unix 时间戳转换, Crontab 说明, Hex / RGB / HSL 转换.

    Do not add LED-only dictionary keys. Do not edit Header, islands, ToolShell, LangSwitch, ZH pages, catalog, site constants, or crontab. Do not pop stash entries. Stage only src/i18n/ui.ts and src/i18n/locales.test.ts.

    Extend locales.test.ts: import ui and fill from ./ui; assert fill('Hi {name}', { name: 'Devtoolbox' }) equals Hi Devtoolbox; assert ui.en.tooLarge equals INPUT_TOO_LARGE_MSG imported from ../lib/limits; assert both locales have langSwitch, navTools, navBlog, navAbout, homeLede, categories.Format, copy, copied, and tools['json-formatter'].name; assert ui re-export compiles by importing type Locale from ./ui as well as ./locales.
  </action>
  <verify>
    <automated>npx vitest run src/i18n/locales.test.ts src/i18n/errors.test.ts -x</automated>
    <fails_when>non-zero exit, or a summary line with 0 passed, or locales.test / errors.test not collected</fails_when>
  </verify>
  <acceptance_criteria>
    - ui.ts re-exports Locale from ./locales per KERN-04, D-01, D-04
    - fill replaces {name} tokens per D-04
    - Chrome keys listed in the action exist on ui.en and ui.zh per KERN-04, D-04
    - Original-ten tools[slug] objects include name plus the island keys listed in the action
    - categories uses PascalCase ToolCategory keys
    - Later-eight tool chrome tests in errors.test.ts still pass
  </acceptance_criteria>
  <done>t('en') and t('zh') expose chrome, categories, original-ten labels, copy/copied, and tooLarge; Locale is sourced from locales.ts and re-exported from ui.ts (KERN-04, D-04).</done>
</task>

<task type="auto">
  <name>Add useToolUi copy, tooLarge, and err helper with tests</name>
  <files>src/i18n/useToolUi.ts, src/i18n/useToolUi.test.ts</files>
  <read_first>src/i18n/ui.ts, src/i18n/errors.ts, src/lib/limits.ts, src/i18n/useToolUi.test.ts, src/i18n/locales.ts, .planning/phases/11-i18n-kernel/11-PATTERNS.md</read_first>
  <action>
    Replace the Wave 0 stub in src/i18n/useToolUi.test.ts with real tests, then implement src/i18n/useToolUi.ts (KERN-03, D-03).

    useToolUi is a named pure function, not a Preact hook: no preact/hooks, no useState, no useMemo. Import t and type Locale from ./ui. Import localizeError from ./errors. copy = t(locale). tooLarge = copy.tooLarge. err(error: string | null): if the value is empty or null, return null; otherwise return localizeError(locale, error). Do not pass empty string through localizeError (it would return empty string). Do not expand ZH_ERRORS.

    useToolUi.test.ts: import useToolUi from ./useToolUi and INPUT_TOO_LARGE_MSG from ../lib/limits. For en: tooLarge and copy.tooLarge equal INPUT_TOO_LARGE_MSG; err('') and err(null) are null; err('Invalid JSON') is Invalid JSON. For zh: err(INPUT_TOO_LARGE_MSG) is 输入过长，无法在浏览器中处理。; tooLarge is a string (the zh dictionary value). Do not import preact.

    Do not edit islands, ToolShell, ToolIsland, Header, LangSwitch, ZH pages, crontab, catalog, or site constants. Do not pop stash entries. Stage only src/i18n/useToolUi.ts and src/i18n/useToolUi.test.ts. After tests pass, run the full suite. Path-limited git add of kernel files only if restaging; never stage the rest of the working tree.
  </action>
  <verify>
    <automated>npx vitest run src/i18n/useToolUi.test.ts src/i18n/locales.test.ts src/i18n/path.test.ts src/i18n/errors.test.ts src/data/tools.test.ts src/components/tools/ToolIsland.test.ts -x</automated>
    <fails_when>non-zero exit, or a summary line with 0 passed, or any of those files not collected</fails_when>
  </verify>
  <acceptance_criteria>
    - useToolUi returns copy, tooLarge, err per KERN-03, D-03
    - err('') and err(null) are null
    - en tooLarge byte-matches INPUT_TOO_LARGE_MSG
    - Catalog and ToolIsland completeness tests still pass
    - Staged paths stay inside the seven-file i18n allowlist
  </acceptance_criteria>
  <done>Tool islands can obtain copy / tooLarge / err() from useToolUi for a given locale without this phase mounting any island (KERN-03, D-03). Kernel files only are staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pathname string → path.ts | Untrusted URL path (404 Astro.url.pathname; later LangSwitch href) crosses into helpers that must emit root-relative paths only |
| Locale argument → path.ts / useToolUi / t() | Illegal locale must degrade to en; no throw, no open redirect |
| Chrome dictionary → pages/islands | Compile-time strings, not request input; still must not introduce LED-only keys or innerHTML |
| fill() template vars | Record values substitute into {token} slots; keys must not mutate objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Spoofing / Tampering | src/i18n/path.ts switchLocalePath / localizedPath | high | mitigate | Collapse leading slashes to one; strip schemes; never return a string that starts with two slashes; illegal locale → en; covered by path.test.ts two-slash case |
| T-11-02 | Tampering | src/i18n/path.ts localeFromPathname | medium | mitigate | Classify zh only for exact /zh or prefix /zh/; /zhfoo/ stays en; case-sensitive |
| T-11-03 | Tampering | src/i18n/useToolUi.ts err() | medium | mitigate | Empty string and null map to null before localizeError so ToolShell does not paint an empty alert |
| T-11-04 | Tampering | src/i18n/ui.ts fill() | low | mitigate | Replace only \{word\} from a string record; missing keys → empty string; do not write onto objects by key |
| T-11-05 | Information disclosure | src/i18n/ui.ts chrome strings | low | accept | Copy is compile-time constants, not user input; islands already avoid innerHTML |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager installs this phase; legitimacy table is empty / N/A |

ASVS L1: high-severity T-11-01 is mitigated in the tracer. T-11-SC accepted because this plan installs nothing.
</threat_model>

<verification>
After all three tasks:

1. npx vitest run src/i18n/locales.test.ts src/i18n/path.test.ts src/i18n/useToolUi.test.ts src/i18n/errors.test.ts -x
2. npm test
3. Path-limited staging only of the seven i18n allowlist files. Confirm git diff --cached --name-only does not list src/lib/crontab.ts, src/components/ToolShell.tsx, src/components/LangSwitch.astro, or src/pages/zh/.
4. git stash list still shows stash@{0} and stash@{1}.
5. src/data/site.ts still contains https://example.com. package.json dependencies were not added.
</verification>

<success_criteria>
- KERN-01: LOCALES, Locale, LOCALE_META exported from locales.ts (D-01)
- KERN-02: path helpers EN unprefixed, ZH /zh/, trailing slash, no throw, no two-slash prefix (D-02, T-11-01)
- KERN-03: useToolUi provides copy / tooLarge / err(); empty err is null (D-03)
- KERN-04: ui.ts chrome keys + original-ten labels + categories + Locale re-export + fill (D-04)
- Hard fences held: no LED ToolShell commit, no crontab commit, stashes unpopped, path-limited add, no LangSwitch/ZH/ToolIsland/ToolShell mount, no new packages, no Tailwind, no origin change
</success_criteria>

<output>
Create `.planning/phases/11-i18n-kernel/11-01-SUMMARY.md` when done
</output>
