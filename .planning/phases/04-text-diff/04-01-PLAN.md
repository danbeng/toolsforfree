---
phase: 04-text-diff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/diff.ts
  - src/lib/diff.test.ts
  - src/components/tools/TextDiff.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/i18n/errors.test.ts
  - src/content/tools/text-diff.md
  - src/content/tools/zh/text-diff.md
  - src/styles/global.css
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-03
  - DIFF-04
  - DIFF-05
  - DIFF-06

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can open /tools/text-diff/ (and /zh/tools/text-diff/ when that tree exists), paste original and changed text in two panes, and see a live line-level add/delete list (DIFF-01, DIFF-02)"
    - "Stats tiles above the panes show integer Added and Removed counts whenever at least one pane has characters, including identical 0 / 0 (DIFF-04)"
    - "Native checkbox Ignore leading/trailing whitespace defaults unchecked; checking it trims per line for equality and does not collapse internal spaces (DIFF-03, CONTEXT whitespace toggle)"
    - "Identical non-empty texts show No differences / 无差异 with Copy enabled using that heading; both empty or whitespace-only is idle, not that heading (DIFF-05)"
    - "Each pane is independently size-capped with isTooLarge; over-cap shows the matching ui.ts too-large string, clears list and copy payload, and does not call diffText (DIFF-06)"
    - "ToolShell Copy writes the UI-SPEC labeled add/delete summary (Added/Removed header plus - / + / two-space body), not a unified patch (CONTEXT copy payload, D-13 catalog additive only)"
    - "Catalog slug text-diff, category Text, featured false; TOOLS toHaveLength 16; getFeaturedTools stays 6; relatedSlugs word-counter, case-converter, json-formatter; existing ten relatedSlugs unchanged (CAT-01, CAT-05, D-13)"
    - "After astro build, dist/_astro/JsonFormatter*.js does not contain the five minify-surviving diff identifiers; only src/lib/diff.ts imports the diff package (CAT-04)"
  artifacts:
    - path: src/lib/diff.ts
      provides: "diffText plus DiffResult / DiffLine; thin wrap of named diffLines; idle before the engine"
      exports: ["diffText"]
    - path: src/lib/diff.test.ts
      provides: "idle, add/delete, ignore-ws, identical, one-row-per-line, isolation, FAQ lock"
    - path: src/components/tools/TextDiff.tsx
      provides: "Default-export island; two panes; checkbox; stats; ol.diff-lines; per-pane isTooLarge; live useMemo; ToolShell copy payload"
    - path: src/components/tools/ToolIsland.astro
      provides: "static TextDiff import and slug === 'text-diff' client:load locale={locale}"
    - path: src/data/tools.ts
      provides: "text-diff catalog row featured false category Text"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 16; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['text-diff'] UI-SPEC chrome keys including too-large variants"
    - path: src/content/tools/text-diff.md
      provides: "locale en SEO howTo 3 faq 3-5 including local / nothing uploaded"
    - path: src/content/tools/zh/text-diff.md
      provides: "locale zh SEO howTo 3 faq with local / no-upload"
    - path: src/styles/global.css
      provides: "additive .diff-lines / .diff-line--add / .diff-line--del / .diff-line--eq rules"
    - path: package.json
      provides: "direct dependency diff 9.0.0"
  key_links:
    - from: src/components/tools/TextDiff.tsx
      to: src/lib/diff.ts
      via: "diffText after per-pane isTooLarge"
      pattern: "diffText"
    - from: src/lib/diff.ts
      to: diff
      via: "named line differ with ignoreWhitespace and oneChangePerToken"
      pattern: "diffLines"
    - from: src/components/tools/TextDiff.tsx
      to: src/lib/limits.ts
      via: "isTooLarge(original) and isTooLarge(changed) before diffText"
      pattern: "isTooLarge"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/TextDiff.tsx
      via: "static import and slug === 'text-diff' client:load"
      pattern: "slug === 'text-diff'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(16)"
      pattern: "toHaveLength\\(16\\)"
    - from: src/components/tools/TextDiff.tsx
      to: src/components/ToolShell.tsx
      via: "error plus labeled copy payload; visual list is children"
      pattern: "ToolShell"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** paste original and changed text and see a readable line-level add/delete diff with stats in the browser, **so that** I can compare texts without uploading anything.

<objective>
Ship the text-diff 8-file vertical slice at existing-tool parity (DIFF-01, DIFF-02, DIFF-03, DIFF-04, DIFF-05, DIFF-06) plus CAT-01..06 kept green, including CAT-04 bundle isolation. Install official diff@9.0.0. Thin src/lib/diff.ts wrapping named diffLines only. Never the two-file patch formatter, never word/char differ, never a hand-rolled LCS. Idle both-empty (whole-string trim) BEFORE the engine. Options { ignoreWhitespace, oneChangePerToken: true }. Island clones HEAD WordCounter locale/t(), HEAD PasswordGenerator checkbox-in-label, HEAD SqlFormatter live useMemo, HEAD ToolShell with no locale prop — not dirty JsonFormatter (useToolUi) or dirty ToolShell. Catalog slug text-diff, Text, featured false; TOOLS 15 → 16; featured stays 6; relatedSlugs word-counter, case-converter, json-formatter; do not rewrite existing ten relatedSlugs (D-13). Too-large strings live in ui.ts (three EN/ZH variants), not INPUT_TOO_LARGE_MSG and not ZH_ERRORS. Visual list ol.diff-lines one node per line; ToolShell pre is copy-only labeled summary. CSS append .diff-lines / .diff-line--add (#3dd68c on #13291f) / .diff-line--del (#f07178); never accent for add/delete. No lib barrel. After astro build, json-formatter chunk must not inherit minify-surviving diff identifiers.
Purpose: Second heavy-library catalog tool; visitors compare two texts in-browser with a readable line-level diff.
Output: diff lib/test, TextDiff island, ToolIsland branch, catalog row, ui.ts chrome, EN+ZH markdown, additive CSS, diff@9.0.0, CAT-04 build grep green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-text-diff/04-CONTEXT.md
@.planning/phases/04-text-diff/04-RESEARCH.md
@.planning/phases/04-text-diff/04-PATTERNS.md
@.planning/phases/04-text-diff/04-UI-SPEC.md
@.planning/phases/03-sql-formatter/03-01-SUMMARY.md
@.planning/phases/01-additive-tool-contract/01-01-SUMMARY.md
@src/lib/json.ts
@src/lib/sql.ts
@src/lib/sql.test.ts
@src/lib/limits.ts
@src/components/tools/WordCounter.tsx
@src/components/tools/PasswordGenerator.tsx
@src/components/tools/SqlFormatter.tsx
@src/components/ToolShell.tsx
@src/components/tools/ToolIsland.astro
@src/components/tools/ToolIsland.test.ts
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/i18n/errors.ts
@src/i18n/errors.test.ts
@src/content.config.ts
@src/content/tools/sql-formatter.md
@src/styles/global.css
@.planning/codebase/CONVENTIONS.md

<interfaces>
From src/lib/json.ts: JsonResult is ok-true with a payload or ok-false with error string; empty trim returns ok false and empty error before the engine. Clone this contract as diffText(original, changed, { ignoreWhitespace }) returning DiffResult with lines, added, removed, identical on success.

From diff@9.0.0 (package root only, no subpath): named export diffLines. Call diffLines(original, changed, { ignoreWhitespace, oneChangePerToken: true }). ChangeObject has value, added, removed, count. Do not install @types/diff. Do not call the two-file patch formatter, createPatch, diffWords, or diffChars. Do not pass newlineIsToken, stripTrailingCr, or a callback.

From HEAD src/components/tools/WordCounter.tsx (git show HEAD:src/components/tools/WordCounter.tsx): default export { locale }: { locale: Locale }; import { t, type Locale } from '../../i18n/ui'; copy = t(locale); labels = copy.tools[slug]; ToolShell error/output without a locale prop; class not className; textarea onInput HTMLTextAreaElement cast; spellcheck false. Do NOT reuse INPUT_TOO_LARGE_MSG.

From HEAD src/components/tools/PasswordGenerator.tsx: native checkbox inside label; onChange casts HTMLInputElement checked.

From HEAD src/components/tools/SqlFormatter.tsx: live useMemo, no Generate button.

From HEAD ToolShell (git show HEAD:src/components/ToolShell.tsx): props { error, output, children } — no locale. Copy is navigator.clipboard.writeText(props.output) when output is non-empty. Do not edit ToolShell.tsx. Do not pass locale into ToolShell.

From src/lib/limits.ts: INPUT_MAX_CHARS 100000; isTooLarge is input.length greater than that. Do not edit limits.ts. Size guard lives in the island, not the lib. Cap is per pane, not combined.

From src/data/tools.ts: append-only row; ToolCategory includes Text; do not edit the first ten objects' relatedSlugs (D-13).

From src/data/tools.test.ts: it has exactly 15 tools uses toHaveLength(15) — bump to 16; featured toHaveLength(6) stays; existsSync(URL) EN+ZH loop already covers new slugs.

From src/components/tools/ToolIsland.astro: static import plus slug === 'kebab' && Component client:load locale={locale}. Do not import the diff package here. Do not use dynamic component tags.

From src/content.config.ts: howTo tuple of exactly 3; faq min 3 max 5; locale en or zh. Keep locale so dirty content.config still builds.

From src/i18n/ui.ts: ui.en.tools and ui.zh.tools must share keys. Append tools['text-diff'] on both with UI-SPEC keys. Do not drop Phase 2/3 keys. Do not add a dummy ZH_ERRORS key.

From HEAD src/styles/global.css: .tool-grid.split already 1fr 1fr at 720px. Append only diff-line rules. Do not retokenize :root.

From CONVENTIONS 8-file checklist: catalog row featured false; lib+test; island; ToolIsland branch; ui.ts EN+ZH; EN md; ZH md. No lib barrel. Heavy package imported only from that tool's src/lib.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end text-diff — paste two panes through lib, island, catalog, and EN/ZH page</name>
  <precondition>Phase 3 completeness is green at catalog length 15: src/data/tools.test.ts snapshots toHaveLength(15), featured length 6, ToolIsland.test.ts source-reads slug-equals branches, and sql-formatter ZH markdown remains on disk</precondition>
  <reversibility rating="costly">Public catalog slug text-diff plus direct diff@9.0.0 become the Text-tool contract; swapping the package later is a coordinated lib, island, FAQ, CSS, and bundle-isolation change.</reversibility>
  <files>package.json, package-lock.json, src/lib/diff.test.ts, src/lib/diff.ts, src/components/tools/TextDiff.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/content/tools/text-diff.md, src/content/tools/zh/text-diff.md, src/styles/global.css</files>
  <read_first>src/lib/json.ts, src/lib/sql.ts, src/lib/sql.test.ts, src/components/tools/WordCounter.tsx, src/components/tools/PasswordGenerator.tsx, src/components/tools/SqlFormatter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/content/tools/sql-formatter.md, src/content.config.ts, src/lib/limits.ts, src/styles/global.css, .planning/phases/04-text-diff/04-RESEARCH.md, .planning/phases/04-text-diff/04-PATTERNS.md, .planning/phases/04-text-diff/04-UI-SPEC.md, .planning/codebase/CONVENTIONS.md</read_first>
  <behavior>
    - diffText('', '', { ignoreWhitespace: false }) and diffText('   ', '\n', { ignoreWhitespace: false }) equal { ok: false, error: '' } and must not treat empty-empty engine output as identical
    - diffText('hello\n', 'hello\n', { ignoreWhitespace: false }) is ok true, identical true, added 0, removed 0
    - diffText('a\nb\n', 'a\nB\n', { ignoreWhitespace: false }) is ok true, identical false, has one del of b and one add of B
    - npm test green at TOOLS length 16 and featured 6 with text-diff catalog row featured false
  </behavior>
  <action>
Land the thinnest production text-diff path through every layer this phase touches (DIFF-01, DIFF-02, DIFF-03, DIFF-04, DIFF-05, DIFF-06, CAT-01, CAT-02, CAT-03, CAT-06). Official package diff@9.0.0 only. Thin src/lib/diff.ts — do not call the npm package from the island or ToolIsland. Do not edit ToolShell.tsx, limits.ts, json.ts, JsonFormatter.tsx, errors.ts, or the existing ten TOOLS relatedSlugs (D-13, CONTEXT catalog additive). Do not create a lib barrel. Do not add @types/diff. Do not import patch APIs or word/char differ. Stage only this plan's files_modified; leave unrelated dirty worktree files alone. Do not commit unrelated dirty i18n/pages. Do not pop any existing git stash. Clone HEAD WordCounter locale/t() wiring (git show HEAD:src/components/tools/WordCounter.tsx). Clone HEAD PasswordGenerator checkbox-in-label. Clone HEAD SqlFormatter live useMemo. Clone HEAD ToolShell (no locale prop). Do not clone dirty worktree JsonFormatter or dirty ToolShell (missing useToolUi / extra locale prop).

1. npm install diff@9.0.0 so package.json and the lockfile record the direct dependency. Confirm the resolved version is 9.0.0. Package legitimacy is Approved in 04-RESEARCH; no human checkpoint.

2. Create src/lib/diff.test.ts then src/lib/diff.ts (tests first). Named exports only. Export DiffLineKind add|del|eq, DiffLine { kind, text }, DiffResult ok-true with lines, added, removed, identical or ok-false with error string. Export diffText(original, changed, { ignoreWhitespace }). If both original.trim() and changed.trim() are empty, return ok false and empty error BEFORE calling the engine (the library returns an empty array for both-empty, which would look like No differences). Otherwise call named diffLines from the package root with ignoreWhitespace from the options object and oneChangePerToken true. Map each change object to one DiffLine: added → add, removed → del, else eq. Increment added/removed by one per mapped row (not per coalesced hunk). Strip a single trailing newline from change.value when storing text. Return identical true when added and removed are both 0. Never throw. Do not return a non-empty English error (size strings live in ui.ts). Do not pre-trim lines in the wrapper (library trims only for equality; displayed text stays the raw value).

3. Append one TOOLS row only (CAT-01, CAT-05, D-13, CONTEXT catalog): slug text-diff, name Text Diff, category Text, shortDescription Compare two texts line by line in your browser., relatedSlugs word-counter, case-converter, json-formatter in that order, featured false. In src/data/tools.test.ts change the snapshot from 15 to 16 and rename that it() to has exactly 16 tools. Do not change featured length 6. Do not edit the first ten objects' relatedSlugs.

4. Add ui.en.tools['text-diff'] and ui.zh.tools['text-diff'] with shared keys verbatim from 04-UI-SPEC Copywriting Contract: name, shortDescription, original, changed, added, removed, ignoreWhitespace, noDifferences, emptyHeading, emptyBody, noDifferencesBody, tooLargeOriginal, tooLargeChanged, tooLargeBoth. EN/ZH strings must match the UI-SPEC table exactly (including No differences / 无差异 and the three too-large variants). Do not drop Phase 2/3 keys. Do not put those too-large strings in ZH_ERRORS or reuse INPUT_TOO_LARGE_MSG.

5. Create src/components/tools/TextDiff.tsx: default export, locale prop, useState original, changed, ignoreWhitespace default false. t(locale), labels from copy.tools['text-diff']. useMemo: if isTooLarge(original) or isTooLarge(changed), pick labels.tooLargeBoth / tooLargeOriginal / tooLargeChanged, output empty, view error, do not call diffText (DIFF-06). Else diffText(original, changed, { ignoreWhitespace }). Lib not-ok → idle (error null, output empty, view idle). Identical → output labels.noDifferences, view identical, stats from the result (DIFF-05). Else output a labeled copy payload built in the island: first line labels.added colon space n, second line labels.removed colon space n, blank line, then one body line per DiffLine with prefix minus-space / plus-space / two spaces (CONTEXT copy payload; not a patch file). useMemo deps original, changed, ignoreWhitespace, labels. Render ToolShell error/output with children in UI-SPEC order: (1) stats row of two tool-card tiles hidden when idle, visible including 0/0 whenever at least one pane has characters, display {n} {added} and {n} {removed} integers never the phrase lines added; (2) checkbox inside label, labels.ignoreWhitespace, default unchecked (DIFF-03, CONTEXT whitespace); (3) div.tool-grid.split with two textareas labeled original / changed, rows matching existing 10rem min-height, spellcheck false, onInput casts HTMLTextAreaElement (DIFF-01, CONTEXT two-pane); (4) visual area: idle uses emptyHeading plus emptyBody; identical uses noDifferences plus noDifferencesBody; else ol.diff-lines with one li per line, class diff-line plus diff-line--kind, prefix span aria-hidden true with + / - / two spaces, line.text as a text child (DIFF-02). Never assign HTML strings into the DOM. Never pass locale into ToolShell. class not className. Import diffText from ../../lib/diff — do not import the npm package from this file.

6. In ToolIsland.astro add a static import of TextDiff and a branch slug === 'text-diff' with client:load and locale={locale}. Keep existing branches. Do not import the npm package in ToolIsland.astro.

7. Append only to src/styles/global.css (CONTEXT highlight via CSS classes): .diff-lines max-height 384px, overflow-y auto, overflow-x auto, list-style none; .diff-line white-space pre-wrap overflow-wrap anywhere, 3px left border; .diff-line--add color #3dd68c background #13291f; .diff-line--del color #f07178. Do not use the accent token for add or delete. Do not retokenize :root. If the worktree CSS has unrelated dirty visual edits, add only this block (prefer appending onto HEAD rules for .tool-grid.split).

8. Add src/content/tools/text-diff.md locale en and src/content/tools/zh/text-diff.md locale zh. Schema: title, description, intro, howTo exactly 3, faq 3 items. EN FAQ must state comparison runs in the browser and nothing is uploaded, and that ignore leading/trailing whitespace trims line edges without collapsing internal spaces, and that Copy is a labeled summary not a patch download. ZH FAQ must state local/no-upload. Discretion on remaining wording. Do not mention later-phase tools.

No Playwright. No tsx tests.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green with TOOLS length 16 and featured 6; text-diff catalog row featured false; ToolIsland contains slug === 'text-diff'; EN+ZH markdown exist; diffText idle empty error before the engine; identical non-empty is identical true; TextDiff uses per-pane isTooLarge, diffText, checkbox default off, ol.diff-lines, and ToolShell labeled copy payload; existing ten relatedSlugs unchanged; diff@9.0.0 is a direct dependency.</done>
  <acceptance_criteria>
    - package.json lists diff at 9.0.0 as a direct dependency; no @types/diff
    - src/lib/diff.ts exports diffText; both-empty/whitespace is ok false empty error; hello vs hello is identical true; a/b vs a/B yields del and add rows
    - TextDiff.tsx default-exports, calls isTooLarge on original and on changed then diffText, live useMemo, ToolShell output is the labeled summary, visual list is children
    - TOOLS includes text-diff category Text featured false; tools.test.ts toHaveLength 16; getFeaturedTools still length 6
    - ToolIsland.astro static-imports TextDiff and has slug === 'text-diff' with locale={locale}
    - EN and ZH markdown exist with howTo length 3 and faq 3-5 including local / nothing uploaded
    - Existing ten TOOLS objects' relatedSlugs bytes unchanged; no lib barrel file created
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Ignore-whitespace, one-row-per-line, chrome keys, too-large source-read, and FAQ lock</name>
  <files>src/lib/diff.ts, src/lib/diff.test.ts, src/i18n/errors.test.ts, src/components/tools/TextDiff.tsx, src/content/tools/text-diff.md, src/content/tools/zh/text-diff.md</files>
  <read_first>src/lib/diff.ts, src/lib/diff.test.ts, src/i18n/errors.test.ts, src/i18n/ui.ts, src/components/tools/TextDiff.tsx, src/content/tools/text-diff.md, src/content/tools/zh/text-diff.md, src/lib/sql.test.ts, .planning/phases/04-text-diff/04-RESEARCH.md, .planning/phases/04-text-diff/04-UI-SPEC.md</read_first>
  <behavior>
    - diffText('foo \n', 'foo\n', { ignoreWhitespace: true }) is ok true, identical true
    - diffText('foo  bar\n', 'foo bar\n', { ignoreWhitespace: true }) is ok true, identical false (internal spaces still differ)
    - diffText('a\nb\nc\n', 'a\nX\nY\nc\n', { ignoreWhitespace: false }) yields two consecutive kind add rows (one row per line, not one coalesced hunk)
    - diffText('', 'x\ny\n', { ignoreWhitespace: false }) is all add; diffText('x\ny\n', '', { ignoreWhitespace: false }) is all del
    - ui.en.tools['text-diff'] and ui.zh.tools['text-diff'] share keys name, shortDescription, original, changed, added, removed, ignoreWhitespace, noDifferences, emptyHeading, emptyBody, noDifferencesBody, tooLargeOriginal, tooLargeChanged, tooLargeBoth
    - TextDiff.tsx source contains isTooLarge(original) and isTooLarge(changed) and does not contain INPUT_TOO_LARGE_MSG
    - EN markdown faq answers mention browser / nothing uploaded; ZH markdown faq answers mention local or 不会上传
  </behavior>
  <action>
Expand the proven lib and island into DIFF-02 one-row-per-line, DIFF-03 trim-vs-internal-spaces, DIFF-04 line counts, CAT-06 chrome-key tests, DIFF-06 island source-read, and local-only FAQ source-read without changing architecture. Do not add packages. Do not edit ToolIsland.astro, tools.ts, JsonFormatter.tsx, errors.ts, or ToolShell. Keep idle whole-string-trim-before-engine from the tracer. Keep oneChangePerToken true so consecutive adds stay two rows.

1. Extend src/lib/diff.test.ts with ignoreWhitespace trim equality (foo trailing-space vs foo) versus internal-space non-equality (foo two-spaces bar vs foo one-space bar); one-empty original → all del; one-empty changed → all add; two consecutive added lines produce two kind add rows; added/removed count lines not hunks. Read files with readFileSync(new URL(..., import.meta.url)) never URL.pathname.

2. Add a text-diff describe in src/i18n/errors.test.ts that asserts the chrome keys listed in behavior on ui.en.tools['text-diff'] and ui.zh.tools['text-diff']. Do not add a dummy ZH_ERRORS key. Keep existing lorem-ipsum, password-generator, and sql-formatter describes.

3. Source-read TextDiff.tsx for isTooLarge(original) and isTooLarge(changed) and for the absence of INPUT_TOO_LARGE_MSG. Source-read both markdown files so EN faq includes local/no-upload wording and ZH faq includes 不会上传 or equivalent local-only wording. howTo remains exactly 3. faq length 3–5.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>Ignore-ws trim equals and internal spaces still differ; consecutive adds are two rows; one-empty is all add or all del; chrome keys locked on en+zh; island source-reads per-pane isTooLarge; FAQ local-only locked; npm test green.</done>
  <acceptance_criteria>
    - ignoreWhitespace true treats leading/trailing-only edits as identical and does not treat internal-space-only edits as identical
    - two consecutive added lines produce two add rows and added === 2
    - errors.test.ts shares the UI-SPEC chrome keys on en and zh without dropping Phase 2/3 describes
    - TextDiff.tsx contains isTooLarge(original) and isTooLarge(changed)
    - EN+ZH markdown FAQ include local / nothing uploaded
  </acceptance_criteria>
</task>

<task type="auto">
  <name>CAT-04 isolation — only diff.ts imports the package; json-formatter chunk stays clean after build</name>
  <files>src/lib/diff.test.ts, src/lib/diff.ts</files>
  <read_first>src/lib/diff.ts, src/lib/diff.test.ts, src/components/tools/TextDiff.tsx, src/components/tools/ToolIsland.astro, src/lib/json.ts, src/components/tools/JsonFormatter.tsx, package.json, .planning/phases/04-text-diff/04-RESEARCH.md, .planning/phases/03-sql-formatter/03-01-SUMMARY.md</read_first>
  <action>
Prove CAT-04: the diff package is imported only from src/lib/diff.ts, TextDiff imports ../../lib/diff, ToolIsland does not import the package, json.ts and JsonFormatter.tsx do not import it, completeness snapshot stays 16 with featured 6, and after a fresh astro build the json-formatter island chunk does not contain minify-surviving diff identifiers (A1). Do not add a lib barrel. Do not edit JsonFormatter.tsx, json.ts, ToolShell, dirty pages, or dirty i18n modules. Do not import the npm package from ToolIsland.astro. Do not trust a stale dist/_astro listing — always rebuild. Do not grep jsdiff or the minified-away function names as proof of isolation (04-RESEARCH pitfall 4).

1. In src/lib/diff.test.ts add a source-isolation test cloned from sql.test.ts: read diff.ts, TextDiff.tsx, ToolIsland.astro, json.ts, and JsonFormatter.tsx with readFileSync(new URL(...), utf8). Assert diff.ts contains the package specifier as a from-clause of diff. Assert the other four files do not contain that specifier. Assert TextDiff.tsx contains diffText and ../../lib/diff.

2. Run a fresh npm run build then check every dist/_astro/JsonFormatter*.js file: none may contain oneChangePerToken, newlineIsToken, stripTrailingCr, ignoreNewlineAtEof, or createTwoFilesPatch. If no JsonFormatter js chunk exists, that is a failure (do not pass on a missing chunk). If npm run build fails because of uncommitted page or i18n files that are not in this plan's files_modified (Phase 3 dirty-tree pattern), copy those failing files aside, restore them from HEAD, rebuild, run the identifier check, then restore the aside copies. Do not rewrite those dirty files this phase. Do not pop any existing git stash. If the json-formatter chunk leaks, keep architecture: move the package load to a dynamic import inside diffText of the same named diffLines from the package root (still only inside diff.ts), rebuild, and re-check. Do not import the package from ToolIsland.astro or a barrel to fix the leak.
  </action>
  <verify>
    <automated>npm test && npm run build && node --input-type=module -e "import fs from 'node:fs'; import path from 'node:path'; const dir='dist/_astro'; if (!fs.existsSync(dir)) { console.error('missing dist/_astro'); process.exit(1); } const files = fs.readdirSync(dir).filter((f) => f.startsWith('JsonFormatter') && f.endsWith('.js')); if (!files.length) { console.error('no JsonFormatter js chunk'); process.exit(1); } let bad = false; for (const f of files) { const t = fs.readFileSync(path.join(dir, f), 'utf8'); if (t.includes('oneChangePerToken') || t.includes('newlineIsToken') || t.includes('stripTrailingCr') || t.includes('ignoreNewlineAtEof') || t.includes('createTwoFilesPatch')) { console.error('CAT-04 leak in ' + f); bad = true; } } process.exit(bad ? 1 : 0);"</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary, or output contains "CAT-04 leak", or output contains "missing dist/_astro", or output contains "no JsonFormatter js chunk"</fails_when>
    <human-check>Open /tools/text-diff/ (and /zh/tools/text-diff/ if that tree is being served): two panes Original/Changed (or 原文/改后); paste differing lines and see per-line + / - rows not only a pre dump; toggle Ignore leading/trailing whitespace; identical texts show No differences / 无差异; Copy writes Added/Removed labeled summary; oversize one pane shows the matching too-large string; homepage featured count still six.</human-check>
  </verify>
  <done>npm test green; only diff.ts imports the diff package; fresh astro build; JsonFormatter island chunk has none of the five minify-surviving identifiers; featured still 6; length 16.</done>
  <acceptance_criteria>
    - diff.test.ts isolation test: diff.ts has the package specifier; TextDiff.tsx, ToolIsland.astro, json.ts, JsonFormatter.tsx do not
    - TextDiff.tsx imports diffText from ../../lib/diff
    - Fresh npm run build succeeds (with dirty-page isolation if needed, without rewriting those pages)
    - Every dist/_astro/JsonFormatter*.js file exists after build and contains none of oneChangePerToken, newlineIsToken, stripTrailingCr, ignoreNewlineAtEof, createTwoFilesPatch
    - No src/lib barrel file exists
  </acceptance_criteria>
</task>

</tasks>

<!-- planner-discipline-allow: oneChangePerToken -->
<!-- planner-discipline-allow: newlineIsToken -->
<!-- planner-discipline-allow: stripTrailingCr -->
<!-- planner-discipline-allow: ignoreNewlineAtEof -->
<!-- planner-discipline-allow: createTwoFilesPatch -->
<!-- planner-discipline-allow: INPUT_TOO_LARGE_MSG -->
<!-- planner-discipline-allow: from 'diff' -->

## Artifacts this phase produces

Slice symbols and paths created by this plan:

- `diffText`, `DiffResult`, `DiffLine` in `src/lib/diff.ts`
- `TextDiff` default export in `src/components/tools/TextDiff.tsx`
- Catalog slug `text-diff` in `TOOLS`
- `ui.en.tools['text-diff']` and `ui.zh.tools['text-diff']`
- `src/content/tools/text-diff.md` and `src/content/tools/zh/text-diff.md`
- Additive `.diff-lines` rules in `src/styles/global.css`
- Direct dependency `diff@9.0.0`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor paste + checkbox → TextDiff island | Untrusted textarea text and boolean toggle cross into in-browser lib only |
| island → visual list + ToolShell output | User text rendered as Preact text children; Copy uses clipboard |
| diff.ts → npm diff | Third-party Myers line differ runs in the visitor browser; no server |
| static SSG → browser | No tool API route; no fetch in this slice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | TextDiff.tsx line list | high | mitigate | Render each line as a classed element plus text child; never assign HTML strings into the DOM; never convertChangesToXML |
| T-04-02 | Denial of service | TextDiff.tsx / isTooLarge | medium | mitigate | Call isTooLarge on original and on changed independently before diffText; INPUT_MAX_CHARS stays 100000 in limits.ts (do not edit); do not run the engine when either pane is over cap |
| T-04-03 | Information disclosure | island / FAQ | medium | mitigate | No fetch and no tool API route; FAQ states nothing is uploaded; session-only component state; do not write localStorage |
| T-04-04 | Denial of service | src/lib/diff.ts | medium | mitigate | Import only named diffLines; do not import patch parsers or patch formatters |
| T-04-05 | Tampering | diffText options | low | accept | Pass a fresh { ignoreWhitespace, oneChangePerToken: true } object; do not spread user JSON into library options; public pretty-printer has no secrets |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Direct install is diff@9.0.0 only (04-RESEARCH Approved, no postinstall, no runtime deps). Do not install @types/diff. No [SLOP] packages. No blocking human checkpoint (seam SUS was null metadata, contradicted by npm view + tarball). |

</threat_model>

<verification>
Inherit Phase 3 `npm test` after tasks 1–2. Task 3 adds `npm run build` plus a Node read of dist/_astro/JsonFormatter*.js that fails on missing chunk or CAT-04 leak (the five minify-surviving identifiers). Completeness: ToolIsland slug-equals loop and EN+ZH existsSync must pass at length 16. Featured length 6. diff.test.ts covers idle, add/delete, ignore-ws, identical, one-row-per-line, and import isolation. Do not run Playwright. Do not trust stale dist/_astro from before this package. If dirty pages break the build, isolate without rewriting them.
</verification>

<success_criteria>
- DIFF-01..06 delivered for slug text-diff at 8-file parity
- Named diffLines with ignoreWhitespace and oneChangePerToken; idle before the engine
- Per-line visual list plus labeled copy payload; No differences for identical non-empty
- Per-pane isTooLarge with ui.ts strings; FAQ local / nothing uploaded in EN+ZH
- Catalog 16, featured 6, existing ten relatedSlugs untouched (D-13)
- CAT-04: only diff.ts imports the package; json-formatter chunk clean after fresh build
- npm test green and npm run build CAT-04 grep green
</success_criteria>

<output>
Create `.planning/phases/04-text-diff/04-01-SUMMARY.md` when done
</output>
