---
phase: 03-sql-formatter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/sql.ts
  - src/lib/sql.test.ts
  - src/components/tools/SqlFormatter.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/i18n/errors.ts
  - src/i18n/errors.test.ts
  - src/content/tools/sql-formatter.md
  - src/content/tools/zh/sql-formatter.md
autonomous: true
requirements:
  - SQL-01
  - SQL-02
  - SQL-03
  - SQL-04
  - SQL-05
  - SQL-06

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can open /tools/sql-formatter/ (and /zh/tools/sql-formatter/), paste SQL, and see pretty-printed output update as they type (SQL-01)"
    - "Native select lists Standard SQL, PostgreSQL, MySQL, SQLite, T-SQL, and BigQuery; default is Standard (sql) each load; changing dialect reformats immediately; lib uses named dialect objects sql, postgresql, mysql, sqlite, transactsql, bigquery — T-SQL import is transactsql (SQL-02)"
    - "Valid SQL keywords are UPPERCASE and indent is two spaces because every format call passes keywordCase upper and tabWidth 2 (SQL-03)"
    - "ToolShell Copy writes the formatted SQL string via existing clipboard chrome (SQL-04)"
    - "Garbage or unclosed-quote SQL returns { ok: false, error: 'Invalid SQL' } mapped in ZH_ERRORS to 无效的 SQL; empty/whitespace is idle { ok: false, error: '' } before the library runs (SQL-05)"
    - "EN and ZH FAQ state the tool is not an executor and dialect is not autodetection (SQL-06)"
    - "Catalog slug sql-formatter, category Format, featured false; TOOLS toHaveLength 15; getFeaturedTools stays 6; relatedSlugs json-formatter, regex-tester, base64; existing ten relatedSlugs unchanged (CAT-01, CAT-05)"
    - "After astro build, dist/_astro/JsonFormatter*.js does not contain SQL parser identifiers; only src/lib/sql.ts imports the sql-formatter package (CAT-04)"
  artifacts:
    - path: src/lib/sql.ts
      provides: "formatSql plus SqlDialect and SqlResult; thin wrap of named dialect objects"
      exports: ["formatSql"]
    - path: src/lib/sql.test.ts
      provides: "idle, pretty-print upper/indent, Invalid SQL throw fixtures, dialect smokes, source isolation"
    - path: src/components/tools/SqlFormatter.tsx
      provides: "Default-export island; locale from ui.ts; native dialect select; isTooLarge; live useMemo; ToolShell copy"
    - path: src/components/tools/ToolIsland.astro
      provides: "static SqlFormatter import and slug === 'sql-formatter' client:load locale={locale}"
    - path: src/data/tools.ts
      provides: "sql-formatter catalog row featured false category Format"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 15; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['sql-formatter'] chrome keys name shortDescription sql dialect"
    - path: src/i18n/errors.ts
      provides: "ZH_ERRORS Invalid SQL to 无效的 SQL"
    - path: src/content/tools/sql-formatter.md
      provides: "locale en SEO howTo 3 faq 3-5 including not-executor and not-autodetect"
    - path: src/content/tools/zh/sql-formatter.md
      provides: "locale zh SEO howTo 3 faq with 不是执行器 and 方言不是自动检测"
    - path: package.json
      provides: "direct dependency sql-formatter 15.8.2"
  key_links:
    - from: src/components/tools/SqlFormatter.tsx
      to: src/lib/sql.ts
      via: "formatSql after isTooLarge"
      pattern: "formatSql"
    - from: src/lib/sql.ts
      to: sql-formatter
      via: "named dialect-aware formatter plus six dialect objects"
      pattern: "formatDialect"
    - from: src/components/tools/SqlFormatter.tsx
      to: src/lib/limits.ts
      via: "isTooLarge(input) before formatSql"
      pattern: "isTooLarge"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/SqlFormatter.tsx
      via: "static import and slug === 'sql-formatter' client:load"
      pattern: "slug === 'sql-formatter'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(15)"
      pattern: "toHaveLength\\(15\\)"
    - from: src/i18n/errors.ts
      to: formatSql errors
      via: "ZH_ERRORS same-slice key"
      pattern: "Invalid SQL"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** paste SQL and choose an explicit dialect to pretty-print it in the browser, **so that** I get copyable UPPERCASE-keyword SQL without executing the query or uploading anything.

<objective>
Ship the sql-formatter 8-file vertical slice at existing-tool parity (SQL-01, SQL-02, SQL-03, SQL-04, SQL-05, SQL-06) plus CAT-04 bundle isolation. Install official sql-formatter@15.8.2. Thin src/lib/sql.ts wrapping named dialect-aware formatter and six dialect objects (sql, postgresql, mysql, sqlite, transactsql, bigquery). Never the string-language convenience wrapper. Default Standard SQL; native select; live useMemo. keywordCase upper and tabWidth 2 on every call. Invalid SQL → English Invalid SQL plus ZH_ERRORS 无效的 SQL. Empty trim idle before the library. Island clones WordCounter locale/INPUT_TOO_LARGE_MSG, HEAD HashGenerator native select, JsonFormatter live memo — not dirty worktree JsonFormatter/HashGenerator that import missing useToolUi. Catalog slug sql-formatter, Format, featured false; TOOLS 14 → 15; featured stays 6; relatedSlugs json-formatter, regex-tester, base64; do not rewrite existing ten relatedSlugs. FAQ: not an executor; dialect is not autodetection. No lib barrel. After astro build, json-formatter chunk must not inherit the SQL parser.
Purpose: First heavy-library catalog tool; visitors pretty-print SQL in-browser without execution.
Output: sql lib/test, SqlFormatter island, ToolIsland branch, catalog row, ui.ts, ZH_ERRORS, EN+ZH markdown, sql-formatter@15.8.2, CAT-04 build grep green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sql-formatter/03-CONTEXT.md
@.planning/phases/03-sql-formatter/03-RESEARCH.md
@.planning/phases/03-sql-formatter/03-PATTERNS.md
@.planning/phases/02-light-text-and-generate-tools/02-04-SUMMARY.md
@.planning/phases/01-additive-tool-contract/01-01-SUMMARY.md
@src/lib/json.ts
@src/lib/json.test.ts
@src/lib/limits.ts
@src/components/tools/WordCounter.tsx
@src/components/ToolShell.tsx
@src/components/tools/ToolIsland.astro
@src/components/tools/ToolIsland.test.ts
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/i18n/errors.ts
@src/i18n/errors.test.ts
@src/content.config.ts
@src/content/tools/json-formatter.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
From src/lib/json.ts: JsonResult is ok-true with formatted or ok-false with error string; empty trim returns ok false and empty error; empty catch maps to Invalid JSON; named export formatJson. Clone this contract as formatSql(input, dialect) returning SqlResult with formatted on success and Invalid SQL on throw.

From sql-formatter@15.8.2 (package root only, no subpath): named exports formatDialect, sql, postgresql, mysql, sqlite, transactsql, bigquery. T-SQL object is transactsql — there is no tsql ESM export. Call formatDialect(trimmed, { dialect: DIALECTS[dialect], keywordCase: 'upper', tabWidth: 2 }). Library default keywordCase is preserve — omitting upper fails SQL-03. Do not install @types/sql-formatter.

From src/components/tools/WordCounter.tsx (clone locale wiring): default export { locale }: { locale: Locale }; import { t, type Locale } from '../../i18n/ui'; copy = t(locale); labels = copy.tools[slug]; isTooLarge first with INPUT_TOO_LARGE_MSG; ToolShell error/output without a locale prop; class not className; textarea onInput HTMLTextAreaElement cast; spellcheck false.

From git HEAD HashGenerator (native select analog — use git show HEAD:src/components/tools/HashGenerator.tsx, not the dirty worktree file): select value plus onChange casting HTMLSelectElement.

From HEAD JsonFormatter live path (do not clone dirty worktree JsonFormatter.tsx): useMemo over input calling the lib after isTooLarge; error is null on ok, r.error or null on failure so idle empty error shows no alert; output is formatted or empty.

From HEAD ToolShell: props { error, output, children } — no locale. Copy is navigator.clipboard.writeText(props.output) when output is non-empty. Do not edit ToolShell.tsx.

From src/lib/limits.ts: INPUT_MAX_CHARS 100000; isTooLarge is input.length greater than that; INPUT_TOO_LARGE_MSG English. Do not edit limits.ts. Size guard lives in the island, not the lib.

From src/i18n/errors.ts: localizeError(locale, error); append Invalid SQL → 无效的 SQL; do not drop Phase 2 keys.

From src/data/tools.ts: append-only row; ToolCategory includes Format; do not edit the first ten objects' relatedSlugs.

From src/data/tools.test.ts: it has exactly 14 tools uses toHaveLength(14) — bump to 15; featured toHaveLength(6) stays; existsSync(URL) EN+ZH loop already covers new slugs.

From src/components/tools/ToolIsland.astro: static import plus slug === 'kebab' && Component client:load locale={locale} for Phase 2 islands. Existing ten branches stay without locale. Do not import the SQL package here. Do not use dynamic component tags.

From src/content.config.ts: howTo tuple of exactly 3; faq min 3 max 5; locale en or zh.

From src/i18n/ui.ts: ui.en.tools and ui.zh.tools must share keys. Append tools['sql-formatter'] on both. Do not drop Phase 2 keys.

From CONVENTIONS 8-file checklist: catalog row featured false; lib+test; island; ToolIsland branch; ui.ts EN+ZH; ZH_ERRORS for new English errors; EN md; ZH md. No lib barrel. Heavy package imported only from that tool's src/lib.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end sql-formatter — paste Standard SQL through lib, island, catalog, and EN/ZH page</name>
  <precondition>Phase 2 completeness is green at catalog length 14: src/data/tools.test.ts snapshots toHaveLength(14), featured length 6, ToolIsland.test.ts source-reads slug-equals branches, and Phase 2 ZH markdown remains on disk</precondition>
  <reversibility rating="costly">Public catalog slug sql-formatter plus direct sql-formatter@15.8.2 become the Format-tool contract; swapping the package later is a coordinated lib, island, FAQ, and bundle-isolation change.</reversibility>
  <files>package.json, package-lock.json, src/lib/sql.test.ts, src/lib/sql.ts, src/components/tools/SqlFormatter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/sql-formatter.md, src/content/tools/zh/sql-formatter.md</files>
  <read_first>src/lib/json.ts, src/lib/json.test.ts, src/components/tools/WordCounter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/json-formatter.md, src/content.config.ts, src/lib/limits.ts, .planning/phases/03-sql-formatter/03-RESEARCH.md, .planning/phases/03-sql-formatter/03-PATTERNS.md, .planning/codebase/CONVENTIONS.md</read_first>
  <behavior>
    - formatSql('', 'sql') and formatSql('   ', 'sql') equal { ok: false, error: '' } and must not call the package (idle before library)
    - formatSql('select * from tbl where id = 1', 'sql') is ok true; formatted contains SELECT, FROM, WHERE, a newline, and a two-space indent before *
    - npm test green at TOOLS length 15 and featured 6 with sql-formatter catalog row featured false
  </behavior>
  <action>
Land the thinnest production sql-formatter path through every layer this phase touches (SQL-01, SQL-02, SQL-03, SQL-04, CAT-01, CAT-02, CAT-03, CAT-06). Official package sql-formatter@15.8.2 only. Thin src/lib/sql.ts — do not call the package from the island or ToolIsland. Do not edit ToolShell.tsx, limits.ts, json.ts, JsonFormatter.tsx, HashGenerator.tsx, or the existing ten TOOLS relatedSlugs (D-13). Do not create a lib barrel. Do not add @types/sql-formatter. Do not import the parser-generator transitive or the CLI parser package. Stage only this plan's files_modified; leave unrelated dirty worktree files alone. Clone WordCounter.tsx locale wiring plus INPUT_TOO_LARGE_MSG. For the native select analog run git show HEAD:src/components/tools/HashGenerator.tsx — do not clone the dirty worktree HashGenerator or JsonFormatter (they import missing useToolUi / i18n/locales). Live reformat via useMemo like HEAD JsonFormatter.

1. npm install sql-formatter@15.8.2 so package.json and the lockfile record the direct dependency. Confirm the resolved version is 15.8.2. Package legitimacy is OK; no human checkpoint.

2. Create src/lib/sql.test.ts then src/lib/sql.ts (tests first). Named exports only. Export type SqlDialect as the six keys sql, postgresql, mysql, sqlite, transactsql, bigquery. Export type SqlResult as ok true plus formatted string or ok false plus error string. Export formatSql(input, dialect). Trim first; if empty return ok false and empty error BEFORE importing-call into the package (the package returns empty string for empty input, which would look like success). Otherwise try the named dialect-aware formatter from 'sql-formatter' with dialect object DIALECTS[dialect], keywordCase upper, and tabWidth 2; on success return ok true formatted; on any throw return ok false error Invalid SQL — never surface the parser dump. Import formatDialect plus sql, postgresql, mysql, sqlite, transactsql, bigquery from the package root. T-SQL import name is transactsql. Do not use the string-language convenience wrapper. Always pass keywordCase upper (package default is preserve).

3. Append one TOOLS row only (CAT-01, CAT-05, D-13): slug sql-formatter, name SQL Formatter, category Format, shortDescription Pretty-print SQL in your browser., relatedSlugs json-formatter, regex-tester, base64, featured false. In src/data/tools.test.ts change the snapshot from 14 to 15 and rename that it() to has exactly 15 tools. Do not change featured length 6. Do not edit the first ten objects' relatedSlugs.

4. Create src/components/tools/SqlFormatter.tsx: default export, locale prop, useState input and dialect default sql, t(locale), labels from copy.tools['sql-formatter'], isTooLarge first yielding INPUT_TOO_LARGE_MSG and empty output, else formatSql(input, dialect) with localizeError(locale, r.error || null) and formatted as ToolShell output (SQL-04). Native select with options Standard SQL (sql), PostgreSQL, MySQL, SQLite, T-SQL (transactsql), BigQuery. onChange casts HTMLSelectElement value as SqlDialect. Textarea labeled from labels.sql, rows 12, spellcheck false. useMemo deps include input, dialect, locale. class not className. Do not pass locale into ToolShell. Do not import the SQL package from this file — import formatSql from ../../lib/sql.

5. In ToolIsland.astro add a static import of SqlFormatter and a branch slug === 'sql-formatter' with client:load and locale={locale}. Keep existing branches. Do not import the SQL package in ToolIsland.astro.

6. Add ui.en.tools['sql-formatter'] and ui.zh.tools['sql-formatter'] with shared keys name, shortDescription, sql, dialect. Do not drop Phase 2 keys.

7. Append ZH_ERRORS 'Invalid SQL': '无效的 SQL' in src/i18n/errors.ts. Keep Phase 2 keys.

8. Add src/content/tools/sql-formatter.md locale en and src/content/tools/zh/sql-formatter.md locale zh. Schema: title, description, intro, howTo exactly 3, faq 3 items. EN FAQ must include: this tool does not execute queries (no database connection, nothing uploaded) AND dialect is not autodetection (default Standard SQL; pick the others explicitly). ZH FAQ must include 不是执行器 and 方言不是自动检测 (SQL-06). Discretion on remaining wording. No version-labeling language. Do not mention later-phase tools.

No Playwright. No tsx tests.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green with TOOLS length 15 and featured 6; sql-formatter catalog row featured false; ToolIsland contains slug === 'sql-formatter'; EN+ZH markdown exist; formatSql pretty-prints Standard SQL with UPPER keywords and 2-space indent; idle empty error; SqlFormatter uses isTooLarge, formatSql, native select default sql, and ToolShell; existing ten relatedSlugs unchanged; sql-formatter@15.8.2 is a direct dependency.</done>
  <acceptance_criteria>
    - package.json lists sql-formatter at 15.8.2 as a direct dependency; no @types/sql-formatter
    - src/lib/sql.ts exports formatSql; empty/whitespace is ok false empty error; valid select * from tbl where id = 1 is ok true with SELECT and two-space indent
    - SqlFormatter.tsx default-exports, calls isTooLarge then formatSql, native select default sql, live useMemo, ToolShell output is formatted SQL
    - TOOLS includes sql-formatter category Format featured false; tools.test.ts toHaveLength 15; getFeaturedTools still length 6
    - ToolIsland.astro static-imports SqlFormatter and has slug === 'sql-formatter' with locale={locale}
    - EN and ZH markdown exist with howTo length 3 and faq 3-5 including not-executor and not-autodetect
    - Existing ten TOOLS objects' relatedSlugs bytes unchanged; no lib barrel file created
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Dialect smokes, throw-catch Invalid SQL, ZH map, and FAQ source-read</name>
  <files>src/lib/sql.ts, src/lib/sql.test.ts, src/i18n/errors.ts, src/i18n/errors.test.ts, src/content/tools/sql-formatter.md, src/content/tools/zh/sql-formatter.md</files>
  <read_first>src/lib/sql.ts, src/lib/sql.test.ts, src/i18n/errors.ts, src/i18n/errors.test.ts, src/content/tools/sql-formatter.md, src/content/tools/zh/sql-formatter.md, src/lib/json.test.ts, .planning/phases/03-sql-formatter/03-RESEARCH.md</read_first>
  <behavior>
    - formatSql('not sql at all !!!', 'sql') equals { ok: false, error: 'Invalid SQL' }
    - formatSql("SELECT 'unterminated", 'sql') equals { ok: false, error: 'Invalid SQL' }
    - formatSql('SELECT * FROM', 'sql') is ok true (formatter not a linter — do not use this as an invalid fixture)
    - formatSql('SELECT TOP 1 * FROM t', 'transactsql').ok is true
    - formatSql('SELECT * FROM t RETURNING *', 'postgresql').ok is true
    - formatSql('SELECT `foo` FROM t', 'mysql').ok is true
    - formatSql('CREATE TABLE t (id INTEGER PRIMARY KEY AUTOINCREMENT)', 'sqlite').ok is true
    - formatSql('SELECT * EXCEPT (a) FROM t', 'bigquery').ok is true
    - readFileSync sql.ts contains formatDialect and the six dialect identifiers including transactsql; does not match \blanguage\s*:
    - ZH_ERRORS['Invalid SQL'] is 无效的 SQL; localizeError('zh', 'Invalid SQL') returns that string; localizeError('en', 'Invalid SQL') stays English
    - EN markdown faq answers mention not execute and not autodetect; ZH markdown faq answers mention 不是执行器 and 方言不是自动检测
  </behavior>
  <action>
Expand the proven lib into SQL-02 dialect coverage, SQL-05 throw-catch, CAT-06 ZH map tests, and SQL-06 FAQ source-read without changing architecture. Do not add packages. Do not edit ToolIsland.astro, tools.ts, SqlFormatter.tsx, or JsonFormatter.tsx. Do not treat incomplete-but-parseable SQL as invalid. Catch every library throw as Invalid SQL — do not special-case message text and never render the parser dump. Keep idle trim-before-library from the tracer.

1. Extend src/lib/sql.test.ts with the throwing fixtures not sql at all !!! and unclosed quote; dialect smokes for transactsql, postgresql, mysql, sqlite, bigquery; source-read of sql.ts via readFileSync(new URL('./sql.ts', import.meta.url)) asserting formatDialect plus transactsql are present and \blanguage\s*: is absent. Do not assert against SELECT * FROM as invalid.

2. Keep errors.ts mapping Invalid SQL → 无效的 SQL. Add a sql-formatter describe in src/i18n/errors.test.ts for that mapping and localizeError en/zh, plus shared ui.en/ui.zh tools['sql-formatter'] keys name, shortDescription, sql, dialect. Keep existing lorem-ipsum and password-generator describes.

3. Source-read both markdown files (readFileSync URL, never URL.pathname) so EN faq text includes execute-negation and autodetection-negation and ZH faq text includes 不是执行器 and 方言不是自动检测. howTo remains exactly 3. faq length 3–5.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>Throwing fixtures return Invalid SQL; six named dialects smoke ok true; sql.ts source-read uses formatDialect and transactsql without a language field; ZH_ERRORS and FAQ locked statements are tested; npm test green.</done>
  <acceptance_criteria>
    - Garbage and unclosed-quote inputs return ok false error Invalid SQL
    - Incomplete SELECT * FROM still formats (ok true)
    - transactsql, postgresql, mysql, sqlite, bigquery each have an ok-true smoke
    - sql.ts source contains formatDialect and transactsql and does not contain a language field
    - errors.test.ts maps Invalid SQL to 无效的 SQL without dropping Phase 2 keys
    - EN+ZH markdown FAQ include the two locked statements
  </acceptance_criteria>
</task>

<task type="auto">
  <name>CAT-04 isolation — only sql.ts imports the package; json-formatter chunk stays clean after build</name>
  <files>src/lib/sql.test.ts, src/lib/sql.ts</files>
  <read_first>src/lib/sql.ts, src/lib/sql.test.ts, src/components/tools/SqlFormatter.tsx, src/components/tools/ToolIsland.astro, src/lib/json.ts, src/components/tools/JsonFormatter.tsx, package.json, .planning/phases/03-sql-formatter/03-RESEARCH.md</read_first>
  <action>
Prove CAT-04: the SQL package is imported only from src/lib/sql.ts, SqlFormatter imports ../../lib/sql, ToolIsland does not import the package, json.ts and JsonFormatter.tsx do not import it, and after a fresh astro build the json-formatter island chunk does not contain SQL parser identifiers (A1). Do not add a lib barrel. Do not edit JsonFormatter.tsx, json.ts, or ToolShell. Do not import the parser-generator transitive or the CLI parser package from any app file. Do not trust a stale dist/_astro listing — always rebuild.

1. In src/lib/sql.test.ts add a source-isolation test: read sql.ts, SqlFormatter.tsx, ToolIsland.astro, json.ts, and JsonFormatter.tsx with readFileSync(new URL(...), utf8). Assert sql.ts contains the package specifier sql-formatter. Assert the other four files do not contain that specifier. Assert SqlFormatter.tsx contains formatSql and ../../lib/sql.

2. Run a fresh npm run build then check every dist/_astro/JsonFormatter*.js file: none may contain nearley or formatDialect. If no JsonFormatter js chunk exists, that is a failure (do not pass on a missing chunk). If the json-formatter chunk leaks, keep architecture: move the package load to a dynamic import inside formatSql of the same named dialect objects and dialect-aware formatter from the package root (still only inside sql.ts; still never the string-language convenience wrapper), rebuild, and re-check. Do not import the package from ToolIsland.astro or a barrel to “fix” the leak.
  </action>
  <verify>
    <automated>npm test && npm run build && node --input-type=module -e "import fs from 'node:fs'; import path from 'node:path'; const dir='dist/_astro'; if (!fs.existsSync(dir)) { console.error('missing dist/_astro'); process.exit(1); } const files = fs.readdirSync(dir).filter((f) => f.startsWith('JsonFormatter') && f.endsWith('.js')); if (!files.length) { console.error('no JsonFormatter js chunk'); process.exit(1); } let bad = false; for (const f of files) { const t = fs.readFileSync(path.join(dir, f), 'utf8'); if (t.includes('nearley') || t.includes('formatDialect')) { console.error('CAT-04 leak in ' + f); bad = true; } } process.exit(bad ? 1 : 0);"</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary, or output contains "CAT-04 leak", or output contains "missing dist/_astro", or output contains "no JsonFormatter js chunk"</fails_when>
    <human-check>Open /tools/sql-formatter/ (and /zh/tools/sql-formatter/ if running the site): paste select * from t; output is UPPERCASE keywords with 2-space indent; change dialect and see live reformat; Copy uses the formatted string; invalid garbage shows Invalid SQL (ZH: 无效的 SQL); homepage featured count still six.</human-check>
  </verify>
  <done>npm test green; only sql.ts imports the SQL package; fresh astro build; JsonFormatter island chunk has no nearley and no formatDialect; featured still 6; length 15.</done>
  <acceptance_criteria>
    - sql.test.ts isolation test: sql.ts has the package specifier; SqlFormatter.tsx, ToolIsland.astro, json.ts, JsonFormatter.tsx do not
    - SqlFormatter.tsx imports formatSql from ../../lib/sql
    - Fresh npm run build succeeds
    - Every dist/_astro/JsonFormatter*.js file exists after build and contains neither nearley nor formatDialect
    - No src/lib barrel file exists
  </acceptance_criteria>
</task>

</tasks>

<!-- planner-discipline-allow: formatDialect -->
<!-- planner-discipline-allow: nearley -->
<!-- planner-discipline-allow: language: -->

## Artifacts this phase produces

Slice symbols and paths created by this plan:

- `formatSql`, `SqlDialect`, `SqlResult` in `src/lib/sql.ts`
- `SqlFormatter` default export in `src/components/tools/SqlFormatter.tsx`
- Catalog slug `sql-formatter` in `TOOLS`
- `ui.en.tools['sql-formatter']` and `ui.zh.tools['sql-formatter']`
- `ZH_ERRORS['Invalid SQL']`
- `src/content/tools/sql-formatter.md` and `src/content/tools/zh/sql-formatter.md`
- Direct dependency `sql-formatter@15.8.2`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor paste + dialect select → SqlFormatter island | Untrusted textarea and select value cross into in-browser lib only |
| island → ToolShell output | Formatted SQL rendered as text in pre/code; Copy uses clipboard |
| sql.ts → sql-formatter npm | Third-party formatter/parser runs in the visitor browser; no server |
| static SSG → browser | No tool API route; no fetch in this slice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | SqlFormatter / ToolShell output | medium | mitigate | Keep formatted SQL in ToolShell text nodes (`pre code`); do not assign HTML strings into the DOM; never render parser dumps |
| T-03-02 | Denial of service | SqlFormatter.tsx / isTooLarge | medium | mitigate | Call isTooLarge on the textarea before formatSql; INPUT_MAX_CHARS stays 100000 in limits.ts (do not edit) |
| T-03-03 | Elevation of privilege | sql.ts / FAQ | high | mitigate | No backend and no in-browser engine; format only; FAQ states the tool is not an executor; do not add sql.js or WASM SQLite |
| T-03-04 | Tampering | formatSql dialect union | medium | mitigate | Map select values through a typed DIALECTS object of imported dialect objects; never index a full dialect registry with a raw user string |
| T-03-05 | Information disclosure | island state | low | accept | Session-only React state; no fetch; last-dialect memory is deferred; public pretty-printer has no secrets |
| T-03-06 | Information disclosure | formatSql catch | medium | mitigate | Empty catch maps every throw to English Invalid SQL; do not put e.message in the UI |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Direct install is sql-formatter@15.8.2 only (legitimacy OK). Do not import argparse (SUS, CLI transitive) or nearley. No [SLOP] packages. |

</threat_model>

<verification>
Inherit Phase 2 `npm test` after tasks 1–2. Task 3 adds `npm run build` plus a Node read of dist/_astro/JsonFormatter*.js that fails on missing chunk or CAT-04 leak (nearley / formatDialect). Completeness: ToolIsland slug-equals loop and EN+ZH existsSync must pass at length 15. Featured length 6. sql.test.ts covers idle, pretty-print, throw fixtures, dialect smokes, and import isolation. Do not run Playwright. Do not trust stale dist/_astro from before this package.
</verification>

<success_criteria>
- SQL-01..06 delivered for slug sql-formatter at 8-file parity
- Named dialect-aware formatter with six dialect objects; keywordCase upper; tabWidth 2
- Invalid SQL errors instead of mangled output; empty input idle
- FAQ not-executor and not-autodetect in EN+ZH
- Catalog 15, featured 6, existing ten relatedSlugs untouched
- CAT-04: only sql.ts imports the package; json-formatter chunk clean after fresh build
- npm test green and npm run build CAT-04 grep green
</success_criteria>

<output>
Create `.planning/phases/03-sql-formatter/03-01-SUMMARY.md` when done
</output>
