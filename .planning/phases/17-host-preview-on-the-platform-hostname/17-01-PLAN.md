---
phase: 17-host-preview-on-the-platform-hostname
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - astro.config.mjs
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A visitor can open https://danbeng.github.io/toolsforfree/ and get the built catalog (HOST-01, HOST-02, D-01, D-02)."
    - "https://danbeng.github.io/toolsforfree/zh/ and https://danbeng.github.io/toolsforfree/tools/json-formatter/ return 200, and each unslashed form redirects at most once to the slashed form with no loop (HOST-02, D-02, D-08)."
    - "A missing path under that prefix is the site 404 document, and its body contains no /zh/404/ link (HOST-03, D-02)."
    - "ci.yml is still the read-only Node 22 test/build gate. deploy.yml is a separate workflow that publishes dist/ only after that gate is green for the same SHA (HOST-01, D-05, D-06, D-07)."
    - "SITE_ORIGIN and site stay https://example.com. public/CNAME is absent. trailingSlash stays always. base is /toolsforfree until Phase 19 removes it (D-03, D-04)."
  artifacts:
    - path: .github/workflows/deploy.yml
      provides: "Pages publisher gated on CI success for the same SHA"
      contains: "workflow_run"
    - path: astro.config.mjs
      provides: "Project-site base so assets and links resolve under /toolsforfree"
      contains: "base: '/toolsforfree'"
  key_links:
    - from: .github/workflows/deploy.yml
      to: .github/workflows/ci.yml
      via: "workflow_run on the workflow named CI, conclusion success, same SHA"
      pattern: "workflow_run"
    - from: astro.config.mjs
      to: dist/
      provides: "built hrefs and assets prefixed with /toolsforfree"
      via: "base: '/toolsforfree' consumed by the Astro build that deploy.yml uploads"
      pattern: "base:"
---

<objective>
Publish dist/ to GitHub Pages on the project-site hostname and prove trailing-slash URLs plus the site 404 before any custom domain.

Purpose: HOST-01, HOST-02, HOST-03. The live Pages URL is https://danbeng.github.io/toolsforfree/ (D-01, D-02), so this phase sets Astro base to /toolsforfree (D-03). Phase 19 must drop that base (or set it to /) when the custom domain is attached. Do not drop it now. Do not change SITE_ORIGIN (D-04). Do not edit ci.yml (D-05).

Output: .github/workflows/deploy.yml and one base key in astro.config.mjs. No other product files.
</objective>

<execution_context>
@C:/Users/nnnnw/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nnnnw/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-host-preview-on-the-platform-hostname/17-CONTEXT.md
@.planning/phases/17-host-preview-on-the-platform-hostname/17-UI-SPEC.md
@.github/workflows/ci.yml
@astro.config.mjs

Locked decisions: D-01 GitHub Pages, Actions source, repo danbeng/toolsforfree, html_url https://danbeng.github.io/toolsforfree/. One publisher. D-02 project site; smoke tests use the /toolsforfree prefix, not the apex. D-03 base is /toolsforfree with no trailing slash on the value; do not commit public/CNAME; do not flip trailingSlash. Costly: Phase 19 must remove base. D-04 SITE_ORIGIN stays https://example.com. D-05 new deploy.yml only; ci.yml stays contents: read, Node 22, npm ci, npm test, npm run build; do not use withastro/action. D-06 Node 22, actions/upload-pages-artifact@v5, actions/deploy-pages@v5, permissions pages: write and id-token: write on deploy.yml only, environment github-pages. D-07 deploy only after CI succeeds for that SHA (workflow_run). D-08 if a slashed directory URL 404s, stop. Do not add a slash-forcing redirect. Do not flip trailingSlash. Do not switch host.

UI-SPEC: deploy-only. No CSS, copy, components, illustration, or 404 restyle. 404 h1 stays the existing notFoundTitle. Do not add src/pages/zh/404.astro. Do not hard-code /toolsforfree into source strings.

Research "omit base" assumed a user site. D-03 overrides that for this phase only because the live URL is a project site. ROADMAP Notes still say "Omit base"; the CONTEXT decision wins.

Phase 19 dependency (record, do not implement): when the custom domain is attached, drop base or set it to /. Leaving /toolsforfree on a root domain prefixes every EN/ZH URL.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Project-site base and a CI-gated Pages workflow</name>
  <files>.github/workflows/deploy.yml, astro.config.mjs</files>
  <read_first>
    .github/workflows/ci.yml
    astro.config.mjs
    .planning/phases/17-host-preview-on-the-platform-hostname/17-CONTEXT.md
  </read_first>
  <action>
    Add base: '/toolsforfree' to the existing defineConfig object in astro.config.mjs, next to site (D-03). Leave site: 'https://example.com' (D-04). Leave trailingSlash: 'always'. Do not add a redirect integration, a slash rewrite, or public/CNAME.

    Create .github/workflows/deploy.yml only. Do not edit .github/workflows/ci.yml (D-05). The workflow name is Deploy. Trigger is workflow_run with workflows: [CI] and types: [completed] (D-07). CI is the name already in ci.yml. Do not use withastro/action.

    One job, named deploy, runs-on ubuntu-latest, environment name github-pages. Job-level permissions are pages: write and id-token: write only (D-06). Do not grant contents: write. if: the triggering workflow is CI, its conclusion is success, and its head_branch is main. That is the SHA gate: a failed or cancelled CI run does not publish.

    concurrency group pages, cancel-in-progress false, so a later green SHA does not cancel an in-flight publish.

    Steps, in order: actions/checkout@v4 with ref set to github.event.workflow_run.head_sha (the green CI SHA, not the default branch tip if they differ). actions/setup-node@v4 with node-version 22 and cache npm. npm ci. npm run build. actions/upload-pages-artifact@v5 with path dist. actions/deploy-pages@v5 with id deployment (D-06). Do not add npm test here; CI already ran it. Do not bump checkout or Node. Do not install packages. Do not set an output URL into SITE_ORIGIN.

    Do not stage src/lib/crontab.ts, src/components/ToolShell.tsx, public/CNAME, or ci.yml. Path-limited add of the two files this task names.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('astro.config.mjs','utf8'); const d=fs.readFileSync('.github/workflows/deploy.yml','utf8'); const c=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(!a.includes('base: \\'/toolsforfree\\'')) process.exit(1); if(!a.includes('trailingSlash: \\'always\\'')) process.exit(1); if(!a.includes('https://example.com')) process.exit(1); for (const s of ['workflow_run','CI','upload-pages-artifact@v5','deploy-pages@v5','node-version: 22','pages: write','id-token: write','github-pages','head_sha']) { if(!d.includes(s)) { console.error('missing '+s); process.exit(1);} } if(d.includes('withastro/action')) process.exit(1); if(!c.includes('contents: read')||!c.includes('node-version: 22')) process.exit(1);"</automated>
  </verify>
  <done>astro.config.mjs has base /toolsforfree and still has site https://example.com and trailingSlash always. deploy.yml exists, is gated on a successful CI workflow_run for that SHA, and uses Node 22 plus the two Pages actions. ci.yml is unchanged. public/CNAME does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Commit only the two host files and confirm CI is green for that SHA</name>
  <files>.github/workflows/deploy.yml, astro.config.mjs</files>
  <precondition>git status --short does not list src/lib/crontab.ts or src/components/ToolShell.tsx as staged. origin is danbeng/toolsforfree. Pages already exists with build_type workflow (D-01). Do not create a second publisher.</precondition>
  <action>
    Confirm git status --short. If src/lib/crontab.ts or src/components/ToolShell.tsx is staged, unstage it and stop. Do not stash pop. Do not git add -A, git add ., or git commit -a.

    Stage only .github/workflows/deploy.yml and astro.config.mjs. Commit with a message that names the project-site base and the CI-gated Pages workflow. Record SHA=$(git rev-parse HEAD) immediately after that commit. Push to origin main. Do not force-push. Do not retag v1.2. Do not push --tags.

    After the push, wait until the CI workflow run for that commit SHA has conclusion success. Record the SHA. Do not treat a green CI check as the visitor URL (D-07). The Deploy workflow starts only after that CI run completes. If Deploy does not start, or its job fails, stop and report the run URL. Do not edit ci.yml to make Deploy start. Do not add a slash redirect to make a later curl pass (D-08).
  </action>
  <verify>
    <automated>files=$(git diff --name-only "${SHA}^..${SHA}") || exit 1; printf '%s\n' "$files" | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const files=s.trim().split(/\r?\n/).filter(Boolean).sort(); const want=['.github/workflows/deploy.yml','astro.config.mjs']; if(files.join('|')!==want.join('|')){console.error(files); process.exit(1);}})"</automated>
  </verify>
  <done>HEAD contains only .github/workflows/deploy.yml and astro.config.mjs relative to the parent commit. The CI run for that SHA is success. Deploy has started from workflow_run for that SHA. crontab.ts and ToolShell.tsx are not in the commit.</done>
</task>

<task type="auto">
  <name>Task 3: Prove slashed URLs and the site 404 on the project hostname</name>
  <files>.github/workflows/deploy.yml</files>
  <precondition>The Deploy run for the SHA from Task 2 has completed with conclusion success, and the Pages deployment for that run is live. If Deploy is still queued, wait. If it failed, stop. Do not curl a stale deployment.</precondition>
  <action>
    After Deploy succeeds, curl -sI the project-site prefix, not the apex (D-02). Required slashed URLs, each must be HTTP 200: https://danbeng.github.io/toolsforfree/ and https://danbeng.github.io/toolsforfree/zh/ and https://danbeng.github.io/toolsforfree/tools/json-formatter/. For each, also curl -sI the unslashed form (no trailing slash). That response must be a single redirect (301 or 302) whose Location is the slashed URL, and a follow of that one hop must be 200. A second redirect is a loop. Stop and fail the phase (D-08).

    Also curl -sI a missing path under the prefix, https://danbeng.github.io/toolsforfree/this-path-is-not-a-tool/. Then GET that path and confirm the body is the site 404 document (it contains the existing English not-found heading) and does not contain the substring /zh/404/ (HOST-03). Do not add src/pages/zh/404.astro. Do not restyle the 404 heading.

    If any slashed directory URL returns 404, stop. The plan fails. Do not add a slash-forcing redirect. Do not flip trailingSlash. Do not switch host (D-08). Record the status line and Location headers in the phase summary.

    Do not change SITE_ORIGIN. Do not commit public/CNAME. Phase 19 still must remove base later; say that in the summary and do not remove it here.
  </action>
  <verify>
    <automated>curl -sI https://danbeng.github.io/toolsforfree/ &amp;&amp; curl -sI https://danbeng.github.io/toolsforfree &amp;&amp; curl -sI https://danbeng.github.io/toolsforfree/zh/ &amp;&amp; curl -sI https://danbeng.github.io/toolsforfree/zh &amp;&amp; curl -sI https://danbeng.github.io/toolsforfree/tools/json-formatter/ &amp;&amp; curl -sI https://danbeng.github.io/toolsforfree/tools/json-formatter &amp;&amp; curl -sI https://danbeng.github.io/toolsforfree/this-path-is-not-a-tool/ &amp;&amp; curl -s https://danbeng.github.io/toolsforfree/this-path-is-not-a-tool/ | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{if(s.includes('/zh/404/')) process.exit(1);})"</automated>
  </verify>
  <done>Slashed home, zh, and json-formatter URLs are 200. Each unslashed form redirects once to the slashed form and that hop is 200. The missing path is the site 404 and its body has no /zh/404/ link. A slashed 404 was not papered over with a redirect. base remains /toolsforfree for Phase 19 to remove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions to Pages | A workflow with a write token publishes dist/ to the public project site |
| Visitor to static host | Untrusted paths are served as files or the site 404; no server tool logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | deploy.yml GITHUB_TOKEN | high | mitigate | Job permissions are pages: write and id-token: write only. No contents: write. ci.yml stays contents: read and is not edited (D-05, D-06). |
| T-17-02 | Tampering | workflow_run SHA | high | mitigate | Checkout ref is github.event.workflow_run.head_sha, and the job if requires CI conclusion success on main. A failed CI SHA is not published (D-07). |
| T-17-03 | Spoofing | canonical origin | medium | accept | SITE_ORIGIN stays https://example.com on purpose (D-04). Placeholder canonicals are tolerated only on this hostname. Phase 18 swaps the origin. Do not bake github.io into SITE_ORIGIN here. |
| T-17-04 | Information Disclosure | dist/ contents | low | accept | The site is a public static catalog. No secrets, no env files, no tool payloads leave the browser. |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages. deploy.yml runs npm ci from the lockfile already on the CI-green SHA. No install checkpoint is required because nothing new is added. |
</threat_model>

<verification>
CI workflow named CI is still contents: read, Node 22, npm ci, npm test, npm run build, and its file was not edited.

Deploy workflow publishes only after that CI run succeeds for the same SHA, using Node 22, upload-pages-artifact@v5, and deploy-pages@v5.

curl -sI https://danbeng.github.io/toolsforfree/ returns 200.
curl -sI https://danbeng.github.io/toolsforfree/zh/ returns 200.
curl -sI https://danbeng.github.io/toolsforfree/tools/json-formatter/ returns 200.
Each unslashed form redirects at most once to the slashed form. No loop.
A missing path under the prefix is the site 404 and the body contains no /zh/404/.
If a slashed directory URL 404s, the phase fails. Do not add a slash redirect.
</verification>

<success_criteria>
HOST-01: dist/ is on GitHub Pages via deploy.yml, and ci.yml was not replaced.
HOST-02: the three slashed project-site URLs are 200, unslashed redirects once, no loop.
HOST-03: a missing path is the site 404 with no /zh/404/ link.
D-03 base is present. Phase 19 must remove it later. It is not removed in this plan.
</success_criteria>

<output>
Create .planning/phases/17-host-preview-on-the-platform-hostname/17-01-SUMMARY.md when done
</output>
