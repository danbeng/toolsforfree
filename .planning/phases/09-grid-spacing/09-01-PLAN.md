---
phase: 09-grid-spacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/pages/index.astro
  - src/pages/tools/index.astro
  - src/components/ToolCard.astro
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - LAY-03

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor viewing home featured and each /tools/ category at min-width 1080px sees a 3-column .card-grid; at min-width 720px and below 1080px two columns; below 720px one column (LAY-01, Catalog grid markup)"
    - "Existing in-tool .tool-grid stays 1-col default / 2-col at min-width 720px and does not gain a 1080px track (LAY-01, Catalog grid markup)"
    - ":root defines --sp-1 through --sp-12 as 4/8/12/16/24/32/48/64/80/96/120/144px next to color tokens and not under the light theme selector (LAY-02, Spacing scale tokens)"
    - ".card-grid gap is var(--sp-4); .tool-card padding is var(--sp-4); .wrap horizontal padding is var(--sp-4); .nav gap is var(--sp-5) (LAY-03, Spacing scale tokens)"
    - "Cards and sections do not overlap or collapse after those token swaps (LAY-03 adjacency, Catalog grid markup)"
    - "SSG catalog is never empty; getToolsByCategory already drops zero-tool groups; do not render empty-grid chrome, No tools copy, a placeholder card, or a tooltip (UI empty, LAY-03 empty)"
    - "First paint is the populated grid; no fetch, skeleton, or spinner (UI loading)"
    - "Layout is CSS plus markup only; no role=alert and no error copy for grid (UI error)"
    - "Home featured is six ToolCards in one .card-grid; each /tools/ category section has its own .card-grid of that category's tools (UI populated, Catalog grid markup)"
    - "Every HEAD card has name plus shortDescription; no optional card fields this phase (UI partial)"
    - "Cards use min-width 0 and overflow-wrap anywhere so titles and descriptions wrap; the grid does not scroll the page sideways (UI overflow, Card chrome)"
    - "Zero never paints; one card occupies the first track only and does not stretch to full wrap width; many fill 1 / 2 / 3 columns per breakpoint (UI zero-one-many, LAY-03 empty)"
    - "Card title and description wrap inside the track with no ellipsis; nav gap token swap adds no text; .nav a.logo font-weight 650 is unchanged (UI long-text, Card chrome)"
    - "Idle .tool-card color is var(--text); hover text is var(--accent) only; no hover fill, no hover border-color, no :active scale (Card chrome)"
    - "h2:has(+ .card-grid) margin-bottom is var(--sp-4) as progressive enhancement with no polyfill; grid columns still work if :has() is absent (RESEARCH A1, Spacing scale tokens)"
    - "HEAD featured and category map order is unchanged; this phase does not edit or reorder TOOLS (LAY-03 ordering, flagged assumption)"
    - "Flagged assumption (LAY-01 unclassified probe): column counts are CSS Grid on .card-grid at 1 / 2 / 3 tracks, not a SPEC.md shape table"
    - "Flagged assumption (LAY-02 unclassified probe): the spacing scale is twelve --sp-* custom properties on :root only"
    - "Files are authored from HEAD chrome; do not apply stash entries; do not commit the ZH page tree or the locale switcher (HEAD vs overlay)"
    - "Zero new npm packages; no Playwright; no jsdom switch; no layout.test.ts; no src/lib/layout.ts; no Tailwind (HEAD vs overlay)"
    - statement: "At min-width 1080px home featured and each /tools/ category .card-grid are three columns inside --content 52rem with no card overlap; at min-width 720px and below 1080px two columns; below 720px one column"
      verification: backstop
  artifacts:
    - path: src/styles/global.css
      provides: "--sp-1 through --sp-12 on :root; .card-grid 1/2/3-col; .tool-card layout-only chrome; retokenized .wrap padding and .nav gap (LAY-01, LAY-02, LAY-03)"
      min_lines: 200
    - path: src/pages/index.astro
      provides: "HEAD home chrome with featured map wrapped in .card-grid (LAY-01, Catalog grid markup)"
    - path: src/pages/tools/index.astro
      provides: "HEAD tools index with one .card-grid per category section (LAY-01, Catalog grid markup)"
    - path: src/components/ToolCard.astro
      provides: "HEAD <a class=tool-card> with tool.name and tool.shortDescription; no locale prop (Card chrome, HEAD vs overlay)"
  key_links:
    - from: src/pages/index.astro
      to: src/components/ToolCard.astro
      via: "featured.map ToolCard inside div.card-grid after h2 Featured tools"
      pattern: "card-grid"
    - from: src/pages/tools/index.astro
      to: src/components/ToolCard.astro
      via: "each category section wraps g.tools.map in div.card-grid"
      pattern: "card-grid"
    - from: src/styles/global.css
      to: ".card-grid"
      via: "@media (min-width: 1080px) repeat(3, minmax(0, 1fr))"
      pattern: "min-width: 1080px"
    - from: src/styles/global.css
      to: ":root"
      via: "--sp-1 through --sp-12 after color/diff tokens"
      pattern: "--sp-1:"
    - from: src/components/ToolCard.astro
      to: src/styles/global.css
      via: "a.tool-card consumes layout-only chrome"
      pattern: "tool-card"
  prohibitions:
    - "Do not commit overlay ZH catalog pages or the locale switcher (HEAD vs overlay)"
    - "Do not add a network endpoint or upload path for catalog cards (privacy)"
    - "Do not persist grid or spacing state in web storage"
---

<objective>
Ship a three-column catalog grid at min-width 1080px and one spacing token scale on HEAD English home featured tools and /tools/ category lists — per Catalog grid markup (LAY-01), Spacing scale tokens (LAY-02, LAY-03), Card chrome, and HEAD vs overlay.

Purpose: Phase 9 visual layout. Phase 10 can polish buttons, tool-panel, and FAQ without inheriting overlay catalog markup or a 3-col in-tool split.

Output: .card-grid wrappers on HEAD src/pages/index.astro and src/pages/tools/index.astro, --sp-1 through --sp-12 plus .card-grid and .tool-card rules in src/styles/global.css, HEAD ToolCard restored. No new npm packages. No Playwright. No src/lib. No Tailwind.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-grid-spacing/09-CONTEXT.md
@.planning/phases/09-grid-spacing/09-RESEARCH.md
@.planning/phases/09-grid-spacing/09-PATTERNS.md
@.planning/phases/09-grid-spacing/09-UI-SPEC.md
@.planning/phases/09-grid-spacing/09-VALIDATION.md
@.planning/phases/08-mobile-hamburger-menu/08-01-SUMMARY.md

<interfaces>
From git show HEAD:src/pages/index.astro — frontmatter imports BaseLayout, ToolCard, SITE_NAME, SITE_TAGLINE, getFeaturedTools, getToolsByCategory. Featured ToolCards are siblings after h2 Featured tools with no wrapper. Category <a>s are not a card grid.

From git show HEAD:src/pages/tools/index.astro — getToolsByCategory plus idFor; each category is a section with sibling ToolCards and no wrapper.

From git show HEAD:src/components/ToolCard.astro — Props { tool: Tool } only. Markup is a.tool-card href=/tools/${tool.slug}/ with strong tool.name and p tool.shortDescription.

From src/data/tools.ts — getFeaturedTools filters featured; getToolsByCategory maps CATEGORY_ORDER and drops empty groups. Do not edit this file. src/data/tools.test.ts already asserts featured length 6.

From src/styles/global.css — :root has --content: 52rem and color tokens; :root[data-theme="light"] overrides colors only; .wrap padding 0 1rem; .nav gap 1.25rem min-height 3.25rem; .tool-grid gap 1rem and 2-col at min-width 720px; no .card-grid; no .tool-card rule.

Locked CONTEXT groups: Catalog grid markup, Spacing scale tokens, Card chrome, HEAD vs overlay. 09-UI-SPEC already resolved discretion: .card-grid gap --sp-4, .nav gap --sp-5, hover color only.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end 3-column catalog grid — one path</name>
  <reversibility rating="costly">.card-grid plus --sp-* on :root become the layout contract Phase 10 and later catalog pages consume; undoing it is a coordinated CSS and markup change, not a data migration.</reversibility>
  <precondition>git show HEAD:src/pages/index.astro contains Featured tools and ToolCard; src/styles/global.css contains .tool-grid and --content: 52rem; git stash list still shows stash@{0} and stash@{1}</precondition>
  <files>src/pages/index.astro, src/pages/tools/index.astro, src/components/ToolCard.astro, src/styles/global.css</files>
  <read_first>.planning/phases/09-grid-spacing/09-UI-SPEC.md, .planning/phases/09-grid-spacing/09-CONTEXT.md, .planning/phases/09-grid-spacing/09-PATTERNS.md, .planning/phases/09-grid-spacing/09-RESEARCH.md, .planning/phases/09-grid-spacing/09-VALIDATION.md, src/pages/index.astro, src/pages/tools/index.astro, src/components/ToolCard.astro, src/styles/global.css</read_first>
  <action>
    Before any markup or CSS edit, restore HEAD bytes of src/pages/index.astro, src/pages/tools/index.astro, and src/components/ToolCard.astro together with git checkout HEAD -- on those three paths only (HEAD vs overlay, Wave 0). Do not apply or pop git stash entries. Do not checkout src/styles/global.css (Phase 8 hamburger lives there). Leave Header, NavMenu, ThemeToggle, ThemeInit, FaqList, ToolShell, src/lib, src/data/tools.ts, and src/i18n untouched. Do not add src/lib/layout.ts.

    Keep HEAD ToolCard markup: anchor class tool-card, strong tool.name, p tool.shortDescription. Do not add an eyebrow. Do not add a locale prop (Card chrome, HEAD vs overlay).

    On HEAD src/pages/index.astro, wrap only the featured map per Catalog grid markup: after h2 Featured tools, a div class card-grid around featured.map ToolCard, then the existing p with View all tools linking /tools/. Keep sibling h1 SITE_NAME, p SITE_TAGLINE, and category anchor list. Do not import overlay home chrome or t().

    On HEAD src/pages/tools/index.astro, keep h1 All tools and the existing groups.map. Each category section stays id={idFor(g.category)} with h2 {g.category} and a div class card-grid around g.tools.map ToolCard (Catalog grid markup, LAY-01). Do not add page-intro or category-block classes.

    In src/styles/global.css, copy the Token implementation and Catalog grid sections of 09-UI-SPEC.md (LAY-01, LAY-02, Spacing scale tokens). Insert --sp-1 through --sp-12 on :root after the color/diff tokens with values 4px, 8px, 12px, 16px, 24px, 32px, 48px, 64px, 80px, 96px, 120px, 144px. Do not duplicate those properties under the light theme selector.

    Add .card-grid as display grid, grid-template-columns minmax(0, 1fr), gap var(--sp-4). At min-width 720px set .card-grid to repeat(2, minmax(0, 1fr)). At min-width 1080px set .card-grid to repeat(3, minmax(0, 1fr)). Use min-width, not max-width. Do not use auto-fit, auto-fill, or flex-wrap. Do not add 1080px to .tool-grid. Leave .tool-grid gap as the literal 1rem. Leave --content at 52rem.

    Do not retokenize .wrap or .nav yet. Do not add .tool-card chrome yet. Task 2 adds those remaining selectors.
  </action>
  <verify>
    <automated>npm test && rg -n "card-grid" src/styles/global.css src/pages/index.astro src/pages/tools/index.astro && rg -n "min-width: 1080px" src/styles/global.css && rg -n "min-width: 720px" src/styles/global.css && rg -F -- "--sp-1: 4px" src/styles/global.css && rg -F -- "--sp-2: 8px" src/styles/global.css && rg -F -- "--sp-3: 12px" src/styles/global.css && rg -F -- "--sp-4: 16px" src/styles/global.css && rg -F -- "--sp-5: 24px" src/styles/global.css && rg -F -- "--sp-6: 32px" src/styles/global.css && rg -F -- "--sp-7: 48px" src/styles/global.css && rg -F -- "--sp-8: 64px" src/styles/global.css && rg -F -- "--sp-9: 80px" src/styles/global.css && rg -F -- "--sp-10: 96px" src/styles/global.css && rg -F -- "--sp-11: 120px" src/styles/global.css && rg -F -- "--sp-12: 144px" src/styles/global.css && rg -n "Featured tools" src/pages/index.astro && rg -n "View all tools" src/pages/index.astro && rg -n "All tools" src/pages/tools/index.astro && rg -n "tool-grid" src/styles/global.css && rg -n "gap: 1rem" src/styles/global.css && test "$(rg -n -- "--sp-1:" src/styles/global.css | head -1 | cut -d: -f1)" -lt "$(rg -n "data-theme=\"light\"" src/styles/global.css | head -1 | cut -d: -f1)"</automated>
    <fails_when>npm test exits non-zero; rg prints no matches for card-grid in the three files, or for min-width 1080px / 720px, any --sp-1 through --sp-12 value, Featured tools, View all tools, All tools, tool-grid, or gap: 1rem; or the first --sp-1: line number is not less than the first data-theme="light" line number</fails_when>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro contains Featured tools, a div class card-grid around featured ToolCards, and View all tools per Catalog grid markup, LAY-01
    - src/pages/tools/index.astro contains All tools and card-grid inside each category section per Catalog grid markup, LAY-01
    - src/components/ToolCard.astro contains class="tool-card", tool.name, and shortDescription per Card chrome, HEAD vs overlay
    - src/styles/global.css contains --sp-1: 4px through --sp-12: 144px on :root before the light theme selector per LAY-02, Spacing scale tokens
    - src/styles/global.css contains .card-grid, min-width: 720px, min-width: 1080px, and still contains .tool-grid with gap: 1rem per LAY-01
  </acceptance_criteria>
  <done>A visitor path exists: HEAD home featured and /tools/ category lists wrap in .card-grid, :root has the twelve spacing tokens, and at min-width 1080px the catalog CSS is three columns without changing .tool-grid (LAY-01, LAY-02, Catalog grid markup, Spacing scale tokens, HEAD vs overlay).</done>
</task>

<task type="auto">
  <name>Tokenize wrap/nav and ship layout-only card chrome</name>
  <files>src/styles/global.css</files>
  <read_first>.planning/phases/09-grid-spacing/09-UI-SPEC.md, src/styles/global.css, src/pages/index.astro, src/pages/tools/index.astro, src/components/ToolCard.astro</read_first>
  <action>
    In src/styles/global.css only, finish LAY-03 and Card chrome from the Token implementation, Catalog grid, and Card chrome sections of 09-UI-SPEC.md. Using gap --sp-4, nav --sp-5, and hover color only per 09-UI-SPEC (CONTEXT discretion resolved).

    Change .wrap to padding: 0 var(--sp-4) and keep max-width var(--content) and margin 0 auto. Change .nav gap to var(--sp-5) and keep align-items center and min-height 3.25rem. Leave .nav-links gap as the literal 1rem. Leave .nav a.logo font-weight 650. Leave --content at 52rem.

    Add h2:has(+ .card-grid) with margin-bottom var(--sp-4). Add main section { margin-top: var(--sp-6); } and main section > h2 { margin-top: 0; }. Do not polyfill :has() (RESEARCH A1).

    Add .tool-card as display block, min-width 0, padding var(--sp-4), border 1px solid var(--border), background var(--panel), color var(--text), text-decoration none, overflow-wrap anywhere. Add .tool-card:hover with color var(--accent) only. Add .tool-card strong as display block, font-size 16px, font-weight 600, line-height 1.2. Add .tool-card p as margin var(--sp-2) 0 0, font-size 16px, font-weight 400, line-height 1.5. New CSS weights this phase are 400 and 600 only.

    Do not add border-radius, box-shadow, hover fill, hover border-color, or :active. Focus stays the existing a:focus-visible 2px accent outline. Do not retokenize #navToggle / #themeToggle 44px boxes, hamburger overlay 16px / 8px literals, body grid 47px / 48px, .tool-panel, .diff-lines, .md-preview, .tool-grid gap, or footer rem values. Do not restyle home or tools h1/h2 font-size.
  </action>
  <verify>
    <automated>rg -F -- "padding: 0 var(--sp-4)" src/styles/global.css && rg -F -- "gap: var(--sp-5)" src/styles/global.css && rg -F -- "h2:has(+ .card-grid)" src/styles/global.css && rg -n "main section" src/styles/global.css && rg -n "\.tool-card" src/styles/global.css && rg -F -- "width: 44px" src/styles/global.css && rg -n "gap: 1rem" src/styles/global.css && rg -F -- "--content: 52rem" src/styles/global.css && rg -n "font-weight: 650" src/styles/global.css && npm test</automated>
    <fails_when>rg prints no matches for wrap padding var(--sp-4), nav gap var(--sp-5), h2:has(+ .card-grid), main section, .tool-card, width: 44px, gap: 1rem, --content: 52rem, or font-weight: 650; npm test exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - src/styles/global.css .wrap uses padding: 0 var(--sp-4) and .nav uses gap: var(--sp-5) per LAY-03, Spacing scale tokens
    - src/styles/global.css contains h2:has(+ .card-grid), main section margin-top var(--sp-6), and .tool-card padding var(--sp-4) with idle color var(--text) and hover color var(--accent) per Card chrome, LAY-03
    - src/styles/global.css still contains width: 44px, gap: 1rem, --content: 52rem, and font-weight: 650
  </acceptance_criteria>
  <done>Touched layout uses the spacing scale, catalog cards read as panel chrome with accent hover text only, and hamburger / tool-grid / footer literals stay as HEAD (LAY-03, Card chrome, Spacing scale tokens).</done>
</task>

<task type="auto">
  <name>Production build and overlay-commit safety gates</name>
  <files>src/styles/global.css, src/pages/index.astro, src/pages/tools/index.astro, src/components/ToolCard.astro</files>
  <read_first>src/styles/global.css, src/pages/index.astro, src/pages/tools/index.astro, src/components/ToolCard.astro, package.json</read_first>
  <action>
    Do not add packages, Playwright, a jsdom Vitest switch, Tailwind, a helper under src/lib, or a new unit test that imports TOOLS. Confirm src/data/tools.ts and src/data/tools.test.ts were not edited. Confirm Header, NavMenu, ThemeToggle, ThemeInit, FaqList, ToolShell, and src/i18n were not edited this phase. Confirm package.json and package-lock.json match HEAD. Confirm src/lib/layout.ts was not created.

    Confirm dist will emit card-grid wrappers from the two catalog pages. Confirm git stash list still shows both overlay-chrome-temp and the unrelated i18n stash; do not apply them.

    Run the existing Vitest suite and a production Astro build (T-09-01). Stage and commit only the four in-scope files listed in this plan frontmatter via path-limited git add. Do not stage the ZH page tree, the untracked locale switcher, or the entire working tree.
  </action>
  <verify>
    <automated>npm test && npm run build && rg -n "card-grid" dist --glob "*.html" && rg -n "tool-card" dist --glob "*.html" && test ! -f src/lib/layout.ts && test ! -f src/layout.test.ts && PKG_DIFF="$(git diff HEAD -- package.json package-lock.json src/data/tools.ts src/data/tools.test.ts)" && test -z "$PKG_DIFF" && STASH_LIST="$(git stash list)" && echo "$STASH_LIST" | rg -F "gsd-phase7-overlay-chrome-temp" && echo "$STASH_LIST" | rg -F "pre-02-01-merge unrelated i18n"</automated>
    <fails_when>npm test or npm run build exits non-zero; no dist HTML contains card-grid or tool-card; package.json, package-lock.json, src/data/tools.ts, or src/data/tools.test.ts differs from HEAD; src/lib/layout.ts or src/layout.test.ts exists; git stash list does not contain gsd-phase7-overlay-chrome-temp or pre-02-01-merge unrelated i18n</fails_when>
    <human-check>
      DevTools width 719px: home featured and each /tools/ category .card-grid are one column. Width 720px and 1079px: those grids are two columns; open /tools/markdown-preview/ (or text-diff) and confirm .tool-grid.split is still two columns, not three. Width 1080px on / and /tools/: three columns inside --content 52rem; no card overlap; no horizontal page scroll from the grid. /tools/#category-auth at 1080px: one card in the first track only. Hover a card in both themes: title and description turn --accent; border and fill stay idle. Tab onto a card: existing 2px accent outline. Toggle theme on the catalog: cards use that theme's --panel / --border / --text.
    </human-check>
  </verify>
  <acceptance_criteria>
    - dist HTML contains card-grid and tool-card so the catalog wrappers survived SSG (LAY-01)
    - package.json matches HEAD; src/lib/layout.ts does not exist; src/data/tools.ts matches HEAD (HEAD vs overlay)
    - git stash list still contains both named stash entries
    - npm test and npm run build both exit 0
  </acceptance_criteria>
  <done>Production HTML includes the catalog grid, no new packages, TOOLS order unchanged, overlay stashes untouched, and existing lib tests still pass (LAY-01, LAY-02, LAY-03, HEAD vs overlay, T-09-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSG TOOLS registry → catalog HTML | Featured and category cards are static anchors from getFeaturedTools / getToolsByCategory; no visitor input |
| Viewport → CSS Grid tracks | min-width 720px / 1080px in global.css decide 1 / 2 / 3 columns |
| Git index → overlay files | Dirty ZH tree and locale switcher must not enter the commit (T-09-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | git commit / overlay pages | high | mitigate | Restore HEAD three catalog files; path-limited git add of the four in-scope files only; never commit src/pages/zh/ or LangSwitch.astro; do not pop stash@{0} or stash@{1} |
| T-09-02 | Tampering | .tool-grid vs .card-grid | medium | mitigate | Keep .tool-grid at 720px 2-col only; catalog uses .card-grid; do not add 1080px to .tool-grid |
| T-09-03 | Tampering | Catalog markup | low | mitigate | No new scripts this phase; no innerHTML; cards remain ordinary SSG anchors |
| T-09-04 | Information disclosure | Grid / theme | low | accept | Column count is public CSS; theme localStorage is unchanged from Phase 7 |
| T-09-05 | Denial of service | Layout CSS | low | accept | Static CSS Grid; no runtime layout worker |
| T-09-06 | Spoofing | .tool-card links | low | accept | hrefs come from the TOOLS registry slugs; not an auth surface |
| T-09-07 | Repudiation | Catalog clicks | low | accept | Public static pages; no accounts or audit log |
| T-09-08 | Elevation of privilege | Spacing tokens | low | accept | Custom properties are not an access-control boundary |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install tasks this phase; do not add Playwright, Tailwind, or icon packs |

High-severity T-09-01 is mitigated by HEAD restore plus path-limited add (block-on high). T-09-SC has no install to perform.
</threat_model>

<verification>
After all three tasks: npm test green; npm run build green; src/pages/index.astro and src/pages/tools/index.astro wrap maps in .card-grid; :root has --sp-1 through --sp-12 before the light theme selector; .card-grid is 1-col / 2-col at 720px / 3-col at 1080px; .wrap padding and .nav gap use --sp-4 / --sp-5; .tool-card layout-only chrome exists; .tool-grid still gap 1rem; 44px hit targets remain; dist HTML contains card-grid; package.json matches HEAD; no src/lib/layout.ts; stashes remain. Manual 719 / 720-1079 / 1080 checklist lives in the last task human-check (end-of-phase harvest). Do not add Playwright or layout.test.ts.
</verification>

<success_criteria>
- LAY-01: catalog .card-grid is 3-col at min-width 1080px; 2-col at min-width 720px; 1-col default; .tool-grid is unchanged
- LAY-02: --sp-1 through --sp-12 exist on :root with 4/8/12/16/24/32/48/64/80/96/120/144px
- LAY-03: touched .card-grid gap, .tool-card padding, .wrap horizontal padding, and .nav gap use the scale; cards/sections do not overlap
- Catalog grid markup, Spacing scale tokens, Card chrome, and HEAD vs overlay are implemented; deferred 4-col at 1440px, ZH catalog commit, overlay hero/kicker, and Phase 10 button/FAQ chrome are absent
</success_criteria>

## Artifacts this phase produces

- `.card-grid` class in `src/styles/global.css` (1-col default, 2-col at min-width 720px, 3-col at min-width 1080px, gap `var(--sp-4)`)
- `--sp-1` through `--sp-12` custom properties on `:root` (4/8/12/16/24/32/48/64/80/96/120/144px)
- wrappers on `src/pages/index.astro` (featured map) and `src/pages/tools/index.astro` (per-category maps)
- `.tool-card` rules in `src/styles/global.css` (layout-only chrome, hover text `var(--accent)` only)
- `.tool-card strong` and `.tool-card p` type rules
- `h2:has(+ .card-grid)` rule (margin-bottom `var(--sp-4)`)
- `main section` and `main section > h2` spacing rules (`--sp-6` / `margin-top: 0`)
- `.wrap` horizontal padding token (`var(--sp-4)`)
- `.nav` gap token (`var(--sp-5)`)

## Flagged assumptions (spec-less probe)

- LAY-01 unclassified: no SPEC.md shapes; interpret as CSS Grid 1 / 2 / 3 columns on `.card-grid` (Catalog grid markup).
- LAY-02 unclassified: no SPEC.md shapes; interpret as twelve `--sp-*` properties on `:root` only (Spacing scale tokens).
- LAY-03 adjacency: cards/sections must not overlap or collapse after the token swap (also the UI-SPEC column backstop).
- LAY-03 empty: SSG catalog is never empty; Auth has one card occupying the first track only.
- LAY-03 ordering: HEAD featured/category map order is unchanged; do not reorder TOOLS.
- RESEARCH A1: `:has()` heading gap is progressive enhancement; no polyfill.

<output>
Create `.planning/phases/09-grid-spacing/09-01-SUMMARY.md` when done
</output>
