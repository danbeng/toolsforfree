---
phase: 02-light-text-and-generate-tools
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - src/lib/password.ts
  - src/lib/password.test.ts
  - src/components/tools/PasswordGenerator.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/i18n/errors.ts
  - src/content/tools/password-generator.md
  - src/content/tools/zh/password-generator.md
autonomous: true
requirements:
  - PASS-01
  - PASS-02
  - PASS-03
  - PASS-04
  - PASS-05
  - PASS-06

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can generate a password of length 8–128 default 16 on /tools/password-generator/ (PASS-01, D-10)"
    - "Toggles lowercase, uppercase, digits, symbols; symbols exactly !@#$%^&*-_=+ ; island defaults all four on (PASS-02, D-10)"
    - "Exclude similar strips i l 1 O 0 from the charset when on; default off (PASS-03, D-10)"
    - "Generation copies RESEARCH randomIndex/generatePassword: crypto.getRandomValues plus rejection sampling; source-read password.ts has getRandomValues and Math.random is absent (PASS-04, T-02-01, T-02-02)"
    - "Copy via ToolShell; Generate button above toggles; generate on mount and on click; toggles do not auto-regen (PASS-05, D-10)"
    - "Empty charset returns Select at least one character set, not an empty success string; ZH_ERRORS in this slice (PASS-06, T-02-06, D-14)"
    - "Out-of-range length returns Length must be between 8 and 128 with ZH_ERRORS"
    - "Catalog slug password-generator, category Generate, featured false; TOOLS toHaveLength 14; featured stays 6; existing ten relatedSlugs untouched (D-03, D-13)"
    - "Island clones JsonFormatter lib wiring; UuidGenerator is placement-only for the button, not the CSPRNG (D-02)"
  artifacts:
    - path: src/lib/password.ts
      provides: "generatePassword plus randomIndex rejection sampling from RESEARCH"
      exports: ["generatePassword"]
    - path: src/lib/password.test.ts
      provides: "length bounds, charset membership, similar exclusion, empty charset, source-read CSPRNG"
    - path: src/components/tools/PasswordGenerator.tsx
      provides: "Lib-backed generate on mount and click; toggles; ToolShell copy"
    - path: src/components/tools/ToolIsland.astro
      provides: "static PasswordGenerator import and slug-equals branch"
    - path: src/data/tools.ts
      provides: "password-generator row featured false category Generate"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 14; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['password-generator'] chrome"
    - path: src/i18n/errors.ts
      provides: "ZH_ERRORS for empty charset and length bounds"
    - path: src/content/tools/password-generator.md
      provides: "locale en SEO howTo faq"
    - path: src/content/tools/zh/password-generator.md
      provides: "locale zh SEO howTo faq"
  key_links:
    - from: src/components/tools/PasswordGenerator.tsx
      to: src/lib/password.ts
      via: "generatePassword on mount and click"
      pattern: "generatePassword"
    - from: src/lib/password.ts
      to: crypto.getRandomValues
      via: "RESEARCH randomIndex rejection sampling"
      pattern: "getRandomValues"
    - from: src/i18n/errors.ts
      to: generatePassword errors
      via: "ZH_ERRORS same-slice keys"
      pattern: "Select at least one character set"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/PasswordGenerator.tsx
      via: "static import and slug === 'password-generator'"
      pattern: "slug === 'password-generator'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(14)"
      pattern: "toHaveLength\\(14\\)"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** generate a copyable password with length and charset toggles using unbiased Web Crypto, **so that** I get a local CSPRNG result and a clear error if the charset is empty.

<objective>
Ship the password-generator 8-file vertical slice (PASS-01..06). Copy RESEARCH CSPRNG rejection-sampling algorithm exactly into src/lib/password.ts (D-10, PASS-04). Clone JsonFormatter plus json.ts for island/lib; UuidGenerator is button placement only — do not put CSPRNG in the tsx (D-02). Defaults: length 16, lower/upper/digits/symbols on, excludeSimilar off (D-10). Symbols !@#$%^&*-_=+ . Empty charset errors (PASS-06). Category Generate, featured false (D-03). ZH_ERRORS for both English errors in this slice (D-14). Do not rewrite existing ten relatedSlugs (D-13). Do not require one character from each class. No entropy hint (deferred PASS-07). Zero packages. No lib barrel.
Purpose: Fourth sequential slice; Phase 2 catalog completeness at length 14 with featured still 6.
Output: password lib/test, PasswordGenerator island, ToolIsland branch, catalog row, ui.ts, ZH_ERRORS, EN+ZH markdown; tools.test.ts length 14.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-light-text-and-generate-tools/02-CONTEXT.md
@.planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md
@.planning/phases/02-light-text-and-generate-tools/02-PATTERNS.md
@.planning/phases/02-light-text-and-generate-tools/02-03-PLAN.md
@src/lib/json.ts
@src/components/tools/JsonFormatter.tsx
@src/components/tools/UuidGenerator.tsx
@src/components/tools/ToolIsland.astro
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/i18n/errors.ts
@src/content/tools/json-formatter.md
@src/components/tools/ToolIsland.test.ts
@.planning/codebase/CONVENTIONS.md

<interfaces>
From RESEARCH CSPRNG block (copy exactly into src/lib/password.ts):
- LOWER a-z, UPPER A-Z, DIGITS 0-9, SYMBOLS !@#$%^&*-_=+, SIMILAR set i l 1 O 0
- randomIndex(n): n in 1..256; limit = 256 - (256 % n); loop crypto.getRandomValues on a 1-byte Uint8Array; if x < limit return x % n
- generatePassword(opts): length 8–128 else error Length must be between 8 and 128; concatenate selected sets; if excludeSimilar filter SIMILAR; empty charset error Select at least one character set; else length characters via randomIndex
- Do not force one char from each selected class

From src/components/tools/UuidGenerator.tsx: useEffect generate on mount; button type="button" in children. Password must still call generatePassword from ../../lib/password — never randomUUID for this tool.

Island defaults (not lib defaults): length 16, lower/upper/digits/symbols true, excludeSimilar false. Generate button above toggles. Changing toggles does not auto-regen.

From src/i18n/errors.ts add in this slice:
- Select at least one character set → 请至少选择一种字符集
- Length must be between 8 and 128 → 长度必须在 8 到 128 之间
Keep the lorem count key from 02-03.

From src/data/tools.ts after 02-03: 13 rows. Append password-generator. Bump snapshot to 14.

From ToolIsland.test.ts: every TOOLS slug needs slug === 'kebab' including password-generator.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Password-generator 8-file slice — RESEARCH CSPRNG lib, catalog, island, EN/ZH</name>
  <precondition>Plan 02-03 is complete: TOOLS snapshots at 13, lorem-ipsum ToolIsland branch exists, featured length is 6, ZH_ERRORS already contains the lorem count key</precondition>
  <reversibility rating="costly">Public catalog slug password-generator and the CSPRNG contract (Web Crypto rejection sampling) become the generator other pages link to; swapping the algorithm later is a coordinated lib plus FAQ change (D-10, PASS-04).</reversibility>
  <files>src/lib/password.test.ts, src/lib/password.ts, src/components/tools/PasswordGenerator.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/password-generator.md, src/content/tools/zh/password-generator.md</files>
  <read_first>src/lib/json.ts, src/lib/json.test.ts, src/components/tools/JsonFormatter.tsx, src/components/tools/UuidGenerator.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/json-formatter.md, src/components/tools/ToolIsland.test.ts, .planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md</read_first>
  <behavior>
    - generatePassword({ length: 16, lower: true, upper: true, digits: true, symbols: true, excludeSimilar: false }) ok true and password length 16
    - length 7 or 129 → { ok: false, error: 'Length must be between 8 and 128' }
    - all four charset flags false → { ok: false, error: 'Select at least one character set' }
    - digits-only length 8: every char in 0-9
    - symbols-only: every char in !@#$%^&*-_=+
    - excludeSimilar true with lower+digits: generated chars are not in i l 1 O 0
    - source-read src/lib/password.ts via readFileSync(new URL('./password.ts', import.meta.url)): includes getRandomValues; the substring Math.random is absent (same technique as ToolIsland.test.ts)
  </behavior>
  <action>
Ship the password-generator 8-file slice (PASS-01..06, D-02, D-03, D-10, D-13, D-14). Copy the RESEARCH CSPRNG rejection-sampling functions randomIndex and generatePassword into src/lib/password.ts — same constants LOWER UPPER DIGITS SYMBOLS SIMILAR, same limit check before remainder, same English error strings. Do not implement generation in the tsx. Do not clone UuidGenerator logic (no randomUUID for passwords). Do not add packages. Do not create a lib barrel. Do not rewrite existing ten relatedSlugs. Do not add entropy bits (deferred PASS-07). Do not require one character from each selected class. Leave unrelated dirty worktree files alone.

1. Write src/lib/password.test.ts from the behavior block first, then password.ts. Export generatePassword only (randomIndex can stay unexported). Source-read with readFileSync URL object, never URL.pathname. Assert getRandomValues is present. Assert Math.random is absent. Length 8 and 128 succeed when charset non-empty.
<!-- planner-discipline-allow: Math.random -->

2. Append TOOLS: slug password-generator, name Password Generator, category Generate, shortDescription Generate a password locally with Web Crypto., relatedSlugs uuid-generator, hash-generator, lorem-ipsum, featured false. Bump tools.test.ts 13 → 14. Featured stays 6.

3. PasswordGenerator.tsx: JsonFormatter wiring (locale, useToolUi, ToolShell, default export) plus Uuid-style placement for a generate button above toggles. State: length 16, lower/upper/digits/symbols true, excludeSimilar false. useEffect on mount calls generatePassword with current opts and sets output or localized error. Click generate does the same. Toggles and length edits do not auto-call generate. isTooLarge not required on numeric length; still do not pass huge strings into the lib. err() for lib errors. Copy via ToolShell output (PASS-05). class not className. CSPRNG stays in the lib.

4. ToolIsland.astro static import PasswordGenerator; slug === 'password-generator' client:load.

5. ui.en and ui.zh tools['password-generator']: name, shortDescription, length, lowercase, uppercase, digits, symbols, excludeSimilar, generate.

6. errors.ts add both English keys with the ZH strings from RESEARCH in this slice. Keep prior keys including the lorem count error.

7. EN+ZH markdown howTo 3, faq 3-5: nothing uploaded; not a password manager; empty charset shows an error; generation uses Web Crypto. Discretion on wording. No version-labeling language.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green at catalog length 14 featured 6; generatePassword matches RESEARCH errors and CSPRNG source-read; island generates via the lib on mount and click; ToolIsland branch present; ZH_ERRORS has both password keys; EN+ZH markdown exist.</done>
  <acceptance_criteria>
    - password.ts implements RESEARCH randomIndex rejection sampling and generatePassword
    - password.test.ts source-reads getRandomValues present and Math.random absent
    - empty charset and out-of-range length return the locked English errors, not ok true
    - excludeSimilar strips i l 1 O 0; symbols charset is !@#$%^&*-_=+
    - PasswordGenerator imports generatePassword from ../../lib/password; button sits above toggles; no auto-regen on toggle
    - TOOLS password-generator category Generate featured false; toHaveLength 14; featured 6
    - ToolIsland has slug === 'password-generator'; existing ten relatedSlugs unchanged; no lib barrel
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Charset membership, similar-exclusion, and empty-charset error paths</name>
  <files>src/lib/password.ts, src/lib/password.test.ts</files>
  <read_first>src/lib/password.ts, src/lib/password.test.ts, .planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md</read_first>
  <action>
Harden PASS-02, PASS-03, PASS-06 on the same generatePassword API (T-02-01, T-02-02, T-02-06). Do not change catalog or island files in this task. Do not add an entropy meter.

1. Lower-only, upper-only, digits-only, symbols-only membership tests at length 32.

2. excludeSimilar true with all sets on: sample a long password (length 128) and assert none of i l 1 O 0 appear.

3. excludeSimilar true with only similar-bearing sets turned into empty after filter (if a combination can empty the charset, it must return Select at least one character set, never ok true with empty password).

4. Keep source-read CSPRNG asserts (getRandomValues present, Math.random absent). Keep unbiased remainder only after the rejection limit (RESEARCH randomIndex).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>Charset and empty-after-filter tests green; CSPRNG source-read still green; npm test green at length 14.</done>
  <acceptance_criteria>
    - Each isolated charset only emits its own characters
    - Similar exclusion removes i l 1 O 0
    - Empty charset is ok false with Select at least one character set
    - Source-read still requires getRandomValues
  </acceptance_criteria>
</task>

<task type="auto">
  <name>EN/ZH generator chrome, ZH_ERRORS, and not-a-manager FAQ</name>
  <files>src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/password-generator.md, src/content/tools/zh/password-generator.md, src/components/tools/PasswordGenerator.tsx</files>
  <read_first>src/i18n/ui.ts, src/i18n/errors.ts, src/content/tools/password-generator.md, src/content/tools/zh/password-generator.md, src/components/tools/PasswordGenerator.tsx</read_first>
  <action>
Finish PASS-05 chrome and CAT-06 error localization (D-14). Do not bump catalog length past 14. Do not rewrite existing ten relatedSlugs. Do not add entropy FAQ as a product feature.

1. Island labels from copy.tools['password-generator']. err() covers both English lib errors.

2. FAQ both locales: local Web Crypto; not a password manager and nothing is stored; empty charset errors instead of a blank success; length 8–128. howTo 3.

3. Confirm Generate is type="button" above toggles; Copy uses ToolShell; output is text not HTML injection (T-02-04).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <human-check>Open /tools/password-generator/; a password appears on load; Copy; Generate again; turn all charset toggles off and Generate to see the empty-charset error; /zh/ localizes that error; homepage featured still six; catalog lists 14 tools.</human-check>
  </verify>
  <done>EN+ZH chrome and FAQ complete; both password ZH_ERRORS keys present; npm test green; catalog 14 featured 6; Phase 2 four tools shipped.</done>
  <acceptance_criteria>
    - ui en/zh tools['password-generator'] share length, lowercase, uppercase, digits, symbols, excludeSimilar, generate
    - ZH_ERRORS contains Select at least one character set and Length must be between 8 and 128
    - markdown faq states local generation and empty-charset error
    - npm test green with toHaveLength 14 and featured 6
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `generatePassword`, `PasswordResult` in `src/lib/password.ts` (RESEARCH randomIndex)
- `PasswordGenerator` default export in `src/components/tools/PasswordGenerator.tsx`
- Catalog slug `password-generator` in `TOOLS`
- `ui.en.tools['password-generator']` and `ui.zh.tools['password-generator']`
- ZH_ERRORS keys `Select at least one character set` and `Length must be between 8 and 128`
- `src/content/tools/password-generator.md` and `src/content/tools/zh/password-generator.md`

Phase 2 also leaves (from prior plans in this phase): `countText` / `countWordsFallback`, `convertCases` / `slugify`, `WORDS` / `generateLorem`, catalog length 14, featured 6.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor toggles → generatePassword | Untrusted options; empty charset must not look like success |
| CSPRNG → password string | Web Crypto in-browser; no server |
| output → clipboard | ToolShell text copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information disclosure | src/lib/password.ts | high | mitigate | Copy RESEARCH randomIndex: crypto.getRandomValues only; source-read forbids a non-CSPRNG PRNG |
| T-02-02 | Tampering | randomIndex remainder | high | mitigate | Reject x >= limit before remainder so charset indexes are unbiased |
| T-02-03 | Denial of service | length bounds | medium | mitigate | Length 8–128 English error; island default 16 |
| T-02-04 | Tampering | PasswordGenerator output | medium | mitigate | Password string in ToolShell text; no HTML injection APIs |
| T-02-05 | Information disclosure | password slice | low | accept | No network in this tool; static site; FAQ states nothing uploaded |
| T-02-06 | Elevation of privilege / misleading | empty charset | high | mitigate | Return Select at least one character set; never ok true with empty password |
| T-02-SC | Tampering | npm installs | high | accept | No package-manager installs this plan |
</threat_model>

<verification>
npm test after every task. Completeness at length 14. CSPRNG source-read. Empty charset error. Featured 6. No Playwright. Phase gate may later run npm run build; this plan's automated verify is npm test.
</verification>

<success_criteria>
- PASS-01..06 delivered for slug password-generator
- RESEARCH CSPRNG algorithm in password.ts with source-read
- Catalog 14, featured 6, existing ten relatedSlugs untouched, no lib barrel
- npm test green
</success_criteria>

<output>
Create `.planning/phases/02-light-text-and-generate-tools/02-04-SUMMARY.md` when done
</output>
