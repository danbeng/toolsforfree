---
phase: 02-light-text-and-generate-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/counter.ts
  - src/lib/counter.test.ts
  - src/components/tools/WordCounter.tsx
  - src/components/tools/ToolIsland.astro
  - src/data/tools.ts
  - src/data/tools.test.ts
  - src/i18n/ui.ts
  - src/content/tools/word-counter.md
  - src/content/tools/zh/word-counter.md
autonomous: true
requirements:
  - COUNT-01
  - COUNT-02
  - COUNT-03
  - COUNT-04

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor can open /tools/word-counter/ (and /zh/tools/word-counter/) and paste text; tiles show word count, characters with spaces, characters without spaces, and line count (COUNT-01, D-12)"
    - "Tiles also show sentence and paragraph counts; sentences split on . ? ! and fullwidth 。？！; paragraphs are non-empty blocks split on blank lines (COUNT-02, D-05)"
    - "CJK Han is not one word: countWords and countWordsFallback both count 你好世界 as 4 and hello 世界 as 3 (COUNT-03, D-04)"
    - "Empty or whitespace-only input returns ok true with all metrics 0, not an idle error string"
    - "Counts recompute from textarea onInput via useMemo after isTooLarge; ToolShell output is a copyable summary while metrics render as tiles in children, not metrics-only in pre (COUNT-04, D-12, T-02-03)"
    - "Catalog has slug word-counter, category Text, featured false; TOOLS toHaveLength 11; getFeaturedTools length stays 6; existing ten relatedSlugs unchanged (D-03, D-13)"
    - "ToolIsland.astro has a static WordCounter import and slug === 'word-counter' branch; EN and ZH markdown exist with howTo length 3 and faq 3-5"
  artifacts:
    - path: src/lib/counter.ts
      provides: "countText, countWords, countWordsFallback; CounterResult ok-true metrics"
      exports: ["countText", "countWords", "countWordsFallback"]
    - path: src/lib/counter.test.ts
      provides: "English metrics, CJK Han 4 words, fallback Han, empty zeros, sentences/paragraphs"
    - path: src/components/tools/WordCounter.tsx
      provides: "Default-export island; locale; useToolUi; isTooLarge; tiles; ToolShell"
    - path: src/components/tools/ToolIsland.astro
      provides: "static import plus slug-equals branch for word-counter"
    - path: src/data/tools.ts
      provides: "word-counter catalog row featured false category Text"
    - path: src/data/tools.test.ts
      provides: "toHaveLength 11; featured still 6"
    - path: src/i18n/ui.ts
      provides: "en and zh tools['word-counter'] chrome keys"
    - path: src/content/tools/word-counter.md
      provides: "locale en SEO howTo faq"
    - path: src/content/tools/zh/word-counter.md
      provides: "locale zh SEO howTo faq"
  key_links:
    - from: src/components/tools/WordCounter.tsx
      to: src/lib/counter.ts
      via: "countText after isTooLarge"
      pattern: "countText"
    - from: src/components/tools/WordCounter.tsx
      to: src/lib/limits.ts
      via: "isTooLarge(input) before lib"
      pattern: "isTooLarge"
    - from: src/components/tools/ToolIsland.astro
      to: src/components/tools/WordCounter.tsx
      via: "static import and slug === 'word-counter' client:load"
      pattern: "slug === 'word-counter'"
    - from: src/data/tools.test.ts
      to: src/data/tools.ts
      via: "expect(TOOLS).toHaveLength(11)"
      pattern: "toHaveLength\\(11\\)"
    - from: src/lib/counter.ts
      to: Intl.Segmenter
      via: "granularity word plus isWordLike; countWordsFallback exported"
      pattern: "countWordsFallback"
---

## Phase Goal

**As a** visitor using Devtoolbox, **I want to** paste text on word-counter and see live word, character, line, sentence, and paragraph counts including CJK, **so that** I can measure text entirely in the browser without uploading anything.

<objective>
Ship the word-counter 8-file vertical slice at existing-tool parity (COUNT-01, COUNT-02, COUNT-03, COUNT-04). Tracer-first for Phase 2 (D-01). Clone JsonFormatter plus src/lib/json.ts, not UuidGenerator (D-02). Category Text, featured false (D-03). Word count via Intl.Segmenter with exported countWordsFallback (D-04). Metric tiles in children, copyable summary on ToolShell (D-12). Do not rewrite existing ten relatedSlugs (D-13). No English lib errors this slice so no ZH_ERRORS keys (D-14). Do not create a lib barrel. Zero new npm packages.
Purpose: Prove the Phase 1 completeness harness on the first product tool and give visitors a working counter.
Output: counter lib/test, WordCounter island, ToolIsland branch, catalog row, ui.ts EN+ZH, EN+ZH markdown; tools.test.ts length 11.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-light-text-and-generate-tools/02-CONTEXT.md
@.planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md
@.planning/phases/02-light-text-and-generate-tools/02-PATTERNS.md
@.planning/phases/01-additive-tool-contract/01-01-SUMMARY.md
@src/lib/json.ts
@src/lib/json.test.ts
@src/lib/limits.ts
@src/components/tools/JsonFormatter.tsx
@src/components/ToolShell.tsx
@src/components/tools/ToolIsland.astro
@src/components/tools/ToolIsland.test.ts
@src/data/tools.ts
@src/data/tools.test.ts
@src/i18n/ui.ts
@src/i18n/useToolUi.ts
@src/i18n/locales.ts
@src/content.config.ts
@src/content/tools/json-formatter.md
@src/styles/global.css
@.planning/codebase/CONVENTIONS.md

<interfaces>
From src/lib/json.ts: JsonResult is ok-true with formatted or ok-false with error string; empty trim returns ok false and empty error; named export formatJson.

From src/components/tools/JsonFormatter.tsx: default export; props { locale: Locale }; useState input; useToolUi; isTooLarge first; useMemo; ToolShell error/output/locale; textarea onInput with HTMLTextAreaElement cast; spellcheck false; class not className.

From src/lib/limits.ts: INPUT_MAX_CHARS 100000; isTooLarge(input) is input.length greater than that. Do not edit this file.

From src/data/tools.ts: ToolCategory includes Text; Tool has slug, name, category, shortDescription, relatedSlugs, featured; TOOLS array; getRelatedTools maps slugs and drops missing. Append only. Do not edit the existing ten objects' relatedSlugs.

From src/data/tools.test.ts: it has exactly 10 tools uses toHaveLength(10) — bump to 11 this plan; featured toHaveLength(6) stays; EN+ZH existsSync loop already covers new slugs.

From src/components/tools/ToolIsland.astro: static import plus slug === 'kebab' && Component client:load locale={locale}. Light tools stay static.

From src/content.config.ts: howTo is a tuple of exactly 3 strings; faq array min 3 max 5; locale en or zh.

From src/i18n/locales.ts: Locale en | zh; ZH htmlLang is zh-Hans. Pass en or zh-Hans into Segmenter (D-04).

From src/i18n/ui.ts: ui.en.tools and ui.zh.tools must share keys (as const UiDict). Add word-counter on both.

From src/styles/global.css: reuse .card-grid and .tool-card for tiles (discretion). Do not add a CSS module.

From CONVENTIONS 8-file checklist: catalog row featured false; lib+test; island; ToolIsland branch; ui.ts EN+ZH; ZH_ERRORS if new English errors; EN md; ZH md.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end word-counter — English paste through catalog, lib, island, and EN/ZH page</name>
  <precondition>Phase 1 completeness harness exists: src/data/tools.test.ts still snapshots toHaveLength(10) and loops EN+ZH markdown; src/components/tools/ToolIsland.test.ts source-reads slug-equals branches; existing ZH markdown for the current ten slugs remains on disk</precondition>
  <reversibility rating="costly">Public catalog slug word-counter becomes an SSG route; renaming later needs content, island, and i18n updates together (D-01, D-03).</reversibility>
  <files>src/lib/counter.test.ts, src/lib/counter.ts, src/components/tools/WordCounter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/content/tools/word-counter.md, src/content/tools/zh/word-counter.md</files>
  <read_first>src/lib/json.ts, src/lib/json.test.ts, src/components/tools/JsonFormatter.tsx, src/components/tools/ToolIsland.astro, src/data/tools.ts, src/data/tools.test.ts, src/i18n/ui.ts, src/content/tools/json-formatter.md, src/content.config.ts, src/lib/limits.ts, .planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md, .planning/codebase/CONVENTIONS.md</read_first>
  <action>
Land the thinnest production word-counter path through every layer this phase touches (COUNT-01, COUNT-04, D-01, D-02, D-03, D-12, D-13). Do not clone UuidGenerator. Do not add packages. Do not edit ToolShell.tsx, limits.ts, json.ts, JsonFormatter.tsx, or the existing ten TOOLS relatedSlugs. Do not create a src/lib barrel. Stage only this plan's files_modified; leave unrelated dirty worktree files alone.

1. Create src/lib/counter.test.ts then src/lib/counter.ts (tests first, then lib, then npm test green). Named exports only. Export type CounterResult as ok true plus numeric words, charsWithSpaces, charsWithoutSpaces, lines, sentences, paragraphs. Export countText(text, localeTag = 'en') returning always ok true (tiles, not idle error). Empty or whitespace-only text: all metrics 0. charsWithSpaces is text.length (UTF-16, same unit as INPUT_MAX_CHARS). charsWithoutSpaces is text with whitespace stripped via replace on \s. lines: empty string 0, else split on CR LF or LF or CR. words via countWords. Implement countWords with feature detect typeof Intl.Segmenter === 'function', new Intl.Segmenter(localeTag, granularity word), count segments where isWordLike is true; else countWordsFallback. Export countWordsFallback now so architecture does not change later (D-04): trim, split on whitespace, split tokens on Script=Han, each Han ideograph one word and each non-Han run one word. English cases this task must pass: hello world is 2 words; empty and spaces-only are 0; a two-line string has lines 2. Sentences and paragraphs may return 0 in this task if unused by the island yet, but keep the fields on the result object.

2. Append one TOOLS row only, do not reorder or edit existing objects (D-13): slug word-counter, name Word Counter, category Text, shortDescription Count words, characters, lines, sentences, and paragraphs locally., relatedSlugs case-converter, regex-tester, lorem-ipsum (forward slugs are OK because getRelatedTools drops missing), featured false (D-03). In src/data/tools.test.ts change the snapshot from 10 to 11 and rename that it() to has exactly 11 tools. Do not change featured length 6.

3. Create src/components/tools/WordCounter.tsx by cloning JsonFormatter structure: default export, locale prop, useState textarea, useToolUi, isTooLarge first yielding tooLarge and empty output (T-02-03), else countText(input, locale === 'zh' ? 'zh-Hans' : 'en'). Pass a newline-joined summary string as ToolShell output so Copy works (CAT-06). Render COUNT-01 tiles in children using existing global card-grid and tool-card classes (D-12): words, chars with spaces, chars without spaces, lines. Textarea labeled from ui copy. Use class not className. Do not put the only metrics in ToolShell pre.

4. In ToolIsland.astro add a static import of WordCounter and a branch slug === 'word-counter' with client:load and locale. Keep existing ten branches. No dynamic component tags.

5. Add ui.en.tools['word-counter'] and ui.zh.tools['word-counter'] with the same keys: name, shortDescription, text, words, chars, charsNoSpaces, lines (sentences and paragraphs keys may be added in task 2). Mirror structure on both locales.

6. Add src/content/tools/word-counter.md locale en and src/content/tools/zh/word-counter.md locale zh. Copy json-formatter frontmatter shape: title, description, intro, howTo exactly 3 steps, faq 3 items. Privacy FAQ: compute runs in the browser, nothing uploaded. Do not use version-labeling language in FAQ. Do not mention later-phase tools.

No Playwright. No tsx tests. No ZH_ERRORS keys (no English error strings in this lib).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>npm test green with TOOLS length 11 and featured 6; word-counter catalog row featured false; ToolIsland contains slug === 'word-counter'; EN+ZH markdown exist; countText counts English words/chars/lines; WordCounter island uses isTooLarge, countText, tiles, and ToolShell; existing ten relatedSlugs unchanged.</done>
  <acceptance_criteria>
    - src/lib/counter.ts exports countText, countWords, and countWordsFallback; empty input is ok true with zeros
    - src/lib/counter.test.ts covers hello world words 2, empty zeros, and line split
    - TOOLS includes word-counter category Text featured false; tools.test.ts toHaveLength 11; getFeaturedTools still length 6
    - ToolIsland.astro static-imports WordCounter and has slug === 'word-counter'
    - WordCounter.tsx default-exports, calls isTooLarge then countText, renders COUNT-01 tiles in children, passes a copyable summary as output
    - EN and ZH markdown exist with howTo length 3 and faq 3-5
    - Existing ten TOOLS objects' relatedSlugs bytes unchanged; no lib barrel file created; no new npm packages
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Sentences, paragraphs, CJK Segmenter, and countWordsFallback Han cases</name>
  <files>src/lib/counter.ts, src/lib/counter.test.ts, src/components/tools/WordCounter.tsx, src/i18n/ui.ts</files>
  <read_first>src/lib/counter.ts, src/lib/counter.test.ts, src/components/tools/WordCounter.tsx, src/i18n/ui.ts, .planning/phases/02-light-text-and-generate-tools/02-RESEARCH.md</read_first>
  <action>
Expand the proven counter into COUNT-02 and COUNT-03 without changing architecture (D-04, D-05, D-12). Do not add packages. Do not edit ToolIsland.astro or tools.ts in this task.

1. In countText, implement sentences: after trim, if empty 0; else count non-empty pieces split on the class of . ? ! and fullwidth 。？！ (D-05). Paragraphs: split on blank lines (newline, optional whitespace, newline) and count pieces whose trim is non-empty; empty document 0.

2. Keep countWords on Intl.Segmenter when present. Keep exported countWordsFallback. Tests must call countWordsFallback directly so Node 22 still exercises the no-Segmenter path. Required expects: 你好世界 is 4 on both countWords and countWordsFallback; hello 世界 is 3 on both; hello world is 2 on both; empty is 0. Do not treat a Chinese paragraph as 1 word via whitespace-only split.

3. Add counter.test.ts cases for sentences (Hello. World! plus a fullwidth 。 string) and paragraphs (two blocks separated by a blank line).

4. WordCounter tiles: add sentences and paragraphs using card-grid / tool-card. Add matching keys on en and zh ui tools['word-counter']: sentences, paragraphs. Include them in the copyable ToolShell summary. Still do not dump metrics only into pre.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>Han 你好世界 is 4 words on Segmenter and fallback; sentences and paragraph metrics match D-05 and blank-line rule; island shows all six tiles; npm test green.</done>
  <acceptance_criteria>
    - countWordsFallback is exported and tested for 你好世界 equals 4 and hello 世界 equals 3
    - countWords on the same strings matches fallback on this Node
    - Sentence split includes ASCII . ? ! and fullwidth 。？！
    - Paragraphs count non-empty blank-line blocks
    - Island children include tiles for words, chars plus/minus spaces, lines, sentences, paragraphs
  </acceptance_criteria>
</task>

<task type="auto">
  <name>EN/ZH chrome, CJK FAQ, and completeness stay green at 11</name>
  <files>src/i18n/ui.ts, src/content/tools/word-counter.md, src/content/tools/zh/word-counter.md, src/components/tools/WordCounter.tsx</files>
  <read_first>src/i18n/ui.ts, src/content/tools/word-counter.md, src/content/tools/zh/word-counter.md, src/content/tools/json-formatter.md, src/components/tools/WordCounter.tsx, src/i18n/errors.ts</read_first>
  <action>
Finish catalog parity chrome for word-counter (D-14, COUNT-04). Counter lib has no English error strings; do not add unused ZH_ERRORS keys. Do not edit errors.ts. Do not bump catalog length again. Do not rewrite existing ten relatedSlugs.

1. Ensure ui.en.tools['word-counter'] and ui.zh.tools['word-counter'] both have name, shortDescription, text, words, chars, charsNoSpaces, lines, sentences, paragraphs. Island reads labels from copy.tools['word-counter'] like JsonFormatter.

2. FAQ (3-5 items) on both markdown files: nothing uploaded; CJK counted with Unicode segmentation so a Chinese paragraph is not one word; counts update as you type. howTo remains exactly 3. Discretion on wording. No later-phase tools. No version-labeling language.

3. Confirm WordCounter still uses useMemo over input so typing updates tiles (COUNT-04). Size guard remains isTooLarge before countText (T-02-03). Render tile numbers as text, not HTML injection APIs (T-02-04).

4. Do not add Playwright or tsx tests. Completeness is already covered by existing ToolIsland.test.ts and tools.test.ts existsSync once files from task 1 exist.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <human-check>Open /tools/word-counter/ (and /zh/tools/word-counter/ if running the site): type English and 你好世界; tiles update live; Copy uses the summary string; homepage featured count still six.</human-check>
  </verify>
  <done>EN+ZH ui keys and markdown FAQ cover privacy plus CJK counting; island live-updates; npm test green; featured still 6; length 11.</done>
  <acceptance_criteria>
    - en and zh ui tools['word-counter'] share the metric label keys the island reads
    - Both markdown files have locale, howTo length 3, faq 3-5, and a CJK counting answer
    - WordCounter uses useMemo plus onInput and isTooLarge
    - npm test still green with toHaveLength 11 and featured 6
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

Slice symbols and paths created by this plan (later Phase 2 plans append their own; they do not consume these types):

- `countText`, `countWords`, `countWordsFallback`, `CounterResult` in `src/lib/counter.ts`
- `WordCounter` default export in `src/components/tools/WordCounter.tsx`
- Catalog slug `word-counter` in `TOOLS`
- `ui.en.tools['word-counter']` and `ui.zh.tools['word-counter']`
- `src/content/tools/word-counter.md` and `src/content/tools/zh/word-counter.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor paste → WordCounter island | Untrusted textarea crosses into in-browser lib only |
| island → ToolShell output | Counts rendered as text; Copy uses clipboard |
| static SSG → browser | No tool API route; no fetch in this slice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Denial of service | WordCounter.tsx / isTooLarge | medium | mitigate | Call isTooLarge on the textarea before countText; INPUT_MAX_CHARS stays 100000 in limits.ts (do not edit) |
| T-02-04 | Tampering | WordCounter tiles / ToolShell | medium | mitigate | Render metrics as text nodes in tiles and ToolShell code/pre; do not assign HTML strings into the DOM |
| T-02-SC | Tampering | npm installs | high | accept | No package-manager installs this plan; zero new dependencies |
</threat_model>

<verification>
npm test after every task (inherited Phase 1 command). Completeness: ToolIsland slug-equals loop and EN+ZH existsSync must pass at length 11. Featured length 6. Han and fallback asserts in counter.test.ts. Do not run Playwright. Phase gate later may add npm run build; this plan's automated verify is npm test only.
</verification>

<success_criteria>
- COUNT-01..04 delivered for slug word-counter at 8-file parity
- CJK 你好世界 is 4 words on countWords and countWordsFallback
- Catalog 11, featured 6, existing ten relatedSlugs untouched
- npm test green
</success_criteria>

<output>
Create `.planning/phases/02-light-text-and-generate-tools/02-01-SUMMARY.md` when done
</output>
